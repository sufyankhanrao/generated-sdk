"""pagarmeapisdk.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from pagarmeapisdk.api_helper import APIHelper
from pagarmeapisdk.models.create_card_request import (
    CreateCardRequest,
)


class UpdateChargeCardRequest(object):
    """Implementation of the 'UpdateChargeCardRequest' model.

    Request for updating card data

    Attributes:
        update_subscription (bool): Indicates if the subscriptions using this
            card must also be updated
        card_id (str): Card id
        card (CreateCardRequest): Card data
        recurrence (bool): Indicates a recurrence
        initiated_type (str): The model property of type str.
        recurrence_model (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "update_subscription": "update_subscription",
        "card_id": "card_id",
        "card": "card",
        "recurrence": "recurrence",
        "initiated_type": "initiated_type",
        "recurrence_model": "recurrence_model",
    }

    _optionals = [
        "initiated_type",
        "recurrence_model",
    ]

    def __init__(self,
                 update_subscription=None,
                 card_id=None,
                 card=None,
                 recurrence=None,
                 initiated_type=APIHelper.SKIP,
                 recurrence_model=APIHelper.SKIP):
        """Initialize a UpdateChargeCardRequest instance."""
        # Initialize members of the class
        self.update_subscription = update_subscription
        self.card_id = card_id
        self.card = card
        self.recurrence = recurrence
        if initiated_type is not APIHelper.SKIP:
            self.initiated_type = initiated_type
        if recurrence_model is not APIHelper.SKIP:
            self.recurrence_model = recurrence_model

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        update_subscription =\
            dictionary.get("update_subscription")\
            if "update_subscription" in dictionary.keys() else None
        card_id = dictionary.get("card_id") if dictionary.get("card_id") else None
        card = CreateCardRequest.from_dictionary(
            dictionary.get("card"))\
            if dictionary.get("card") else None
        recurrence =\
            dictionary.get("recurrence")\
            if "recurrence" in dictionary.keys() else None
        initiated_type =\
            dictionary.get("initiated_type")\
            if dictionary.get("initiated_type") else APIHelper.SKIP
        recurrence_model =\
            dictionary.get("recurrence_model")\
            if dictionary.get("recurrence_model") else APIHelper.SKIP
        # Return an object of this model
        return cls(update_subscription,
                   card_id,
                   card,
                   recurrence,
                   initiated_type,
                   recurrence_model)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"update_subscription={self.update_subscription!r}, "
                f"card_id={self.card_id!r}, "
                f"card={self.card!r}, "
                f"recurrence={self.recurrence!r}, "
                f"initiated_type={(self.initiated_type
                     if hasattr(self, 'initiated_type') else None)!r}, "
                f"recurrence_model={(self.recurrence_model
                     if hasattr(self, 'recurrence_model') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"update_subscription={self.update_subscription!s}, "
                f"card_id={self.card_id!s}, "
                f"card={self.card!s}, "
                f"recurrence={self.recurrence!s}, "
                f"initiated_type={(self.initiated_type
                     if hasattr(self, 'initiated_type') else None)!s}, "
                f"recurrence_model={(self.recurrence_model
                     if hasattr(self, 'recurrence_model') else None)!s})")
