"""paginationtester.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from paginationtester.api_helper import APIHelper
from paginationtester.models.links import Links
from paginationtester.models.transaction import (
    Transaction,
)


class Transactions(object):
    """Implementation of the 'Transactions' model.

    Attributes:
        data (List[Transaction]): The model property of type List[Transaction].
        links (Links): The model property of type Links.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "data": "data",
        "links": "links",
    }

    _optionals = [
        "data",
        "links",
    ]

    def __init__(
        self,
        data=APIHelper.SKIP,
        links=APIHelper.SKIP):
        """Initialize a Transactions instance."""
        # Initialize members of the class
        if data is not APIHelper.SKIP:
            self.data = data
        if links is not APIHelper.SKIP:
            self.links = links

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        data = None
        if dictionary.get("data") is not None:
            data = [
                Transaction.from_dictionary(x)
                    for x in dictionary.get("data")
            ]
        else:
            data = APIHelper.SKIP
        links =\
            Links.from_dictionary(
            dictionary.get("links"))\
                if "links" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(data,
                   links)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _data=(
            self.data
            if hasattr(self, "data")
            else None
        )
        _links=(
            self.links
            if hasattr(self, "links")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"data={_data!r}"
            f"links={_links!r}"
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _data=(
            self.data
            if hasattr(self, "data")
            else None
        )
        _links=(
            self.links
            if hasattr(self, "links")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"data={_data!s}"
            f"links={_links!s}"
            f")"
        )
