"""webhooksandcallbacksapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from webhooksandcallbacksapi.api_helper import (
    APIHelper,
)


class WebhookRegistration(object):
    """Implementation of the 'WebhookRegistration' model.

    Attributes:
        url (str): The endpoint URL that will receive webhook events
        events (List[EventEnum]): List of events to subscribe to
        secret (str): Secret key for webhook signature verification
        active (bool): Whether the webhook is active

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "url": "url",
        "events": "events",
        "secret": "secret",
        "active": "active",
    }

    _optionals = [
        "secret",
        "active",
    ]

    def __init__(
        self,
        url=None,
        events=None,
        secret=APIHelper.SKIP,
        active=True):
        """Initialize a WebhookRegistration instance."""
        # Initialize members of the class
        self.url = url
        self.events = events
        if secret is not APIHelper.SKIP:
            self.secret = secret
        self.active = active

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        url =\
            dictionary.get("url")\
            if dictionary.get("url")\
                else None
        events =\
            dictionary.get("events")\
            if dictionary.get("events")\
                else None
        secret =\
            dictionary.get("secret")\
            if dictionary.get("secret")\
                else APIHelper.SKIP
        active =\
            dictionary.get("active")\
            if dictionary.get("active")\
                else True

        # Return an object of this model
        return cls(url,
                   events,
                   secret,
                   active)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _url=self.url
        _events=self.events
        _secret=(
            self.secret
            if hasattr(self, "secret")
            else None
        )
        _active=(
            self.active
            if hasattr(self, "active")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"url={_url!r}"
            f"events={_events!r}"
            f"secret={_secret!r}"
            f"active={_active!r}"
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _url=self.url
        _events=self.events
        _secret=(
            self.secret
            if hasattr(self, "secret")
            else None
        )
        _active=(
            self.active
            if hasattr(self, "active")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"url={_url!s}"
            f"events={_events!s}"
            f"secret={_secret!s}"
            f"active={_active!s}"
            f")"
        )
