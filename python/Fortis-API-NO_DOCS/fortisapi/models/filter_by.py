# -*- coding: utf-8 -*-

"""
fortisapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from fortisapi.api_helper import APIHelper


class FilterBy(object):

    """Implementation of the 'filter_by' model.

    Attributes:
        key (str): Resource key to filter by
        operator (Operator1Enum): Filter operand.  Please note `=` does not
            imply a strict equality check.   Only time fields support the `<=`
            and `>=` operators.
        value (float | str | bool): Filter value.   For time fields, `value`
            must be sent in Unix format: `946702799`
        additional_properties (Dict[str, object]): The additional properties
            for the model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "key": 'key',
        "operator": 'operator',
        "value": 'value'
    }

    def __init__(self,
                 key=None,
                 operator=None,
                 value=None,
                 additional_properties=None):
        """Constructor for the FilterBy class"""

        # Initialize members of the class
        self.key = key 
        self.operator = operator 
        self.value = value 

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from fortisapi.utilities.union_type_lookup import UnionTypeLookUp

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        key = dictionary.get("key") if dictionary.get("key") else None
        operator = APIHelper.deserialize_union_type(UnionTypeLookUp.get('FilterByOperator'), dictionary.get('operator'), False) if dictionary.get('operator') is not None else None
        value = APIHelper.deserialize_union_type(UnionTypeLookUp.get('FilterByValue'), dictionary.get('value'), False) if dictionary.get('value') is not None else None
        # Clean out expected properties from dictionary
        additional_properties = {k: v for k, v in dictionary.items() if k not in cls._names.values()}
        # Return an object of this model
        return cls(key,
                   operator,
                   value,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        from fortisapi.utilities.union_type_lookup import UnionTypeLookUp

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.key,
                                           type_callable=lambda value: isinstance(value, str)) \
                and UnionTypeLookUp.get('FilterByOperator').validate(dictionary.operator).is_valid \
                and UnionTypeLookUp.get('FilterByValue').validate(dictionary.value).is_valid

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('key'),
                                       type_callable=lambda value: isinstance(value, str)) \
            and UnionTypeLookUp.get('FilterByOperator').validate(dictionary.get('operator')).is_valid \
            and UnionTypeLookUp.get('FilterByValue').validate(dictionary.get('value')).is_valid

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'key={self.key!r}, '
                f'operator={self.operator!r}, '
                f'value={self.value!r}, '
                f'additional_properties={self.additional_properties!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'key={self.key!s}, '
                f'operator={self.operator!s}, '
                f'value={self.value!s}, '
                f'additional_properties={self.additional_properties!s})')
