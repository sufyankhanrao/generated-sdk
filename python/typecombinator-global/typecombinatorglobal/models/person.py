# -*- coding: utf-8 -*-

"""
typecombinatorglobal

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from typecombinatorglobal.api_helper import APIHelper


class Person(object):

    """Implementation of the 'Person' model.

    Attributes:
        mtype (str): The model property of type str.
        name (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "name": 'name',
        "mtype": 'type'
    }

    _optionals = [
        'mtype',
    ]

    def __init__(self,
                 name=None,
                 mtype='Person'):
        """Constructor for the Person class"""

        # Initialize members of the class
        self.mtype = mtype 
        self.name = name 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        discriminators = {
            'Empl': Employee.from_dictionary,
            'Entr': Entrepreneur.from_dictionary,
            'Product Manager': ProductManager.from_dictionary,
            'Software Developer': SoftwareDeveloper.from_dictionary
        }
        unboxer = discriminators.get(dictionary.get('type'))

        # Delegate unboxing to another function if a discriminator
        # value for a child class is present.
        if unboxer:
            return unboxer(dictionary)

        # Extract variables from the dictionary
        name = dictionary.get("name") if dictionary.get("name") else None
        mtype = dictionary.get("type") if dictionary.get("type") else 'Person'
        # Return an object of this model
        return cls(name,
                   mtype)

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'mtype={(self.mtype if hasattr(self, "mtype") else None)!r}, '
                f'name={self.name!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'mtype={(self.mtype if hasattr(self, "mtype") else None)!s}, '
                f'name={self.name!s})')

class Employee(Person):

    """Implementation of the 'Employee' model.
    NOTE: This class inherits from 'Person'.

    Attributes:
        ssn (float): The model property of type float.
        salary (float): The model property of type float.
        build (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "ssn": 'ssn',
        "name": 'name',
        "salary": 'salary',
        "build": 'build',
        "mtype": 'type'
    }

    _optionals = [
        'salary',
        'build',
    ]
    _optionals.extend(Person._optionals)

    def __init__(self,
                 ssn=None,
                 name=None,
                 salary=APIHelper.SKIP,
                 build=APIHelper.SKIP,
                 mtype='Empl'):
        """Constructor for the Employee class"""

        # Initialize members of the class
        self.ssn = ssn 
        if salary is not APIHelper.SKIP:
            self.salary = salary 
        if build is not APIHelper.SKIP:
            self.build = build 

        # Call the constructor for the base class
        super(Employee, self).__init__(name,
                                       mtype)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        discriminators = {
            'Product Manager': ProductManager.from_dictionary,
            'Software Developer': SoftwareDeveloper.from_dictionary
        }
        unboxer = discriminators.get(dictionary.get('type'))

        # Delegate unboxing to another function if a discriminator
        # value for a child class is present.
        if unboxer:
            return unboxer(dictionary)

        # Extract variables from the dictionary
        ssn = dictionary.get("ssn") if dictionary.get("ssn") else None
        name = dictionary.get("name") if dictionary.get("name") else None
        salary = dictionary.get("salary") if dictionary.get("salary") else APIHelper.SKIP
        build = dictionary.get("build") if dictionary.get("build") else APIHelper.SKIP
        mtype = dictionary.get("type") if dictionary.get("type") else 'Empl'
        # Return an object of this model
        return cls(ssn,
                   name,
                   salary,
                   build,
                   mtype)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.ssn,
                                           type_callable=lambda value: isinstance(value, float)) \
                and APIHelper.is_valid_type(value=dictionary.name,
                                            type_callable=lambda value: isinstance(value, str))

        if not isinstance(dictionary, dict):
            return False

        discriminators = {
            'Product Manager': ProductManager.validate,
            'Software Developer': SoftwareDeveloper.validate
        }

        validator  = discriminators.get(dictionary.get('type'))

        # Delegate validation to another function if a discriminator
        # value for a child class is present.
        if validator :
            return validator(dictionary)

        return APIHelper.is_valid_type(value=dictionary.get('ssn'),
                                       type_callable=lambda value: isinstance(value, float)) \
            and APIHelper.is_valid_type(value=dictionary.get('name'),
                                        type_callable=lambda value: isinstance(value, str))

    def __repr__(self):
        base_repr = super().__repr__()
        return (f'{self.__class__.__name__}('
                f'{base_repr[base_repr.find("(") + 1:-1]}, '
                f'ssn={self.ssn!r}, '
                f'salary={(self.salary if hasattr(self, "salary") else None)!r}, '
                f'build={(self.build if hasattr(self, "build") else None)!r})')

    def __str__(self):
        base_str = super().__str__()
        return (f'{self.__class__.__name__}('
                f'{base_str[base_str.find("(") + 1:-1]}, '
                f'ssn={self.ssn!s}, '
                f'salary={(self.salary if hasattr(self, "salary") else None)!s}, '
                f'build={(self.build if hasattr(self, "build") else None)!s})')

class Entrepreneur(Person):

    """Implementation of the 'Entrepreneur' model.
    NOTE: This class inherits from 'Person'.

    Attributes:
        number_of_startups (float): The model property of type float.
        build (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "number_of_startups": 'number_of_startups',
        "name": 'name',
        "build": 'build',
        "mtype": 'type'
    }

    _optionals = [
        'build',
    ]
    _optionals.extend(Person._optionals)

    def __init__(self,
                 number_of_startups=None,
                 name=None,
                 build=APIHelper.SKIP,
                 mtype='Entr'):
        """Constructor for the Entrepreneur class"""

        # Initialize members of the class
        self.number_of_startups = number_of_startups 
        if build is not APIHelper.SKIP:
            self.build = build 

        # Call the constructor for the base class
        super(Entrepreneur, self).__init__(name,
                                           mtype)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        number_of_startups = dictionary.get("number_of_startups") if dictionary.get("number_of_startups") else None
        name = dictionary.get("name") if dictionary.get("name") else None
        build = dictionary.get("build") if dictionary.get("build") else APIHelper.SKIP
        mtype = dictionary.get("type") if dictionary.get("type") else 'Entr'
        # Return an object of this model
        return cls(number_of_startups,
                   name,
                   build,
                   mtype)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.number_of_startups,
                                           type_callable=lambda value: isinstance(value, float)) \
                and APIHelper.is_valid_type(value=dictionary.name,
                                            type_callable=lambda value: isinstance(value, str))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('number_of_startups'),
                                       type_callable=lambda value: isinstance(value, float)) \
            and APIHelper.is_valid_type(value=dictionary.get('name'),
                                        type_callable=lambda value: isinstance(value, str))

    def __repr__(self):
        base_repr = super().__repr__()
        return (f'{self.__class__.__name__}('
                f'{base_repr[base_repr.find("(") + 1:-1]}, '
                f'number_of_startups={self.number_of_startups!r}, '
                f'build={(self.build if hasattr(self, "build") else None)!r})')

    def __str__(self):
        base_str = super().__str__()
        return (f'{self.__class__.__name__}('
                f'{base_str[base_str.find("(") + 1:-1]}, '
                f'number_of_startups={self.number_of_startups!s}, '
                f'build={(self.build if hasattr(self, "build") else None)!s})')

class ProductManager(Employee):

    """Implementation of the 'ProductManager' model.
    NOTE: This class inherits from 'Employee'.

    Attributes:
        number_of_reportees (float): The model property of type float.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "number_of_reportees": 'number_of_reportees',
        "ssn": 'ssn',
        "name": 'name',
        "salary": 'salary',
        "build": 'build',
        "mtype": 'type'
    }

    def __init__(self,
                 number_of_reportees=None,
                 ssn=None,
                 name=None,
                 salary=APIHelper.SKIP,
                 build=APIHelper.SKIP,
                 mtype='Product Manager'):
        """Constructor for the ProductManager class"""

        # Initialize members of the class
        self.number_of_reportees = number_of_reportees 

        # Call the constructor for the base class
        super(ProductManager, self).__init__(ssn,
                                             name,
                                             salary,
                                             build,
                                             mtype)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        number_of_reportees = dictionary.get("number_of_reportees") if dictionary.get("number_of_reportees") else None
        ssn = dictionary.get("ssn") if dictionary.get("ssn") else None
        name = dictionary.get("name") if dictionary.get("name") else None
        salary = dictionary.get("salary") if dictionary.get("salary") else APIHelper.SKIP
        build = dictionary.get("build") if dictionary.get("build") else APIHelper.SKIP
        mtype = dictionary.get("type") if dictionary.get("type") else 'Product Manager'
        # Return an object of this model
        return cls(number_of_reportees,
                   ssn,
                   name,
                   salary,
                   build,
                   mtype)

    def __repr__(self):
        base_repr = super().__repr__()
        return (f'{self.__class__.__name__}('
                f'{base_repr[base_repr.find("(") + 1:-1]}, '
                f'number_of_reportees={self.number_of_reportees!r})')

    def __str__(self):
        base_str = super().__str__()
        return (f'{self.__class__.__name__}('
                f'{base_str[base_str.find("(") + 1:-1]}, '
                f'number_of_reportees={self.number_of_reportees!s})')

class SoftwareDeveloper(Employee):

    """Implementation of the 'SoftwareDeveloper' model.
    NOTE: This class inherits from 'Employee'.

    Attributes:
        boss_name (str): The model property of type str.
        languages (List[str]): The model property of type List[str].

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "ssn": 'ssn',
        "name": 'name',
        "boss_name": 'boss_name',
        "languages": 'languages',
        "salary": 'salary',
        "build": 'build',
        "mtype": 'type'
    }

    _optionals = [
        'boss_name',
        'languages',
    ]
    _optionals.extend(Employee._optionals)

    def __init__(self,
                 ssn=None,
                 name=None,
                 boss_name=APIHelper.SKIP,
                 languages=APIHelper.SKIP,
                 salary=APIHelper.SKIP,
                 build=APIHelper.SKIP,
                 mtype='Software Developer'):
        """Constructor for the SoftwareDeveloper class"""

        # Initialize members of the class
        if boss_name is not APIHelper.SKIP:
            self.boss_name = boss_name 
        if languages is not APIHelper.SKIP:
            self.languages = languages 

        # Call the constructor for the base class
        super(SoftwareDeveloper, self).__init__(ssn,
                                                name,
                                                salary,
                                                build,
                                                mtype)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        ssn = dictionary.get("ssn") if dictionary.get("ssn") else None
        name = dictionary.get("name") if dictionary.get("name") else None
        boss_name = dictionary.get("boss_name") if dictionary.get("boss_name") else APIHelper.SKIP
        languages = dictionary.get("languages") if dictionary.get("languages") else APIHelper.SKIP
        salary = dictionary.get("salary") if dictionary.get("salary") else APIHelper.SKIP
        build = dictionary.get("build") if dictionary.get("build") else APIHelper.SKIP
        mtype = dictionary.get("type") if dictionary.get("type") else 'Software Developer'
        # Return an object of this model
        return cls(ssn,
                   name,
                   boss_name,
                   languages,
                   salary,
                   build,
                   mtype)

    def __repr__(self):
        base_repr = super().__repr__()
        return (f'{self.__class__.__name__}('
                f'{base_repr[base_repr.find("(") + 1:-1]}, '
                f'boss_name={(self.boss_name if hasattr(self, "boss_name") else None)!r}, '
                f'languages={(self.languages if hasattr(self, "languages") else None)!r})')

    def __str__(self):
        base_str = super().__str__()
        return (f'{self.__class__.__name__}('
                f'{base_str[base_str.find("(") + 1:-1]}, '
                f'boss_name={(self.boss_name if hasattr(self, "boss_name") else None)!s}, '
                f'languages={(self.languages if hasattr(self, "languages") else None)!s})')
