"""typecombinatorglobal.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from typecombinatorglobal.api_helper import (
    APIHelper,
)
from typecombinatorglobal.configuration import (
    Server,
)
from typecombinatorglobal.controllers.base_controller import (
    BaseController,
)
from typecombinatorglobal.http.http_method_enum import (
    HttpMethodEnum,
)
from typecombinatorglobal.utilities.union_type_lookup import (
    UnionTypeLookUp,
)


class QueryController(BaseController):
    """A Controller to access Endpoints in the typecombinatorglobal API."""

    def __init__(self, config):
        """Initialize QueryController object."""
        super(QueryController, self).__init__(config)

    def send_primitive(self,
                       one_of_primitive,
                       any_of_primitive,
                       one_of_and_any_of_primitive):
        """Perform a POST request to /primitive.

        Send all Globally defined primitive TypeCombinators in query

        Args:
            one_of_primitive (str | int): The request query parameter.
            any_of_primitive (str | int): The request query parameter.
            one_of_and_any_of_primitive (str | int | bool): The request query
                parameter.

        Returns:
            str | int: Response from the API.

        Raises:
            APIException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/primitive")
            .http_method(HttpMethodEnum.POST)
            .query_param(Parameter()
                .key("oneOfPrimitive")
                .value(one_of_primitive)
                .validator(lambda value: UnionTypeLookUp
                    .get("OneOfPrimitive").validate(value)))
            .query_param(Parameter()
                .key("anyOfPrimitive")
                .value(any_of_primitive)
                .validator(lambda value: UnionTypeLookUp
                    .get("AnyOfPrimitive").validate(value)))
            .query_param(Parameter()
                .key("oneOfAndAnyOfPrimitive")
                .value(one_of_and_any_of_primitive)
                .validator(lambda value: UnionTypeLookUp
                    .get("OneOfAndAnyOfPrimitive").validate(value)))
            .header_param(Parameter()
                .key("accept")
                .value("application/json")),
        ).response(
            ResponseHandler()
            .deserializer(lambda value: APIHelper.deserialize_union_type(
                 UnionTypeLookUp
                 .get("AnyOfPrimitive"), value)),
        ).execute()
