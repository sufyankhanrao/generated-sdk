"""mindbodypublicapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from mindbodypublicapi.api_helper import APIHelper


class GetClientCompleteInfoRequest(object):
    """Implementation of the 'GetClientCompleteInfoRequest' model.

    request for GetClientCompleteInfoRequest

    Attributes:
        client_id (str): Filters results to client with these ID.
        start_date (datetime): Filters results to pricing options that are
            purchased on or after this date.   Default: **today’s date**.
        end_date (datetime): Filters results to pricing options that are
            purchased on or before this date.   Default: **today’s date**.
        cross_regional_lookup (bool): Used to retrieve a clients pricing
            options from multiple sites within an organization.When included
            and set to `true`,  it searches a maximum of ten sites with which
            this client is associated.When a client is associated with more
            than ten sites, use `ClientAssociatedSitesOffset` as many times as
            needed to search the additional sites with which the client is
            associated.  You can use the `CrossRegionalClientAssociations`
            value from `GET CrossRegionalClientAssociations` to determine how
            many sites the client is associated with.   Note that a `SiteID`
            is returned and populated in the `ClientServices` response when
            `CrossRegionalLookup` is set to `true`.   Default: **false**
        client_associated_sites_offset (int): Used to retrieve a client’s
            pricing options from multiple sites within an organization when
            the client is associated with more than ten sites. To change which
            ten sites are searched, change this offset value. A value of 0
            means that no sites are skipped and the first ten sites are
            returned. You can use the `CrossRegionalClientAssociations` value
            from `GET CrossRegionalClientAssociations` to determine how many
            sites the client is associated with. Note that you must always
            have `CrossRegionalLookup` set to `true` to use this parameter.<br
            />  Default: **0**    For example, if a client is associated with
            25 sites, you need to call `GetClientServices` three times, as
            follows:  * Use `GET CrossRegionalClientAssociations` to determine
            how many sites a client is associated with, which tells you how
            many additional calls you need to make.  * Either omit
            `ClientAssociatedSitesOffset` or set it to 0 to return the
            client’s services (pricing options) from sites 1-10.  * Set
            `ClientAssociatedSitesOffset` to 10 to return the client pricing
            options from sites 11-20  * Set `ClientAssociatedSitesOffset` to
            20 to return the client pricing options from sites 21-25
        required_client_data (List[str]): Used to retrieve list of purchased
            services, contract details, membership details and arrival
            programs for a specific client.   Default `ClientServices`,
            `ClientContracts`, `ClientMemberships` and `ClientArrivals` will
            be returned when `RequiredClientDatais` not set.   When
            `RequiredClientData` is set to `Contracts` then only
            `ClientContracts` will be returned in the response.   When
            `RequiredClientData` is set to Services then only `ClientServices`
            will be returned in the response.  When `RequiredClientData` is
            set to `Memberships` then only `ClientMemberships` will be
            returned in the response.   When `RequiredClientData` is set to
            `ArrivalPrograms` then only `ClientArrivals` will be returned in
            the response.
        exclude_inactive_sites (bool): Used to exclude inactive and deleted
            sites from the results

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "client_id": "ClientId",
        "start_date": "StartDate",
        "end_date": "EndDate",
        "cross_regional_lookup": "CrossRegionalLookup",
        "client_associated_sites_offset": "ClientAssociatedSitesOffset",
        "required_client_data": "RequiredClientData",
        "exclude_inactive_sites": "ExcludeInactiveSites",
    }

    _optionals = [
        "start_date",
        "end_date",
        "cross_regional_lookup",
        "client_associated_sites_offset",
        "required_client_data",
        "exclude_inactive_sites",
    ]

    def __init__(self,
                 client_id=None,
                 start_date=APIHelper.SKIP,
                 end_date=APIHelper.SKIP,
                 cross_regional_lookup=APIHelper.SKIP,
                 client_associated_sites_offset=APIHelper.SKIP,
                 required_client_data=APIHelper.SKIP,
                 exclude_inactive_sites=APIHelper.SKIP):
        """Initialize a GetClientCompleteInfoRequest instance."""
        # Initialize members of the class
        self.client_id = client_id
        if start_date is not APIHelper.SKIP:
            self.start_date =\
                 APIHelper.apply_datetime_converter(
                start_date, APIHelper.RFC3339DateTime) if start_date else None
        if end_date is not APIHelper.SKIP:
            self.end_date =\
                 APIHelper.apply_datetime_converter(
                end_date, APIHelper.RFC3339DateTime) if end_date else None
        if cross_regional_lookup is not APIHelper.SKIP:
            self.cross_regional_lookup = cross_regional_lookup
        if client_associated_sites_offset is not APIHelper.SKIP:
            self.client_associated_sites_offset = client_associated_sites_offset
        if required_client_data is not APIHelper.SKIP:
            self.required_client_data = required_client_data
        if exclude_inactive_sites is not APIHelper.SKIP:
            self.exclude_inactive_sites = exclude_inactive_sites

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        client_id =\
            dictionary.get("ClientId")\
            if dictionary.get("ClientId") else None
        start_date = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("StartDate")).datetime\
            if dictionary.get("StartDate") else APIHelper.SKIP
        end_date = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("EndDate")).datetime\
            if dictionary.get("EndDate") else APIHelper.SKIP
        cross_regional_lookup =\
            dictionary.get("CrossRegionalLookup")\
            if "CrossRegionalLookup" in dictionary.keys() else APIHelper.SKIP
        client_associated_sites_offset =\
            dictionary.get("ClientAssociatedSitesOffset")\
            if dictionary.get("ClientAssociatedSitesOffset") else APIHelper.SKIP
        required_client_data =\
            dictionary.get("RequiredClientData")\
            if dictionary.get("RequiredClientData") else APIHelper.SKIP
        exclude_inactive_sites =\
            dictionary.get("ExcludeInactiveSites")\
            if "ExcludeInactiveSites" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(client_id,
                   start_date,
                   end_date,
                   cross_regional_lookup,
                   client_associated_sites_offset,
                   required_client_data,
                   exclude_inactive_sites)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"client_id={self.client_id!r}, "
                f"start_date={(self.start_date
                     if hasattr(self, 'start_date') else None)!r}, "
                f"end_date={(self.end_date
                     if hasattr(self, 'end_date') else None)!r}, "
                f"cross_regional_lookup={(self.cross_regional_lookup
                     if hasattr(self, 'cross_regional_lookup') else None)!r}, "
                f"client_associated_sites_offset={(self.client_associated_sites_offset
                     if hasattr(self, 'client_associated_sites_offset') else None)!r}, "
                f"required_client_data={(self.required_client_data
                     if hasattr(self, 'required_client_data') else None)!r}, "
                f"exclude_inactive_sites={(self.exclude_inactive_sites
                     if hasattr(self, 'exclude_inactive_sites') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"client_id={self.client_id!s}, "
                f"start_date={(self.start_date
                     if hasattr(self, 'start_date') else None)!s}, "
                f"end_date={(self.end_date
                     if hasattr(self, 'end_date') else None)!s}, "
                f"cross_regional_lookup={(self.cross_regional_lookup
                     if hasattr(self, 'cross_regional_lookup') else None)!s}, "
                f"client_associated_sites_offset={(self.client_associated_sites_offset
                     if hasattr(self, 'client_associated_sites_offset') else None)!s}, "
                f"required_client_data={(self.required_client_data
                     if hasattr(self, 'required_client_data') else None)!s}, "
                f"exclude_inactive_sites={(self.exclude_inactive_sites
                     if hasattr(self, 'exclude_inactive_sites') else None)!s})")
