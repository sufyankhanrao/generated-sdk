"""mindbodypublicapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from mindbodypublicapi.api_helper import APIHelper
from mindbodypublicapi.models.pagination_response import (
    PaginationResponse,
)


class GetActiveSessionTimesResponse(object):
    """Implementation of the 'GetActiveSessionTimesResponse' model.

    Attributes:
        pagination_response (PaginationResponse): Contains information about
            the pagination to use.
        active_session_times (List[str]): List of available start times for
            active sessions. Note the following:  * The times returned
            represent possibilities for scheduling a session, not necessarily
            currently scheduled sessions.  * The response includes either all
            schedule types or those filtered by supplying `ScheduleType` or
            `SessionTypeIds`.  * Each session has an associated schedule type,
            but when you supply `SessionTypeIds`, they may map to one or more
            of the schedule types.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "pagination_response": "PaginationResponse",
        "active_session_times": "ActiveSessionTimes",
    }

    _optionals = [
        "pagination_response",
        "active_session_times",
    ]

    def __init__(self,
                 pagination_response=APIHelper.SKIP,
                 active_session_times=APIHelper.SKIP):
        """Initialize a GetActiveSessionTimesResponse instance."""
        # Initialize members of the class
        if pagination_response is not APIHelper.SKIP:
            self.pagination_response = pagination_response
        if active_session_times is not APIHelper.SKIP:
            self.active_session_times = active_session_times

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        pagination_response = PaginationResponse.from_dictionary(
            dictionary.get("PaginationResponse"))\
            if "PaginationResponse" in dictionary.keys() else APIHelper.SKIP
        active_session_times =\
            dictionary.get("ActiveSessionTimes")\
            if dictionary.get("ActiveSessionTimes") else APIHelper.SKIP
        # Return an object of this model
        return cls(pagination_response,
                   active_session_times)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"pagination_response={(self.pagination_response
                     if hasattr(self, 'pagination_response') else None)!r}, "
                f"active_session_times={(self.active_session_times
                     if hasattr(self, 'active_session_times') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"pagination_response={(self.pagination_response
                     if hasattr(self, 'pagination_response') else None)!s}, "
                f"active_session_times={(self.active_session_times
                     if hasattr(self, 'active_session_times') else None)!s})")
