"""mindbodypublicapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from mindbodypublicapi.api_helper import APIHelper


class GetSalesRepsRequest(object):
    """Implementation of the 'GetSalesRepsRequest' model.

    This is the request class for the get sales reps API

    Attributes:
        sales_rep_numbers (List[int]): This is the list of the sales rep
            numbers for which the salesrep data will be fetched.
        active_only (bool): When `true`, will return only active reps data.
            Default : **false**
        limit (int): Number of results to include, defaults to 100
        offset (int): Page offset, defaults to 0.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "sales_rep_numbers": "SalesRepNumbers",
        "active_only": "ActiveOnly",
        "limit": "Limit",
        "offset": "Offset",
    }

    _optionals = [
        "sales_rep_numbers",
        "active_only",
        "limit",
        "offset",
    ]

    def __init__(self,
                 sales_rep_numbers=APIHelper.SKIP,
                 active_only=APIHelper.SKIP,
                 limit=APIHelper.SKIP,
                 offset=APIHelper.SKIP):
        """Initialize a GetSalesRepsRequest instance."""
        # Initialize members of the class
        if sales_rep_numbers is not APIHelper.SKIP:
            self.sales_rep_numbers = sales_rep_numbers
        if active_only is not APIHelper.SKIP:
            self.active_only = active_only
        if limit is not APIHelper.SKIP:
            self.limit = limit
        if offset is not APIHelper.SKIP:
            self.offset = offset

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        sales_rep_numbers =\
            dictionary.get("SalesRepNumbers")\
            if dictionary.get("SalesRepNumbers") else APIHelper.SKIP
        active_only =\
            dictionary.get("ActiveOnly")\
            if "ActiveOnly" in dictionary.keys() else APIHelper.SKIP
        limit =\
            dictionary.get("Limit")\
            if dictionary.get("Limit") else APIHelper.SKIP
        offset =\
            dictionary.get("Offset")\
            if dictionary.get("Offset") else APIHelper.SKIP
        # Return an object of this model
        return cls(sales_rep_numbers,
                   active_only,
                   limit,
                   offset)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"sales_rep_numbers={(self.sales_rep_numbers
                     if hasattr(self, 'sales_rep_numbers') else None)!r}, "
                f"active_only={(self.active_only
                     if hasattr(self, 'active_only') else None)!r}, "
                f"limit={(self.limit if hasattr(self, 'limit') else None)!r}, "
                f"offset={(self.offset if hasattr(self, 'offset') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"sales_rep_numbers={(self.sales_rep_numbers
                     if hasattr(self, 'sales_rep_numbers') else None)!s}, "
                f"active_only={(self.active_only
                     if hasattr(self, 'active_only') else None)!s}, "
                f"limit={(self.limit if hasattr(self, 'limit') else None)!s}, "
                f"offset={(self.offset if hasattr(self, 'offset') else None)!s})")
