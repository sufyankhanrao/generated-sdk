# -*- coding: utf-8 -*-

"""
mindbodypublicapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class ScheduledServiceTypeEnum(object):

    """Implementation of the 'ScheduledServiceType' enum.

    The type of the scheduled service; i.e, a class, appointment, or
    enrollment.

    Attributes:
        CLASS: The enum member of type str.
        APPOINTMENT: The enum member of type str.
        ENROLLMENT: The enum member of type str.
        ALL: The enum member of type str.

    """
    CLASS = 'Class'

    APPOINTMENT = 'Appointment'

    ENROLLMENT = 'Enrollment'

    ALL = 'All'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
