"""mindbodypublicapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from mindbodypublicapi.api_helper import APIHelper


class GenderOption(object):
    """Implementation of the 'GenderOption' model.

    A gender option available at a site

    Attributes:
        id (int): The id of the gender option.
        name (str): The value that describes this gender option.
        is_active (bool): When `true`, this indicates that the gender option
            is active and may be assigned to a client.
        is_default (bool): When true, this indicates that this is the default
            gender option at the site.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "id": "Id",
        "name": "Name",
        "is_active": "IsActive",
        "is_default": "IsDefault",
    }

    _optionals = [
        "id",
        "name",
        "is_active",
        "is_default",
    ]

    def __init__(self,
                 id=APIHelper.SKIP,
                 name=APIHelper.SKIP,
                 is_active=APIHelper.SKIP,
                 is_default=APIHelper.SKIP):
        """Initialize a GenderOption instance."""
        # Initialize members of the class
        if id is not APIHelper.SKIP:
            self.id = id
        if name is not APIHelper.SKIP:
            self.name = name
        if is_active is not APIHelper.SKIP:
            self.is_active = is_active
        if is_default is not APIHelper.SKIP:
            self.is_default = is_default

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        id = dictionary.get("Id") if dictionary.get("Id") else APIHelper.SKIP
        name =\
            dictionary.get("Name")\
            if dictionary.get("Name") else APIHelper.SKIP
        is_active =\
            dictionary.get("IsActive")\
            if "IsActive" in dictionary.keys() else APIHelper.SKIP
        is_default =\
            dictionary.get("IsDefault")\
            if "IsDefault" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(id,
                   name,
                   is_active,
                   is_default)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"id={(self.id if hasattr(self, 'id') else None)!r}, "
                f"name={(self.name if hasattr(self, 'name') else None)!r}, "
                f"is_active={(self.is_active
                     if hasattr(self, 'is_active') else None)!r}, "
                f"is_default={(self.is_default
                     if hasattr(self, 'is_default') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"id={(self.id if hasattr(self, 'id') else None)!s}, "
                f"name={(self.name if hasattr(self, 'name') else None)!s}, "
                f"is_active={(self.is_active
                     if hasattr(self, 'is_active') else None)!s}, "
                f"is_default={(self.is_default
                     if hasattr(self, 'is_default') else None)!s})")
