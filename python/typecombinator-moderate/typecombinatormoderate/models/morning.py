"""typecombinatormoderate.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from typecombinatormoderate.api_helper import (
    APIHelper,
)


class Morning(object):
    """Implementation of the 'Morning' model.

    Course morning session

    Attributes:
        starts_at (str): Session start time
        ends_at (str): Session end time
        offer_tea_break (bool): Offer tea break during session
        session_type (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "starts_at": "startsAt",
        "ends_at": "endsAt",
        "offer_tea_break": "offerTeaBreak",
        "session_type": "sessionType",
    }

    _optionals = [
        "session_type",
    ]

    def __init__(self,
                 starts_at=None,
                 ends_at=None,
                 offer_tea_break=None,
                 session_type="Morning"):
        """Initialize a Morning instance."""
        # Initialize members of the class
        self.starts_at = starts_at
        self.ends_at = ends_at
        self.offer_tea_break = offer_tea_break
        self.session_type = session_type

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        starts_at =\
            dictionary.get("startsAt")\
            if dictionary.get("startsAt") else None
        ends_at = dictionary.get("endsAt") if dictionary.get("endsAt") else None
        offer_tea_break =\
            dictionary.get("offerTeaBreak")\
            if "offerTeaBreak" in dictionary.keys() else None
        session_type =\
            dictionary.get("sessionType")\
            if dictionary.get("sessionType") else "Morning"
        # Return an object of this model
        return cls(starts_at,
                   ends_at,
                   offer_tea_break,
                   session_type)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                value=dictionary.starts_at,
                type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(
                value=dictionary.ends_at,
                type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(
                value=dictionary.offer_tea_break,
                type_callable=lambda value: isinstance(value, bool))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
            value=dictionary.get("startsAt"),
            type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("endsAt"),
            type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("offerTeaBreak"),
            type_callable=lambda value: isinstance(value, bool))

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"starts_at={self.starts_at!r}, "
                f"ends_at={self.ends_at!r}, "
                f"offer_tea_break={self.offer_tea_break!r}, "
                f"session_type={(self.session_type
                     if hasattr(self, 'session_type') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"starts_at={self.starts_at!s}, "
                f"ends_at={self.ends_at!s}, "
                f"offer_tea_break={self.offer_tea_break!s}, "
                f"session_type={(self.session_type
                     if hasattr(self, 'session_type') else None)!s})")
