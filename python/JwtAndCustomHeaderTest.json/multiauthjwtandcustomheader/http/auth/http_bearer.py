# -*- coding: utf-8 -*-

"""
multiauthjwtandcustomheader

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""

import os
from apimatic_core.authentication.header_auth import HeaderAuth


class HttpBearer(HeaderAuth):

    @property
    def error_message(self):
        """Display error message on occurrence of authentication failure
        in HttpBearer

        """
        return "HttpBearer: AccessToken is undefined."

    def __init__(self, http_bearer_credentials):
        self._access_token = http_bearer_credentials.access_token \
            if http_bearer_credentials is not None else None
        auth_params = {}
        if self._access_token:
            auth_params = {"Authorization": "Bearer {}".format(self._access_token)}
        super().__init__(auth_params=auth_params)


class HttpBearerCredentials:

    @property
    def access_token(self):
        return self._access_token

    def __init__(self, access_token):
        if access_token is None:
            raise ValueError('access_token cannot be None')
        self._access_token = access_token

    def clone_with(self, access_token=None):
        return HttpBearerCredentials(access_token or self.access_token)

    @classmethod
    def from_environment(cls):
        access_token = os.getenv('HTTP_BEARER_ACCESS_TOKEN', None)

        if access_token is None:
            return None

        return cls(
            access_token=access_token
        )