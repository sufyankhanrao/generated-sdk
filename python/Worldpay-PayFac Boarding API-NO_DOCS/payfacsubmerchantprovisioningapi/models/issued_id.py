"""payfacsubmerchantprovisioningapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

import dateutil.parser

from payfacsubmerchantprovisioningapi.api_helper import (
    APIHelper,
)


class IssuedId(object):
    """Implementation of the 'IssuedId' model.

    Id Verification for the  Owners

    Attributes:
        id_number (str): ID Number of the Owner
        issued_city (str): City in which ID has been issued
        issued_state (IssuedStateEnum): State in which ID has been issued.
            Only valid US state codes (including Puerto Rico, Guam and Virgin
            Islands), Canadian province codes and Military state codes (AE -
            Armed Forces Africa, Canada, Europe, Middle East; AA - Armed
            Forces Americas (except Canada) ; AP - Armed Forces Pacific) are
            allowed
        issued_country (IssuedCountryEnum): Country Code in which ID has been
            issued
        date_issued (date): Format yyyy-mm-dd. Date on which ID has been issued
        date_expires (date): Format yyyy-mm-dd. Date on which ID expires
        id_type (IdTypeEnum): ID Type of the Owner

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "id_number": "idNumber",
        "id_type": "idType",
        "issued_city": "issuedCity",
        "issued_state": "issuedState",
        "issued_country": "issuedCountry",
        "date_issued": "dateIssued",
        "date_expires": "dateExpires",
    }

    _optionals = [
        "issued_city",
        "issued_state",
        "issued_country",
        "date_issued",
        "date_expires",
    ]

    def __init__(self,
                 id_number=None,
                 id_type=None,
                 issued_city=APIHelper.SKIP,
                 issued_state=APIHelper.SKIP,
                 issued_country=APIHelper.SKIP,
                 date_issued=APIHelper.SKIP,
                 date_expires=APIHelper.SKIP):
        """Initialize a IssuedId instance."""
        # Initialize members of the class
        self.id_number = id_number
        if issued_city is not APIHelper.SKIP:
            self.issued_city = issued_city
        if issued_state is not APIHelper.SKIP:
            self.issued_state = issued_state
        if issued_country is not APIHelper.SKIP:
            self.issued_country = issued_country
        if date_issued is not APIHelper.SKIP:
            self.date_issued = date_issued
        if date_expires is not APIHelper.SKIP:
            self.date_expires = date_expires
        self.id_type = id_type

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        id_number =\
            dictionary.get("idNumber")\
            if dictionary.get("idNumber") else None
        id_type = dictionary.get("idType") if dictionary.get("idType") else None
        issued_city =\
            dictionary.get("issuedCity")\
            if dictionary.get("issuedCity") else APIHelper.SKIP
        issued_state =\
            dictionary.get("issuedState")\
            if dictionary.get("issuedState") else APIHelper.SKIP
        issued_country =\
            dictionary.get("issuedCountry")\
            if dictionary.get("issuedCountry") else APIHelper.SKIP
        date_issued = dateutil.parser.parse(dictionary.get("dateIssued")).date()\
            if dictionary.get("dateIssued") else APIHelper.SKIP
        date_expires = dateutil.parser.parse(dictionary.get("dateExpires")).date()\
            if dictionary.get("dateExpires") else APIHelper.SKIP
        # Return an object of this model
        return cls(id_number,
                   id_type,
                   issued_city,
                   issued_state,
                   issued_country,
                   date_issued,
                   date_expires)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"id_number={self.id_number!r}, "
                f"issued_city={(self.issued_city
                     if hasattr(self, 'issued_city') else None)!r}, "
                f"issued_state={(self.issued_state
                     if hasattr(self, 'issued_state') else None)!r}, "
                f"issued_country={(self.issued_country
                     if hasattr(self, 'issued_country') else None)!r}, "
                f"date_issued={(self.date_issued
                     if hasattr(self, 'date_issued') else None)!r}, "
                f"date_expires={(self.date_expires
                     if hasattr(self, 'date_expires') else None)!r}, "
                f"id_type={self.id_type!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"id_number={self.id_number!s}, "
                f"issued_city={(self.issued_city
                     if hasattr(self, 'issued_city') else None)!s}, "
                f"issued_state={(self.issued_state
                     if hasattr(self, 'issued_state') else None)!s}, "
                f"issued_country={(self.issued_country
                     if hasattr(self, 'issued_country') else None)!s}, "
                f"date_issued={(self.date_issued
                     if hasattr(self, 'date_issued') else None)!s}, "
                f"date_expires={(self.date_expires
                     if hasattr(self, 'date_expires') else None)!s}, "
                f"id_type={self.id_type!s})")
