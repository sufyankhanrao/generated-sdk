# -*- coding: utf-8 -*-

"""
payfacsubmerchantprovisioningapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class AddTerminalsEnum(object):

    """Implementation of the 'AddTerminals' enum.

    The addTerminal field indicates whether or not all the terminals that
    exist on the template mid will be created for the submerchant. This is an
    optional field. If it is omitted, the value will default to “Yes”. If the
    value is “Yes”, all terminals on the template mid will be created for the
    submerchant. If “No”, no terminals will be created for the submerchant
    using this call. Terminals can be created using the Create Terminal
    endpoint.

    Attributes:
        YES: The enum member of type str.
        NO: The enum member of type str.

    """
    YES = 'Yes'

    NO = 'No'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
