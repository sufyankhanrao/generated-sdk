"""payfacsubmerchantprovisioningapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class Type1Enum(object):
    """Implementation of the 'Type1' enum.

    Required. Type of Sub-Merchant Contact.   1) Only CustomerService Contact
    is mandatory. It could be same as any Owner Contact information.  2) For
    Customer Service Contact, firstName, lastName, phoneNumber and email are
    mandatory during POST.   3) For all other contact types, firstName and
    lastName are mandatory during POST.

    Attributes:
        CUSTOMERSERVICE: The enum member of type str.
        PRIMARY: The enum member of type str.
        TERMINALDEPLOYMENT: The enum member of type str.

    """

    CUSTOMERSERVICE = "CustomerService"

    PRIMARY = "Primary"

    TERMINALDEPLOYMENT = "TerminalDeployment"

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
