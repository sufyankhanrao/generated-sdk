# -*- coding: utf-8 -*-

"""
payfacsubmerchantprovisioningapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class Type2Enum(object):

    """Implementation of the 'Type2' enum.

    Required. Address Type   1) Only Physical address is mandatory.  2) For
    any address type, streetAddress1, city, state, postalCode and countryCode
    are mandatory.

    Attributes:
        PHYSICAL: The enum member of type str.
        MAILING: The enum member of type str.
        TERMINALDEPLOYMENT: The enum member of type str.

    """
    PHYSICAL = 'Physical'

    MAILING = 'Mailing'

    TERMINALDEPLOYMENT = 'TerminalDeployment'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
