"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from verizon.api_helper import APIHelper
from verizon.models.triggers_list_options import (
    TriggersListOptions,
)


class AnomalyTriggerResult(object):
    """Implementation of the 'AnomalyTriggerResult' model.

    A result containing a list of anomaly triggers.

    Attributes:
        triggers (List[TriggersListOptions]): Trigger value chunk details.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "triggers": "triggers",
    }

    _optionals = [
        "triggers",
    ]

    def __init__(self,
                 triggers=APIHelper.SKIP):
        """Initialize a AnomalyTriggerResult instance."""
        # Initialize members of the class
        if triggers is not APIHelper.SKIP:
            self.triggers = triggers

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        triggers = None
        if dictionary.get("triggers") is not None:
            triggers = [
                TriggersListOptions.from_dictionary(x)
                    for x in dictionary.get("triggers")
            ]
        else:
            triggers = APIHelper.SKIP
        # Return an object of this model
        return cls(triggers)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"triggers={(self.triggers
                     if hasattr(self, 'triggers') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"triggers={(self.triggers
                     if hasattr(self, 'triggers') else None)!s})")
