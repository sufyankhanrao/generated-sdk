"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from verizon.api_helper import APIHelper
from verizon.models.license_device_id import (
    LicenseDeviceId,
)


class LicenseDeviceList(object):
    """Implementation of the 'LicenseDeviceList' model.

    List of all devices.

    Attributes:
        device_ids (List[LicenseDeviceId]): For 4G devices, IMEI (decimal, up
            to 15 digits).
        ip_address (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "device_ids": "deviceIds",
        "ip_address": "ipAddress",
    }

    _optionals = [
        "device_ids",
        "ip_address",
    ]

    def __init__(self,
                 device_ids=APIHelper.SKIP,
                 ip_address=APIHelper.SKIP):
        """Initialize a LicenseDeviceList instance."""
        # Initialize members of the class
        if device_ids is not APIHelper.SKIP:
            self.device_ids = device_ids
        if ip_address is not APIHelper.SKIP:
            self.ip_address = ip_address

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        device_ids = None
        if dictionary.get("deviceIds") is not None:
            device_ids = [
                LicenseDeviceId.from_dictionary(x)
                    for x in dictionary.get("deviceIds")
            ]
        else:
            device_ids = APIHelper.SKIP
        ip_address =\
            dictionary.get("ipAddress")\
            if dictionary.get("ipAddress") else APIHelper.SKIP
        # Return an object of this model
        return cls(device_ids,
                   ip_address)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"device_ids={(self.device_ids
                     if hasattr(self, 'device_ids') else None)!r}, "
                f"ip_address={(self.ip_address
                     if hasattr(self, 'ip_address') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"device_ids={(self.device_ids
                     if hasattr(self, 'device_ids') else None)!s}, "
                f"ip_address={(self.ip_address
                     if hasattr(self, 'ip_address') else None)!s})")
