"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from verizon.api_helper import APIHelper
from verizon.models.address_item import AddressItem


class Locations(object):
    """Implementation of the 'Locations' model.

    Location details.

    Attributes:
        address_list (List[AddressItem]): The model property of type
            List[AddressItem].

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "address_list": "addressList",
    }

    _optionals = [
        "address_list",
    ]

    def __init__(self,
                 address_list=APIHelper.SKIP):
        """Initialize a Locations instance."""
        # Initialize members of the class
        if address_list is not APIHelper.SKIP:
            self.address_list = address_list

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        address_list = None
        if dictionary.get("addressList") is not None:
            address_list = [
                AddressItem.from_dictionary(x)
                    for x in dictionary.get("addressList")
            ]
        else:
            address_list = APIHelper.SKIP
        # Return an object of this model
        return cls(address_list)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"address_list={(self.address_list
                     if hasattr(self, 'address_list') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"address_list={(self.address_list
                     if hasattr(self, 'address_list') else None)!s})")
