"""verizon.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from verizon.api_helper import APIHelper
from verizon.models.configuration import Configuration


class Fields(object):
    """Implementation of the 'Fields' model.

    List of fields affected by the event.

    Attributes:
        configuration (Configuration): List of the field names and values to
            set.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "configuration": "configuration",
    }

    _optionals = [
        "configuration",
    ]

    def __init__(self,
                 configuration=APIHelper.SKIP):
        """Initialize a Fields instance."""
        # Initialize members of the class
        if configuration is not APIHelper.SKIP:
            self.configuration = configuration

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        configuration = Configuration.from_dictionary(
            dictionary.get("configuration"))\
            if "configuration" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(configuration)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"configuration={(self.configuration
                     if hasattr(self, 'configuration') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"configuration={(self.configuration
                     if hasattr(self, 'configuration') else None)!s})")
