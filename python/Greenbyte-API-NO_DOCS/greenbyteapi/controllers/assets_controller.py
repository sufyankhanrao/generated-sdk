# -*- coding: utf-8 -*-

"""
greenbyteapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""

from greenbyteapi.api_helper import APIHelper
from greenbyteapi.configuration import Server
from greenbyteapi.http.api_response import ApiResponse
from greenbyteapi.controllers.base_controller import BaseController
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter
from greenbyteapi.http.http_method_enum import HttpMethodEnum
from apimatic_core.types.array_serialization_format import SerializationFormats
from apimatic_core.authentication.multiple.single_auth import Single
from greenbyteapi.models.device import Device
from greenbyteapi.models.devices_published_after_date_response import DevicesPublishedAfterDateResponse
from greenbyteapi.models.site_with_data import SiteWithData
from greenbyteapi.models.power_curve import PowerCurve
from greenbyteapi.exceptions.problem_details_exception import ProblemDetailsException
from greenbyteapi.exceptions.api_exception import APIException


class AssetsController(BaseController):

    """A Controller to access Endpoints in the greenbyteapi API."""
    def __init__(self, config):
        super(AssetsController, self).__init__(config)

    def get_devices(self,
                    device_type_ids=None,
                    site_ids=None,
                    parent_ids=None,
                    fields=None,
                    page_size=50,
                    page=1,
                    use_utc=False):
        """Does a GET request to /devices.

        Gets a list of devices that the API key has permissions for.
        _🔐 This endpoint requires the **Assets** endpoint permission._
        _This request can also be made using the POST method, 
        with a request to `devices.json` and 
        a JSON request body instead of query parameters._

        Args:
            device_type_ids (List[int], optional): Only include devices of
                these types. Examples: * 1 - Wind turbine * 2 - Production
                meter * 3 - Met mast * 4 - Inverter * 10 - Device group * 11 -
                Grid meter * 12 - Combiner box * 23 - String * 27 - Virtual
                Meteo Sensor
            site_ids (List[int], optional): Only include devices at these
                sites.
            parent_ids (List[int], optional): Only include devices with these
                parent devices.
            fields (List[str], optional): Which fields to include in the
                response. Valid fields are those defined in the `Device`
                schema (See Response Type). By default all fields are included.
            page_size (int, optional): The number of items to return per page.
            page (int, optional): Which page to return when the number of
                items exceed the page size.
            use_utc (bool, optional): Set to true to get timestamps in UTC.

        Returns:
            ApiResponse: An object with the response value as well as other
                useful information such as status codes and headers. A list of
                devices with associated metadata.

        Raises:
            APIException: When an error occurs while fetching the data from
                the remote API. This exception includes the HTTP Response
                code, an error message, and the HTTP body that was received in
                the request.

        """

        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path('/devices')
            .http_method(HttpMethodEnum.GET)
            .query_param(Parameter()
                         .key('deviceTypeIds')
                         .value(device_type_ids))
            .query_param(Parameter()
                         .key('siteIds')
                         .value(site_ids))
            .query_param(Parameter()
                         .key('parentIds')
                         .value(parent_ids))
            .query_param(Parameter()
                         .key('fields')
                         .value(fields))
            .query_param(Parameter()
                         .key('pageSize')
                         .value(page_size))
            .query_param(Parameter()
                         .key('page')
                         .value(page))
            .query_param(Parameter()
                         .key('useUtc')
                         .value(use_utc))
            .header_param(Parameter()
                          .key('accept')
                          .value('application/json'))
            .array_serialization_format(SerializationFormats.CSV)
            .auth(Single('ApiKeyHeaderAuth'))
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(Device.from_dictionary)
            .is_api_response(True)
            .local_error('400', 'The request cannot be fulfilled due to bad syntax.', ProblemDetailsException)
            .local_error('401', 'The request is missing a valid API key.\n', APIException)
            .local_error('403', 'One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.\n', APIException)
            .local_error('405', 'The HTTP method is not allowed for the endpoint.', APIException)
            .local_error('429', 'The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  "1m", "12h", or "1d").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.\n', ProblemDetailsException)
        ).execute()

    def get_devices_published_after_date(self,
                                         site_id,
                                         date):
        """Does a GET request to /devices-published-after-date.

        Gets the number of devices published on a site after a certain date as
        well as the IDs of the authorized devices.
        _🔐 This endpoint requires the **Assets** endpoint permission._

        Args:
            site_id (int): The request query parameter.
            date (datetime): Date after which published devices will be
                fetched.

        Returns:
            ApiResponse: An object with the response value as well as other
                useful information such as status codes and headers. A list of
                devices with associated metadata.

        Raises:
            APIException: When an error occurs while fetching the data from
                the remote API. This exception includes the HTTP Response
                code, an error message, and the HTTP body that was received in
                the request.

        """

        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path('/devices-published-after-date')
            .http_method(HttpMethodEnum.GET)
            .query_param(Parameter()
                         .key('siteId')
                         .value(site_id))
            .query_param(Parameter()
                         .key('date')
                         .value(APIHelper.when_defined(APIHelper.RFC3339DateTime, date)))
            .header_param(Parameter()
                          .key('accept')
                          .value('application/json'))
            .auth(Single('ApiKeyHeaderAuth'))
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(DevicesPublishedAfterDateResponse.from_dictionary)
            .is_api_response(True)
            .local_error('400', 'The request cannot be fulfilled due to bad syntax.', ProblemDetailsException)
            .local_error('401', 'The request is missing a valid API key.\n', APIException)
            .local_error('403', 'One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.\n', APIException)
            .local_error('405', 'The HTTP method is not allowed for the endpoint.', APIException)
            .local_error('429', 'The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  "1m", "12h", or "1d").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.\n', ProblemDetailsException)
        ).execute()

    def get_sites(self,
                  fields=None,
                  page_size=50,
                  page=1):
        """Does a GET request to /sites.

        Gets a list of sites that the API key has permissions
        for.
        _🔐 This endpoint requires the **Assets** endpoint permission._
        _This request can also be made using the POST method, 
        with a request to `sites.json` and 
        a JSON request body instead of query parameters._

        Args:
            fields (List[str], optional): Which fields to include in the
                response. Valid fields are those defined in the `SiteWithData`
                schema (See Response Type). By default all fields are included.
            page_size (int, optional): The number of items to return per page.
            page (int, optional): Which page to return when the number of
                items exceed the page size.

        Returns:
            ApiResponse: An object with the response value as well as other
                useful information such as status codes and headers. A list of
                sites with associated metadata.

        Raises:
            APIException: When an error occurs while fetching the data from
                the remote API. This exception includes the HTTP Response
                code, an error message, and the HTTP body that was received in
                the request.

        """

        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path('/sites')
            .http_method(HttpMethodEnum.GET)
            .query_param(Parameter()
                         .key('fields')
                         .value(fields))
            .query_param(Parameter()
                         .key('pageSize')
                         .value(page_size))
            .query_param(Parameter()
                         .key('page')
                         .value(page))
            .header_param(Parameter()
                          .key('accept')
                          .value('application/json'))
            .array_serialization_format(SerializationFormats.CSV)
            .auth(Single('ApiKeyHeaderAuth'))
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(SiteWithData.from_dictionary)
            .is_api_response(True)
            .local_error('400', 'The request cannot be fulfilled due to bad syntax.', ProblemDetailsException)
            .local_error('401', 'The request is missing a valid API key.\n', APIException)
            .local_error('403', 'One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.\n', APIException)
            .local_error('405', 'The HTTP method is not allowed for the endpoint.', APIException)
            .local_error('429', 'The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  "1m", "12h", or "1d").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.\n', ProblemDetailsException)
        ).execute()

    def get_power_curves(self,
                         device_ids,
                         timestamp=None,
                         learned=False):
        """Does a GET request to /powercurves.

        Gets the default or learned power curves for wind turbines.
        Other device types are not supported.
        _🔐 This endpoint requires the **PowerCurves** endpoint permission._
        _This request can also be made using the POST method, 
        with a request to `powercurves.json` and 
        a JSON request body instead of query parameters._

        Args:
            device_ids (List[int]): What devices to get power curves for. Only
                wind turbines are supported.
            timestamp (date, optional): The date for which to get power
                curves. The default is the current date.
            learned (bool, optional): Whether to get learned power curves
                instead of default power curves.

        Returns:
            ApiResponse: An object with the response value as well as other
                useful information such as status codes and headers. A list of
                objects containing device ids and associated power curves.

        Raises:
            APIException: When an error occurs while fetching the data from
                the remote API. This exception includes the HTTP Response
                code, an error message, and the HTTP body that was received in
                the request.

        """

        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path('/powercurves')
            .http_method(HttpMethodEnum.GET)
            .query_param(Parameter()
                         .key('deviceIds')
                         .value(device_ids))
            .query_param(Parameter()
                         .key('timestamp')
                         .value(timestamp))
            .query_param(Parameter()
                         .key('learned')
                         .value(learned))
            .header_param(Parameter()
                          .key('accept')
                          .value('application/json'))
            .array_serialization_format(SerializationFormats.CSV)
            .auth(Single('ApiKeyHeaderAuth'))
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(PowerCurve.from_dictionary)
            .is_api_response(True)
            .local_error('400', 'The request cannot be fulfilled due to bad syntax.', ProblemDetailsException)
            .local_error('401', 'The request is missing a valid API key.\n', APIException)
            .local_error('403', 'One of the following:\n* The API key does not authorize access to the requested endpoint because of a missing endpoint permission.\n* The API key does not authorize access to the requested data. Devices, sites or data signals can be limited.\n', APIException)
            .local_error('405', 'The HTTP method is not allowed for the endpoint.', APIException)
            .local_error('429', 'The API key has been used in too many requests in a given amount\nof time. The following headers will be set in the response:\n* `X-Rate-Limit-Limit` – The rate limit period (for example\n  "1m", "12h", or "1d").\n* `X-Rate-Limit-Remaining` – The remaining number of requests\n  for this period.\n* `X-Rate-Limit-Reset` – The UTC timestamp string (in ISO 8601\n  format) when the remaining number of requests resets.\n\nThe limit is currently 1,000 requests/minute per API key and IP\naddress.\n', ProblemDetailsException)
        ).execute()
