"""greenbyteapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from greenbyteapi.api_helper import APIHelper
from greenbyteapi.models.predict_recommendation import (
    PredictRecommendation,
)


class PredictStatusActive(object):
    """Implementation of the 'PredictStatusActive' model.

    Status info for an active Predict alert.

    Attributes:
        recommendations (List[PredictRecommendation]): Recommended actions for
            resolving the alert. Will be null if not calculated yet.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "recommendations": "recommendations",
    }

    _optionals = [
        "recommendations",
    ]

    def __init__(self,
                 recommendations=APIHelper.SKIP):
        """Initialize a PredictStatusActive instance."""
        # Initialize members of the class
        if recommendations is not APIHelper.SKIP:
            self.recommendations = recommendations

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        recommendations = None
        if dictionary.get("recommendations") is not None:
            recommendations = [
                PredictRecommendation.from_dictionary(x)
                    for x in dictionary.get("recommendations")
            ]
        else:
            recommendations = APIHelper.SKIP
        # Return an object of this model
        return cls(recommendations)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"recommendations={(self.recommendations
                     if hasattr(self, 'recommendations') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"recommendations={(self.recommendations
                     if hasattr(self, 'recommendations') else None)!s})")
