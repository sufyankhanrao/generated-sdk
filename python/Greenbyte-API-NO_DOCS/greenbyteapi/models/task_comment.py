"""greenbyteapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from greenbyteapi.api_helper import APIHelper
from greenbyteapi.models.user import User


class TaskComment(object):
    """Implementation of the 'TaskComment' model.

    A comment added to a task.

    Attributes:
        comment_id (int): The id of a task comment.
        task_id (int): The id of a task.
        text (str): The model property of type str.
        timestamp_created (datetime): The timestamp when the comment was
            created (added to the task). The timestamp is in the time zone
            configured in the Greenbyte Platform without UTC offset.
        created_by (User): The model property of type User.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "comment_id": "commentId",
        "task_id": "taskId",
        "text": "text",
        "timestamp_created": "timestampCreated",
        "created_by": "createdBy",
    }

    def __init__(self,
                 comment_id=None,
                 task_id=None,
                 text=None,
                 timestamp_created=None,
                 created_by=None):
        """Initialize a TaskComment instance."""
        # Initialize members of the class
        self.comment_id = comment_id
        self.task_id = task_id
        self.text = text
        self.timestamp_created =\
             APIHelper.apply_datetime_converter(
            timestamp_created, APIHelper.RFC3339DateTime) if timestamp_created else None
        self.created_by = created_by

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        comment_id =\
            dictionary.get("commentId")\
            if dictionary.get("commentId") else None
        task_id = dictionary.get("taskId") if dictionary.get("taskId") else None
        text = dictionary.get("text") if dictionary.get("text") else None
        timestamp_created = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("timestampCreated")).datetime\
            if dictionary.get("timestampCreated") else None
        created_by = User.from_dictionary(
            dictionary.get("createdBy"))\
            if dictionary.get("createdBy") else None
        # Return an object of this model
        return cls(comment_id,
                   task_id,
                   text,
                   timestamp_created,
                   created_by)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                value=dictionary.comment_id,
                type_callable=lambda value: isinstance(value, int)) \
                and APIHelper.is_valid_type(
                value=dictionary.task_id,
                type_callable=lambda value: isinstance(value, int)) \
                and APIHelper.is_valid_type(
                value=dictionary.text,
                type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(
                value=dictionary.timestamp_created,
                type_callable=lambda value: isinstance(value, APIHelper.RFC3339DateTime)) \
                and APIHelper.is_valid_type(
                value=dictionary.created_by,
                type_callable=lambda value: User.validate(value),
                is_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
            value=dictionary.get("commentId"),
            type_callable=lambda value: isinstance(value, int)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("taskId"),
            type_callable=lambda value: isinstance(value, int)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("text"),
            type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("timestampCreated"),
            type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("createdBy"),
            type_callable=lambda value: User.validate(value),
            is_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"comment_id={self.comment_id!r}, "
                f"task_id={self.task_id!r}, "
                f"text={self.text!r}, "
                f"timestamp_created={self.timestamp_created!r}, "
                f"created_by={self.created_by!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"comment_id={self.comment_id!s}, "
                f"task_id={self.task_id!s}, "
                f"text={self.text!s}, "
                f"timestamp_created={self.timestamp_created!s}, "
                f"created_by={self.created_by!s})")
