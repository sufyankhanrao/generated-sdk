# -*- coding: utf-8 -*-

"""
greenbyteapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from greenbyteapi.api_helper import APIHelper


class TaskCategory(object):

    """Implementation of the 'TaskCategory' model.

    Basic information about a task category.

    Attributes:
        category_id (int): The id of a task category.
        title (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "category_id": 'categoryId',
        "title": 'title'
    }

    def __init__(self,
                 category_id=None,
                 title=None):
        """Constructor for the TaskCategory class"""

        # Initialize members of the class
        self.category_id = category_id 
        self.title = title 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        category_id = dictionary.get("categoryId") if dictionary.get("categoryId") else None
        title = dictionary.get("title") if dictionary.get("title") else None
        # Return an object of this model
        return cls(category_id,
                   title)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.category_id,
                                           type_callable=lambda value: isinstance(value, int)) \
                and APIHelper.is_valid_type(value=dictionary.title,
                                            type_callable=lambda value: isinstance(value, str))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('categoryId'),
                                       type_callable=lambda value: isinstance(value, int)) \
            and APIHelper.is_valid_type(value=dictionary.get('title'),
                                        type_callable=lambda value: isinstance(value, str))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'category_id={self.category_id!r}, '
                f'title={self.title!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'category_id={self.category_id!s}, '
                f'title={self.title!s})')
