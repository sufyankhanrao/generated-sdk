# -*- coding: utf-8 -*-

"""
greenbyteapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class StatusCategoryEnum(object):

    """Implementation of the 'StatusCategory' enum.

    The category a status belongs to.

    Attributes:
        STOP: Statuses indicating that the device is prevented from producing
            power or is in a critical state.
        WARNING: Statuses indicating that the device is not operating
            normally, but is not in a critical state.
        INFORMATIONAL: Statuses that occur during normal operation.
        COMMUNICATION: Statuses related to communication between the device
            and Greenbyte.
        CURTAILMENT: Statuses generated based on curtailment.

    """
    STOP = 'stop'

    WARNING = 'warning'

    INFORMATIONAL = 'informational'

    COMMUNICATION = 'communication'

    CURTAILMENT = 'curtailment'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
