"""greenbyteapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

import dateutil.parser

from greenbyteapi.api_helper import APIHelper


class Recurrence(object):
    """Implementation of the 'Recurrence' model.

    Recurrence settings for the task. To calculate when the
    task is recurring, use the `timestampStart` field and
    then add to it multiples of the specified interval; the
    `intervalType` field determines if the task is recurring
    on daily, weekly, monthly, or yearly basis.
    If the task is not recurring, this field is null.
    **Note:** Only the main (first) task in a recurring
    series have recurrence settings. For the other tasks in
    the series, the field `mainTaskId` can be used to find
    it.

    Attributes:
        duration (int): The interval with which the task repeats.
        duration_type (DurationTypeEnum): The type of with which the task
            repeats.
        date_end (date): When the recurring task series ends (exclusive).  The
            end date is **not** included in the recurring task series: for
            example, to have a task series occur until and including the last
            day of March 2020, set `dateEnd` to "2020-04-01".

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "duration": "duration",
        "duration_type": "durationType",
        "date_end": "dateEnd",
    }

    _optionals = [
        "duration",
        "duration_type",
        "date_end",
    ]

    def __init__(self,
                 duration=APIHelper.SKIP,
                 duration_type=APIHelper.SKIP,
                 date_end=APIHelper.SKIP):
        """Initialize a Recurrence instance."""
        # Initialize members of the class
        if duration is not APIHelper.SKIP:
            self.duration = duration
        if duration_type is not APIHelper.SKIP:
            self.duration_type = duration_type
        if date_end is not APIHelper.SKIP:
            self.date_end = date_end

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        duration =\
            dictionary.get("duration")\
            if dictionary.get("duration") else APIHelper.SKIP
        duration_type =\
            dictionary.get("durationType")\
            if dictionary.get("durationType") else APIHelper.SKIP
        date_end = dateutil.parser.parse(dictionary.get("dateEnd")).date()\
            if dictionary.get("dateEnd") else APIHelper.SKIP
        # Return an object of this model
        return cls(duration,
                   duration_type,
                   date_end)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"duration={(self.duration
                     if hasattr(self, 'duration') else None)!r}, "
                f"duration_type={(self.duration_type
                     if hasattr(self, 'duration_type') else None)!r}, "
                f"date_end={(self.date_end
                     if hasattr(self, 'date_end') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"duration={(self.duration
                     if hasattr(self, 'duration') else None)!s}, "
                f"duration_type={(self.duration_type
                     if hasattr(self, 'duration_type') else None)!s}, "
                f"date_end={(self.date_end
                     if hasattr(self, 'date_end') else None)!s})")
