"""greenbyteapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from greenbyteapi.api_helper import APIHelper
from greenbyteapi.models.data_signal import (
    DataSignal,
)


class DataRealTimeItem(object):
    """Implementation of the 'DataRealTimeItem' model.

    An object containing a single data point for a specific aggregate, data
    signal and interval.

    Attributes:
        aggregate (AggregateModeEnum): How data is aggregated in the asset
            structure.
        aggregate_id (int): The id of this aggregate group: device id, site
            id, or the constant -1 for portfolio. For `siteLevel` aggregation
            a generated unique id is used.
        aggregate_path_names (List[str]): For `siteLevel` aggregation this
            contains the title for each level in the hierarchy. For other
            types of aggregation it will be empty.
        device_ids (List[int]): The ids of the devices in this aggregate group.
        calculation (CalculationModeRealTimeEnum): Which operation to use when
            aggregating data.
        data_signal (DataSignal): A data signal.
        data (Dict[str, float]): A single key value pair with timestamp as key
            and the data measurement as value. The timestamps are in UTC.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "aggregate": "aggregate",
        "aggregate_id": "aggregateId",
        "device_ids": "deviceIds",
        "calculation": "calculation",
        "data_signal": "dataSignal",
        "data": "data",
        "aggregate_path_names": "aggregatePathNames",
    }

    _optionals = [
        "aggregate_path_names",
    ]

    def __init__(self,
                 aggregate="device",
                 aggregate_id=None,
                 device_ids=None,
                 calculation=None,
                 data_signal=None,
                 data=None,
                 aggregate_path_names=APIHelper.SKIP):
        """Initialize a DataRealTimeItem instance."""
        # Initialize members of the class
        self.aggregate = aggregate
        self.aggregate_id = aggregate_id
        if aggregate_path_names is not APIHelper.SKIP:
            self.aggregate_path_names = aggregate_path_names
        self.device_ids = device_ids
        self.calculation = calculation
        self.data_signal = data_signal
        self.data = data

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        aggregate =\
            dictionary.get("aggregate")\
            if dictionary.get("aggregate") else "device"
        aggregate_id =\
            dictionary.get("aggregateId")\
            if dictionary.get("aggregateId") else None
        device_ids =\
            dictionary.get("deviceIds")\
            if dictionary.get("deviceIds") else None
        calculation =\
            dictionary.get("calculation")\
            if dictionary.get("calculation") else None
        data_signal = DataSignal.from_dictionary(
            dictionary.get("dataSignal"))\
            if dictionary.get("dataSignal") else None
        data = dictionary.get("data") if dictionary.get("data") else None
        aggregate_path_names =\
            dictionary.get("aggregatePathNames")\
            if dictionary.get("aggregatePathNames") else APIHelper.SKIP
        # Return an object of this model
        return cls(aggregate,
                   aggregate_id,
                   device_ids,
                   calculation,
                   data_signal,
                   data,
                   aggregate_path_names)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"aggregate={self.aggregate!r}, "
                f"aggregate_id={self.aggregate_id!r}, "
                f"aggregate_path_names={(self.aggregate_path_names
                     if hasattr(self, 'aggregate_path_names') else None)!r}, "
                f"device_ids={self.device_ids!r}, "
                f"calculation={self.calculation!r}, "
                f"data_signal={self.data_signal!r}, "
                f"data={self.data!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"aggregate={self.aggregate!s}, "
                f"aggregate_id={self.aggregate_id!s}, "
                f"aggregate_path_names={(self.aggregate_path_names
                     if hasattr(self, 'aggregate_path_names') else None)!s}, "
                f"device_ids={self.device_ids!s}, "
                f"calculation={self.calculation!s}, "
                f"data_signal={self.data_signal!s}, "
                f"data={self.data!s})")
