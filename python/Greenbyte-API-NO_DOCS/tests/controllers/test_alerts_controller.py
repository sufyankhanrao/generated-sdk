
"""
greenbyteapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""

# ruff: noqa: PLR0915, W291
from apimatic_core.utilities.comparison_helper import (
    ComparisonHelper,
)

from greenbyteapi.api_helper import APIHelper
from tests.controllers.controller_test_base import (
    ControllerTestBase,
)


class AlertsControllerTests(ControllerTestBase):
    """
    Endpoint tests for validating the API behavior.

    Ensures controller methods execute correctly and produce the expected
    responses using the shared test client and response catcher.
    """

    controller = None

    @classmethod
    def setUpClass(cls):
        """
        Initialize the shared test client and controller for all test methods.
        """
        super().setUpClass()
        cls.controller = cls.client.alerts
        cls.response_catcher = cls.controller.http_call_back

    def test_get_active_alerts(self):
        """
        Gets active alerts for multiple devices.The timestamps are in the time zone
        configured in the Greenbyte Platform.Use the useUtc flag to get timestamps in
        UTC._🔐 This endpoint requires the **Alerts** endpoint permission.__This
        request can also be made using the POST method, with a request to
        `activealerts.json` and a JSON request body instead of query parameters._.
        """
        # Parameters for the API call
        device_ids = APIHelper.json_deserialize(
            "[1,2,3]",
        )
        fields = APIHelper.json_deserialize(
            "[\"ruleId\",\"timestampStart\"]",
        )
        sort_by = None
        sort_asc = False
        page_size = 50
        page = 1
        use_utc = False

        # Perform the API call through the SDK function
        result = self.controller.get_active_alerts(
            device_ids,
            fields,
            sort_by,
            sort_asc,
            page_size,
            page,
            use_utc,
        )
        # Test response code
        assert self.response_catcher.response.status_code == 200
        # Test headers
        expected_headers = {
            "content-type": "application/json; charset=utf-8",
        }

        assert ComparisonHelper.match_headers(
            expected_headers,
            self.response_catcher.response.headers,
        )
        # Test whether the captured response is as we expected
        assert result is not None
        expected_body = APIHelper.json_deserialize(
            "[{\"deviceId\":179,\"ruleId\":104,\"description\":\"Power curve le"
            "ss than 90%\",\"details\":\"Evaluation period: 6 hours\\nData cove"
            "rage: 70% of devices\\nExclude data during: Active Stop or Warning"
            " status\\nData condition: The consecutive value of the performance"
            " index of Backen 2 is less than 90%\\nData condition: The consecut"
            "ive value of the wind speed of Backen 2 is greater than 5 m/s\",\""
            "timestampStart\":\"2020-03-18T06:50:00\",\"timestampEnd\":\"2020-0"
            "3-18T14:00:00\",\"message\":\"Low Performance Wind\",\"comment\":"
            "\"A comment\"},{\"deviceId\":183,\"ruleId\":104,\"description\":\""
            "Power curve less than 90%\",\"details\":\"Evaluation period: 6 hou"
            "rs\\nData coverage: 70% of devices\\nExclude data during: Active S"
            "top or Warning status\\nData condition: The consecutive value of t"
            "he performance index of Backen 6 is less than 90%\\nData condition"
            ": The consecutive value of the wind speed of Backen 6 is greater t"
            "han 5 m/s\",\"timestampStart\":\"2020-03-18T07:40:00\",\"timestamp"
            "End\":\"2020-03-18T14:00:00\",\"message\":\"Low Performance Wind\""
            ",\"comment\":\"A comment\"}]",
        )
        received_body = APIHelper.json_deserialize(
            self.response_catcher.response.text,
        )
        assert ComparisonHelper.match_body(
            expected_body,
            received_body,
        )

    def test_get_active_alerts_1(self):
        """
        Gets active alerts for multiple devices.The timestamps are in the time zone
        configured in the Greenbyte Platform.Use the useUtc flag to get timestamps in
        UTC._🔐 This endpoint requires the **Alerts** endpoint permission.__This
        request can also be made using the POST method, with a request to
        `activealerts.json` and a JSON request body instead of query parameters._.
        """
        # Parameters for the API call
        device_ids = APIHelper.json_deserialize(
            "[1,2,3]",
        )
        fields = APIHelper.json_deserialize(
            "[\"ruleId\",\"timestampStart\"]",
        )
        sort_by = None
        sort_asc = False
        page_size = 50
        page = 1
        use_utc = False

        # Perform the API call through the SDK function
        self.controller.get_active_alerts(
            device_ids,
            fields,
            sort_by,
            sort_asc,
            page_size,
            page,
            use_utc,
        )
        # Test response code
        assert self.response_catcher.response.status_code == 204

    def test_get_alerts(self):
        """
        Gets alerts for multiple devices and the given time period.The timestamps are
        in the time zone configured in the Greenbyte Platform.Use the useUtc flag to
        get timestamps in UTC._🔐 This endpoint requires the **Alerts** endpoint
        permission.__This request can also be made using the POST method, with a
        request to `alerts.json` and a JSON request body instead of query parameters._.
        """
        # Parameters for the API call
        device_ids = APIHelper.json_deserialize(
            "[1,2,3]",
        )
        timestamp_start = APIHelper.RFC3339DateTime.from_value(
            "2024-01-01T00:00:00Z",
        ).datetime
        timestamp_end = APIHelper.RFC3339DateTime.from_value(
            "2024-01-08T00:00:00Z",
        ).datetime
        fields = APIHelper.json_deserialize(
            "[\"ruleId\",\"timestampStart\"]",
        )
        sort_by = None
        sort_asc = False
        page_size = 50
        page = 1
        use_utc = False

        # Perform the API call through the SDK function
        result = self.controller.get_alerts(
            device_ids,
            timestamp_start,
            timestamp_end,
            fields,
            sort_by,
            sort_asc,
            page_size,
            page,
            use_utc,
        )
        # Test response code
        assert self.response_catcher.response.status_code == 200
        # Test headers
        expected_headers = {
            "content-type": "application/json; charset=utf-8",
        }

        assert ComparisonHelper.match_headers(
            expected_headers,
            self.response_catcher.response.headers,
        )
        # Test whether the captured response is as we expected
        assert result is not None
        expected_body = APIHelper.json_deserialize(
            "[{\"deviceId\":179,\"ruleId\":104,\"description\":\"Power curve le"
            "ss than 90%\",\"details\":\"Evaluation period: 6 hours\\nData cove"
            "rage: 70% of devices\\nExclude data during: Active Stop or Warning"
            " status\\nData condition: The consecutive value of the performance"
            " index of Backen 2 is less than 90%\\nData condition: The consecut"
            "ive value of the wind speed of Backen 2 is greater than 5 m/s\",\""
            "timestampStart\":\"2020-03-18T06:50:00\",\"timestampEnd\":\"2020-0"
            "3-18T14:00:00\",\"message\":\"Low Performance Wind\",\"comment\":"
            "\"A comment\"},{\"deviceId\":183,\"ruleId\":104,\"description\":\""
            "Power curve less than 90%\",\"details\":\"Evaluation period: 6 hou"
            "rs\\nData coverage: 70% of devices\\nExclude data during: Active S"
            "top or Warning status\\nData condition: The consecutive value of t"
            "he performance index of Backen 6 is less than 90%\\nData condition"
            ": The consecutive value of the wind speed of Backen 6 is greater t"
            "han 5 m/s\",\"timestampStart\":\"2020-03-18T07:40:00\",\"timestamp"
            "End\":\"2020-03-18T14:00:00\",\"message\":\"Low Performance Wind\""
            ",\"comment\":\"A comment\"}]",
        )
        received_body = APIHelper.json_deserialize(
            self.response_catcher.response.text,
        )
        assert ComparisonHelper.match_body(
            expected_body,
            received_body,
        )

    def test_get_alerts_1(self):
        """
        Gets alerts for multiple devices and the given time period.The timestamps are
        in the time zone configured in the Greenbyte Platform.Use the useUtc flag to
        get timestamps in UTC._🔐 This endpoint requires the **Alerts** endpoint
        permission.__This request can also be made using the POST method, with a
        request to `alerts.json` and a JSON request body instead of query parameters._.
        """
        # Parameters for the API call
        device_ids = APIHelper.json_deserialize(
            "[1,2,3]",
        )
        timestamp_start = APIHelper.RFC3339DateTime.from_value(
            "2024-01-01T00:00:00Z",
        ).datetime
        timestamp_end = APIHelper.RFC3339DateTime.from_value(
            "2024-01-08T00:00:00Z",
        ).datetime
        fields = APIHelper.json_deserialize(
            "[\"ruleId\",\"timestampStart\"]",
        )
        sort_by = None
        sort_asc = False
        page_size = 50
        page = 1
        use_utc = False

        # Perform the API call through the SDK function
        self.controller.get_alerts(
            device_ids,
            timestamp_start,
            timestamp_end,
            fields,
            sort_by,
            sort_asc,
            page_size,
            page,
            use_utc,
        )
        # Test response code
        assert self.response_catcher.response.status_code == 204

