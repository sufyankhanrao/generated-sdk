# -*- coding: utf-8 -*-

"""
foxitesigntest

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from foxitesigntest.api_helper import APIHelper
from foxitesigntest.models.party import Party


class BinaryFolder(object):

    """Implementation of the 'BinaryFolder' model.

    A folder meant to be used when sending PDF files in multipart

    Attributes:
        folder_name (str): Name of the document(s) folder
        file_urls (List[str]): The model property of type List[str].
        file_names (List[str]): The model property of type List[str].
        parties (List[Party]): The model property of type List[Party].
        fields (List[Any]): The model property of type List[Any].
        send_now (bool): Use this field to send the folder to the recipient
            parties. Each party will then receive a unique link in their email
            to sign the document. The invitation mail and subject in this case
            will be the same as the default invitation mail setup in your
            account.
        create_embedded_signing_session (str): The model property of type str.
        create_embedded_signing_session_for_all_parties (str): The model
            property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "folder_name": 'folderName',
        "file_urls": 'fileUrls',
        "file_names": 'fileNames',
        "parties": 'parties',
        "fields": 'fields',
        "create_embedded_signing_session": 'createEmbeddedSigningSession',
        "create_embedded_signing_session_for_all_parties": 'createEmbeddedSigningSessionForAllParties',
        "send_now": 'sendNow'
    }

    _optionals = [
        'send_now',
    ]

    def __init__(self,
                 folder_name=None,
                 file_urls=None,
                 file_names=None,
                 parties=None,
                 fields=None,
                 create_embedded_signing_session='true',
                 create_embedded_signing_session_for_all_parties='true',
                 send_now=True):
        """Constructor for the BinaryFolder class"""

        # Initialize members of the class
        self.folder_name = folder_name 
        self.file_urls = file_urls 
        self.file_names = file_names 
        self.parties = parties 
        self.fields = fields 
        self.send_now = send_now 
        self.create_embedded_signing_session = create_embedded_signing_session 
        self.create_embedded_signing_session_for_all_parties = create_embedded_signing_session_for_all_parties 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        folder_name = dictionary.get("folderName") if dictionary.get("folderName") else None
        file_urls = dictionary.get("fileUrls") if dictionary.get("fileUrls") else None
        file_names = dictionary.get("fileNames") if dictionary.get("fileNames") else None
        parties = None
        if dictionary.get('parties') is not None:
            parties = [Party.from_dictionary(x) for x in dictionary.get('parties')]
        fields = dictionary.get("fields") if dictionary.get("fields") else None
        create_embedded_signing_session = dictionary.get("createEmbeddedSigningSession") if dictionary.get("createEmbeddedSigningSession") else 'true'
        create_embedded_signing_session_for_all_parties = dictionary.get("createEmbeddedSigningSessionForAllParties") if dictionary.get("createEmbeddedSigningSessionForAllParties") else 'true'
        send_now = dictionary.get("sendNow") if dictionary.get("sendNow") else True
        # Return an object of this model
        return cls(folder_name,
                   file_urls,
                   file_names,
                   parties,
                   fields,
                   create_embedded_signing_session,
                   create_embedded_signing_session_for_all_parties,
                   send_now)

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'folder_name={self.folder_name!r}, '
                f'file_urls={self.file_urls!r}, '
                f'file_names={self.file_names!r}, '
                f'parties={self.parties!r}, '
                f'fields={self.fields!r}, '
                f'send_now={(self.send_now if hasattr(self, "send_now") else None)!r}, '
                f'create_embedded_signing_session={self.create_embedded_signing_session!r}, '
                f'create_embedded_signing_session_for_all_parties={self.create_embedded_signing_session_for_all_parties!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'folder_name={self.folder_name!s}, '
                f'file_urls={self.file_urls!s}, '
                f'file_names={self.file_names!s}, '
                f'parties={self.parties!s}, '
                f'fields={self.fields!s}, '
                f'send_now={(self.send_now if hasattr(self, "send_now") else None)!s}, '
                f'create_embedded_signing_session={self.create_embedded_signing_session!s}, '
                f'create_embedded_signing_session_for_all_parties={self.create_embedded_signing_session_for_all_parties!s})')
