"""foxitesigntest.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class PlanNamesEnum(object):
    """Implementation of the 'planNames' enum.

    Attributes:
        TRIAL: A trial account for testing purposes.
        BUSINESS PREMIUM: A business premium account account with some
            enhanced features.
        PROFESSIONAL: A professional account with enhanced features.
        ENTERPRISE: An Enteprise Account with access to more advanced
            automations for enterprise needs.

    """

    TRIAL = "Trial"

    BUSINESS_PREMIUM = "Business Premium"

    PROFESSIONAL = "Professional"

    ENTERPRISE = "Enterprise"

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
