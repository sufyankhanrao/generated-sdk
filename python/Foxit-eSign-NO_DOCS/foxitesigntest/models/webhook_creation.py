"""foxitesigntest.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from foxitesigntest.api_helper import APIHelper
from foxitesigntest.models.webhook_events import (
    WebhookEvents,
)


class WebhookCreation(object):
    """Implementation of the 'WebhookCreation' model.

    Attributes:
        channel_name (str): Name of the webhook channel
        webhook_url (str): URL of Webhook call. It should be publicly
            accessible.
        webhook_level (str): Value can be either Account or API App.<br/><br/>
            Access via Account (Web and API) or only API level
        webhook_secret (str): If a value is provided here, then each webhook
            request will be signed with a signature generated by taking an
            HMAC-SHA-256 base64 digest of the raw HTTP Body of the Webhook
            post, using this Webhook secret.
        events (WebhookEvents): The model property of type WebhookEvents.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "channel_name": "channelName",
        "webhook_url": "webhookUrl",
        "webhook_level": "webhookLevel",
        "events": "events",
        "webhook_secret": "webhookSecret",
    }

    _optionals = [
        "webhook_secret",
    ]

    def __init__(self,
                 channel_name=None,
                 webhook_url=None,
                 webhook_level="API App",
                 events=None,
                 webhook_secret=APIHelper.SKIP):
        """Initialize a WebhookCreation instance."""
        # Initialize members of the class
        self.channel_name = channel_name
        self.webhook_url = webhook_url
        self.webhook_level = webhook_level
        if webhook_secret is not APIHelper.SKIP:
            self.webhook_secret = webhook_secret
        self.events = events

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        channel_name =\
            dictionary.get("channelName")\
            if dictionary.get("channelName") else None
        webhook_url =\
            dictionary.get("webhookUrl")\
            if dictionary.get("webhookUrl") else None
        webhook_level =\
            dictionary.get("webhookLevel")\
            if dictionary.get("webhookLevel") else "API App"
        events = WebhookEvents.from_dictionary(
            dictionary.get("events"))\
            if dictionary.get("events") else None
        webhook_secret =\
            dictionary.get("webhookSecret")\
            if dictionary.get("webhookSecret") else APIHelper.SKIP
        # Return an object of this model
        return cls(channel_name,
                   webhook_url,
                   webhook_level,
                   events,
                   webhook_secret)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"channel_name={self.channel_name!r}, "
                f"webhook_url={self.webhook_url!r}, "
                f"webhook_level={self.webhook_level!r}, "
                f"webhook_secret={(self.webhook_secret
                     if hasattr(self, 'webhook_secret') else None)!r}, "
                f"events={self.events!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"channel_name={self.channel_name!s}, "
                f"webhook_url={self.webhook_url!s}, "
                f"webhook_level={self.webhook_level!s}, "
                f"webhook_secret={(self.webhook_secret
                     if hasattr(self, 'webhook_secret') else None)!s}, "
                f"events={self.events!s})")
