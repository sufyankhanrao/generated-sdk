"""foxitesigntest.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from foxitesigntest.api_helper import APIHelper


class SessionGeneration(object):
    """Implementation of the 'SessionGeneration' model.

    Attributes:
        folder_id (int): Folder id you want to use to regenerate the expired
            folder embedded signing token for this folder. You can determine
            the folder id from the folder URL.
        email_id (str): Recipient email id you want to use to regenerate the
            expired folder embedded signing token for this folder.
        party_id (str): Recipient party id you want to use to regenerate the
            expired folder embedded signing token for this folder.
        session_expire (str): Use this field to initiate the expiration of the
            embedded signing session.
        expiry (str): Use this field to enter the time duration in
            milliseconds of the expiry on the embedded signing session.
            *Note*: required if `sessionExpire` is `true`

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "folder_id": "folderId",
        "email_id": "emailId",
        "party_id": "partyId",
        "session_expire": "sessionExpire",
        "expiry": "expiry",
    }

    _optionals = [
        "party_id",
        "session_expire",
        "expiry",
    ]

    def __init__(self,
                 folder_id=None,
                 email_id=None,
                 party_id=APIHelper.SKIP,
                 session_expire="false",
                 expiry=APIHelper.SKIP):
        """Initialize a SessionGeneration instance."""
        # Initialize members of the class
        self.folder_id = folder_id
        self.email_id = email_id
        if party_id is not APIHelper.SKIP:
            self.party_id = party_id
        self.session_expire = session_expire
        if expiry is not APIHelper.SKIP:
            self.expiry = expiry

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        folder_id =\
            dictionary.get("folderId")\
            if dictionary.get("folderId") else None
        email_id =\
            dictionary.get("emailId")\
            if dictionary.get("emailId") else None
        party_id =\
            dictionary.get("partyId")\
            if dictionary.get("partyId") else APIHelper.SKIP
        session_expire =\
            dictionary.get("sessionExpire")\
            if dictionary.get("sessionExpire") else "false"
        expiry =\
            dictionary.get("expiry")\
            if dictionary.get("expiry") else APIHelper.SKIP
        # Return an object of this model
        return cls(folder_id,
                   email_id,
                   party_id,
                   session_expire,
                   expiry)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"folder_id={self.folder_id!r}, "
                f"email_id={self.email_id!r}, "
                f"party_id={(self.party_id
                     if hasattr(self, 'party_id') else None)!r}, "
                f"session_expire={(self.session_expire
                     if hasattr(self, 'session_expire') else None)!r}, "
                f"expiry={(self.expiry if hasattr(self, 'expiry') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"folder_id={self.folder_id!s}, "
                f"email_id={self.email_id!s}, "
                f"party_id={(self.party_id
                     if hasattr(self, 'party_id') else None)!s}, "
                f"session_expire={(self.session_expire
                     if hasattr(self, 'session_expire') else None)!s}, "
                f"expiry={(self.expiry if hasattr(self, 'expiry') else None)!s})")
