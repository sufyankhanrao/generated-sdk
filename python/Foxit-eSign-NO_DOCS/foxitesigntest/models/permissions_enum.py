"""foxitesigntest.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class PermissionsEnum(object):
    """Implementation of the 'permissions' enum.

    Attributes:
        FILL_FIELDS_AND_SIGN: The enum member of type str.
        FILL_FIELDS_ONLY: The enum member of type str.
        SIGN_ONLY: The enum member of type str.
        VIEW_ONLY: The enum member of type str.
        PARTY_ASSIGNER: The enum member of type str.

    """

    FILL_FIELDS_AND_SIGN = "FILL_FIELDS_AND_SIGN"

    FILL_FIELDS_ONLY = "FILL_FIELDS_ONLY"

    SIGN_ONLY = "SIGN_ONLY"

    VIEW_ONLY = "VIEW_ONLY"

    PARTY_ASSIGNER = "PARTY_ASSIGNER"

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
