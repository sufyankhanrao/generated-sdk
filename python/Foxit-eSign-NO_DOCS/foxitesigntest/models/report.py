"""foxitesigntest.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from foxitesigntest.api_helper import APIHelper


class Report(object):
    """Implementation of the 'Report' model.

    The optional filters that a report may leverage

    Attributes:
        status (EnvelopeStatusEnum): Download data by the folder status.
            Status parameter can have any of the following values:
            EXECUTED,SHARED,DRAFT, PARTIALLY SIGNED, CANCELLED, EXPIRED and
            DELETED.
        creation_date_from (str): Start of the Creation Date range. Accepted
            format: YYYY-MM-DD.
        creation_date_to (str): End of the Creation Date range. Accepted
            format: YYYY-MM-DD.
        folder_name (str): Any folder which contains this string.
        include_fields (str): Including folder fields in report.
        author_email (str): Author email in report.
        signer_email (str): Signer email in report.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "status": "status",
        "creation_date_from": "creationDateFrom",
        "creation_date_to": "creationDateTo",
        "folder_name": "folderName",
        "include_fields": "includeFields",
        "author_email": "authorEmail",
        "signer_email": "signerEmail",
    }

    _optionals = [
        "folder_name",
        "include_fields",
        "author_email",
        "signer_email",
    ]

    def __init__(self,
                 status=None,
                 creation_date_from=None,
                 creation_date_to=None,
                 folder_name=APIHelper.SKIP,
                 include_fields="false",
                 author_email=APIHelper.SKIP,
                 signer_email=APIHelper.SKIP):
        """Initialize a Report instance."""
        # Initialize members of the class
        self.status = status
        self.creation_date_from = creation_date_from
        self.creation_date_to = creation_date_to
        if folder_name is not APIHelper.SKIP:
            self.folder_name = folder_name
        self.include_fields = include_fields
        if author_email is not APIHelper.SKIP:
            self.author_email = author_email
        if signer_email is not APIHelper.SKIP:
            self.signer_email = signer_email

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        status = dictionary.get("status") if dictionary.get("status") else None
        creation_date_from =\
            dictionary.get("creationDateFrom")\
            if dictionary.get("creationDateFrom") else None
        creation_date_to =\
            dictionary.get("creationDateTo")\
            if dictionary.get("creationDateTo") else None
        folder_name =\
            dictionary.get("folderName")\
            if dictionary.get("folderName") else APIHelper.SKIP
        include_fields =\
            dictionary.get("includeFields")\
            if dictionary.get("includeFields") else "false"
        author_email =\
            dictionary.get("authorEmail")\
            if dictionary.get("authorEmail") else APIHelper.SKIP
        signer_email =\
            dictionary.get("signerEmail")\
            if dictionary.get("signerEmail") else APIHelper.SKIP
        # Return an object of this model
        return cls(status,
                   creation_date_from,
                   creation_date_to,
                   folder_name,
                   include_fields,
                   author_email,
                   signer_email)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"status={self.status!r}, "
                f"creation_date_from={self.creation_date_from!r}, "
                f"creation_date_to={self.creation_date_to!r}, "
                f"folder_name={(self.folder_name
                     if hasattr(self, 'folder_name') else None)!r}, "
                f"include_fields={(self.include_fields
                     if hasattr(self, 'include_fields') else None)!r}, "
                f"author_email={(self.author_email
                     if hasattr(self, 'author_email') else None)!r}, "
                f"signer_email={(self.signer_email
                     if hasattr(self, 'signer_email') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"status={self.status!s}, "
                f"creation_date_from={self.creation_date_from!s}, "
                f"creation_date_to={self.creation_date_to!s}, "
                f"folder_name={(self.folder_name
                     if hasattr(self, 'folder_name') else None)!s}, "
                f"include_fields={(self.include_fields
                     if hasattr(self, 'include_fields') else None)!s}, "
                f"author_email={(self.author_email
                     if hasattr(self, 'author_email') else None)!s}, "
                f"signer_email={(self.signer_email
                     if hasattr(self, 'signer_email') else None)!s})")
