# -*- coding: utf-8 -*-

"""
foxitesigntest

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class UserRolesEnum(object):

    """Implementation of the 'userRoles' enum.

    The level of permission that a user will leverage for user management
    purposes

    Attributes:
        USER: The enum member of type str.
        ADMIN: The enum member of type str.
        SUPER_ADMIN: The enum member of type str.

    """
    USER = 'user'

    ADMIN = 'admin'

    SUPER_ADMIN = 'super_admin'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
