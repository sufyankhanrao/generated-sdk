"""additionalpropertiestester.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from additionalpropertiestester.api_helper import (
    APIHelper,
)


class ParentDisabled(object):
    """Implementation of the 'ParentDisabled' model.

    Attributes:
        id (Any): Id of any type
        mtype (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "id": "id",
        "mtype": "type",
    }

    _optionals = [
        "mtype",
    ]

    def __init__(
        self,
        id=None,
        mtype="User"):
        """Initialize a ParentDisabled instance."""
        # Initialize members of the class
        self.id = id
        self.mtype = mtype

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        discriminators = {
            "Comp": ChildEnabledParentDisabled.from_dictionary,
        }
        unboxer = discriminators.get(dictionary.get("type"))

        # Delegate unboxing to another function if a discriminator
        # value for a child class is present.
        if unboxer:
            return unboxer(dictionary)

        # Extract variables from the dictionary
        id =\
            dictionary.get("id")\
            if dictionary.get("id")\
                else None
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else "User"

        # Return an object of this model
        return cls(id,
                   mtype)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _id=self.id
        _mtype=(
            self.mtype
            if hasattr(self, "mtype")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"id={_id!r}"
            f"mtype={_mtype!r}"
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _id=self.id
        _mtype=(
            self.mtype
            if hasattr(self, "mtype")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"id={_id!s}"
            f"mtype={_mtype!s}"
            f")"
        )

class ChildEnabledParentDisabled(ParentDisabled):
    """Implementation of the 'ChildEnabledParentDisabled' model.

    Inherits a User with disabled additional properties, but has number typed
    additionalProperties
    NOTE: This class inherits from 'ParentDisabled'.

    Attributes:
        company (str): The model property of type str.
        additional_properties (Dict[str, float]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "company": "company",
        "id": "id",
        "mtype": "type",
    }

    def __init__(
        self,
        company=None,
        id=None,
        mtype="Comp",
        additional_properties=None):
        """Initialize a ChildEnabledParentDisabled instance."""
        # Initialize members of the class
        self.company = company

        # Call the constructor for the base class
        super(ChildEnabledParentDisabled, self).__init__(
            id,
            mtype)

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        company =\
            dictionary.get("company")\
            if dictionary.get("company")\
                else None
        id =\
            dictionary.get("id")\
            if dictionary.get("id")\
                else None
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type")\
                else "Comp"

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: float(value))
        # Return an object of this model
        return cls(company,
                   id,
                   mtype,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _company=self.company
        _additional_properties=self.additional_properties
        _base_repr = super().__repr__()
        _base_repr = _base_repr[_base_repr.find("(") + 1:-1]
        return (
            f"{self.__class__.__name__}("
            f"base_repr={_base_repr!r}"
            f"company={_company!r}"
            f"additional_properties={_additional_properties!r}"
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _company=self.company
        _additional_properties=self.additional_properties
        _base_str = super().__str__()
        _base_str = _base_str[_base_str.find("(") + 1:-1]
        return (
            f"{self.__class__.__name__}("
            f"base_str={_base_str!s}"
            f"company={_company!s}"
            f"additional_properties={_additional_properties!s}"
            f")"
        )
