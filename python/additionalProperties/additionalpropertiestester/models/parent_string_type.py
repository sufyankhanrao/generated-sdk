"""additionalpropertiestester.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from additionalpropertiestester.api_helper import (
    APIHelper,
)


class ParentStringType(object):
    """Implementation of the 'ParentStringType' model.

    Attributes:
        name (str): The model property of type str.
        mtype (str): The model property of type str.
        additional_properties (Dict[str, str]): The additional properties for
            the model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "name": "name",
        "mtype": "type",
    }

    _optionals = [
        "mtype",
    ]

    def __init__(self,
                 name=None,
                 mtype="User",
                 additional_properties=None):
        """Initialize a ParentStringType instance."""
        # Initialize members of the class
        self.name = name
        self.mtype = mtype

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        discriminators = {
            "CompDiff": ChildNumberType.from_dictionary,
            "CompSame": ChildStringType.from_dictionary,
            "ChildDisabledParentEnabled": ChildDisabledParentEnabled.from_dictionary,
        }
        unboxer = discriminators.get(dictionary.get("type"))

        # Delegate unboxing to another function if a discriminator
        # value for a child class is present.
        if unboxer:
            return unboxer(dictionary)

        # Extract variables from the dictionary
        name = dictionary.get("name") if dictionary.get("name") else None
        mtype = dictionary.get("type") if dictionary.get("type") else "User"
        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items() if k not in cls._names.values()},
            unboxing_function=lambda value: str(value))
        # Return an object of this model
        return cls(name,
                   mtype,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"name={self.name!r}, "
                f"mtype={(self.mtype if hasattr(self, 'mtype') else None)!r}, "
                f"additional_properties={self.additional_properties!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"name={self.name!s}, "
                f"mtype={(self.mtype if hasattr(self, 'mtype') else None)!s}, "
                f"additional_properties={self.additional_properties!s})")

class ChildNumberType(ParentStringType):
    """Implementation of the 'ChildNumberType' model.

    Inherits a User with string type additional properties, but has number
    typed additionalProperties
    NOTE: This class inherits from 'ParentStringType'.

    Attributes:
        company (str): The model property of type str.
        additional_properties (Dict[str, str]): The additional properties for
            the model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "company": "company",
        "name": "name",
        "mtype": "type",
    }

    def __init__(self,
                 company=None,
                 name=None,
                 mtype="CompDiff",
                 additional_properties=None):
        """Initialize a ChildNumberType instance."""
        # Initialize members of the class
        self.company = company

        # Call the constructor for the base class
        super(ChildNumberType, self).__init__(name,
                                              mtype,
                                              additional_properties)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        company = dictionary.get("company") if dictionary.get("company") else None
        name = dictionary.get("name") if dictionary.get("name") else None
        mtype = dictionary.get("type") if dictionary.get("type") else "CompDiff"
        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items() if k not in cls._names.values()},
            unboxing_function=lambda value: str(value))
        # Return an object of this model
        return cls(company,
                   name,
                   mtype,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        base_repr = super().__repr__()
        return (f"{self.__class__.__name__}("
                f"{base_repr[base_repr.find('(') + 1:-1]}, "
                f"company={self.company!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        base_str = super().__str__()
        return (f"{self.__class__.__name__}("
                f"{base_str[base_str.find('(') + 1:-1]}, "
                f"company={self.company!s})")

class ChildStringType(ParentStringType):
    """Implementation of the 'ChildStringType' model.

    Inherits a User with string type additional properties, also has string
    typed additionalProperties
    NOTE: This class inherits from 'ParentStringType'.

    Attributes:
        company (str): The model property of type str.
        additional_properties (Dict[str, str]): The additional properties for
            the model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "company": "company",
        "name": "name",
        "mtype": "type",
    }

    def __init__(self,
                 company=None,
                 name=None,
                 mtype="CompSame",
                 additional_properties=None):
        """Initialize a ChildStringType instance."""
        # Initialize members of the class
        self.company = company

        # Call the constructor for the base class
        super(ChildStringType, self).__init__(name,
                                              mtype,
                                              additional_properties)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        company = dictionary.get("company") if dictionary.get("company") else None
        name = dictionary.get("name") if dictionary.get("name") else None
        mtype = dictionary.get("type") if dictionary.get("type") else "CompSame"
        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items() if k not in cls._names.values()},
            unboxing_function=lambda value: str(value))
        # Return an object of this model
        return cls(company,
                   name,
                   mtype,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        base_repr = super().__repr__()
        return (f"{self.__class__.__name__}("
                f"{base_repr[base_repr.find('(') + 1:-1]}, "
                f"company={self.company!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        base_str = super().__str__()
        return (f"{self.__class__.__name__}("
                f"{base_str[base_str.find('(') + 1:-1]}, "
                f"company={self.company!s})")

class ChildDisabledParentEnabled(ParentStringType):
    """Implementation of the 'ChildDisabledParentEnabled' model.

    Inherits a User with disabled additional properties, but has number typed
    additionalProperties
    NOTE: This class inherits from 'ParentStringType'.

    Attributes:
        company (str): The model property of type str.
        additional_properties (Dict[str, str]): The additional properties for
            the model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "company": "company",
        "name": "name",
        "mtype": "type",
    }

    def __init__(self,
                 company=None,
                 name=None,
                 mtype="ChildDisabledParentEnabled",
                 additional_properties=None):
        """Initialize a ChildDisabledParentEnabled instance."""
        # Initialize members of the class
        self.company = company

        # Call the constructor for the base class
        super(ChildDisabledParentEnabled, self).__init__(name,
                                                         mtype,
                                                         additional_properties)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        company = dictionary.get("company") if dictionary.get("company") else None
        name = dictionary.get("name") if dictionary.get("name") else None
        mtype =\
            dictionary.get("type")\
            if dictionary.get("type") else "ChildDisabledParentEnabled"
        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items() if k not in cls._names.values()},
            unboxing_function=lambda value: str(value))
        # Return an object of this model
        return cls(company,
                   name,
                   mtype,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        base_repr = super().__repr__()
        return (f"{self.__class__.__name__}("
                f"{base_repr[base_repr.find('(') + 1:-1]}, "
                f"company={self.company!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        base_str = super().__str__()
        return (f"{self.__class__.__name__}("
                f"{base_str[base_str.find('(') + 1:-1]}, "
                f"company={self.company!s})")
