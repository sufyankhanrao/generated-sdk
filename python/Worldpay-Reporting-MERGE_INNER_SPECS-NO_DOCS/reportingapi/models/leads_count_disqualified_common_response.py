"""reportingapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from reportingapi.api_helper import APIHelper
from reportingapi.models.leads_count_disqualified_summary_details import (
    LeadsCountDisqualifiedSummaryDetails,
)


class LeadsCountDisqualifiedCommonResponse(object):
    """Implementation of the 'LeadsCountDisqualifiedCommonResponse' model.

    Leads count for selected month range.

    Attributes:
        total_count (int): Total number of leads for the selected month range
        counts (List[LeadsCountDisqualifiedSummaryDetails]): Leads count
            grouped by disqualification reason.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "total_count": "totalCount",
        "counts": "counts",
    }

    _optionals = [
        "total_count",
        "counts",
    ]

    def __init__(self,
                 total_count=APIHelper.SKIP,
                 counts=APIHelper.SKIP):
        """Initialize a LeadsCountDisqualifiedCommonResponse instance."""
        # Initialize members of the class
        if total_count is not APIHelper.SKIP:
            self.total_count = total_count
        if counts is not APIHelper.SKIP:
            self.counts = counts

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        total_count =\
            dictionary.get("totalCount")\
            if dictionary.get("totalCount") else APIHelper.SKIP
        counts = None
        if dictionary.get("counts") is not None:
            counts = [
                LeadsCountDisqualifiedSummaryDetails.from_dictionary(x)
                    for x in dictionary.get("counts")
            ]
        else:
            counts = APIHelper.SKIP
        # Return an object of this model
        return cls(total_count,
                   counts)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"total_count={(self.total_count
                     if hasattr(self, 'total_count') else None)!r}, "
                f"counts={(self.counts if hasattr(self, 'counts') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"total_count={(self.total_count
                     if hasattr(self, 'total_count') else None)!s}, "
                f"counts={(self.counts if hasattr(self, 'counts') else None)!s})")

class LeadsCountDisqualifiedResponse(LeadsCountDisqualifiedCommonResponse):
    """Implementation of the 'LeadsCountDisqualifiedResponse' model.

    Leads count for selected month range.
    NOTE: This class inherits from 'LeadsCountDisqualifiedCommonResponse'.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "total_count": "totalCount",
        "counts": "counts",
    }

    def __init__(self,
                 total_count=APIHelper.SKIP,
                 counts=APIHelper.SKIP):
        """Initialize a LeadsCountDisqualifiedResponse instance."""
        # Initialize members of the class

        # Call the constructor for the base class
        super(LeadsCountDisqualifiedResponse, self).__init__(total_count,
                                                             counts)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        total_count =\
            dictionary.get("totalCount")\
            if dictionary.get("totalCount") else APIHelper.SKIP
        counts = None
        if dictionary.get("counts") is not None:
            counts = [
                LeadsCountDisqualifiedSummaryDetails.from_dictionary(x)
                    for x in dictionary.get("counts")
            ]
        else:
            counts = APIHelper.SKIP
        # Return an object of this model
        return cls(total_count,
                   counts)

    def __repr__(self):
        """Return a unambiguous string representation."""
        base_repr = super().__repr__()
        return (f"{self.__class__.__name__}("
                f"{base_repr[base_repr.find('(') + 1:-1]})")

    def __str__(self):
        """Return a human-readable string representation."""
        base_str = super().__str__()
        return (f"{self.__class__.__name__}("
                f"{base_str[base_str.find('(') + 1:-1]})")
