# -*- coding: utf-8 -*-

"""
reportingapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from reportingapi.api_helper import APIHelper
from reportingapi.models.leads_count_category_summary_details import LeadsCountCategorySummaryDetails


class LeadsCountCategoryCommonResponse(object):

    """Implementation of the 'LeadsCountCategoryCommonResponse' model.

    Leads count for the selected month range.

    Attributes:
        total_count (int): Total number of leads generated for the independent
            sales channel(s) for the selected range of month.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "total_count": 'totalCount'
    }

    _optionals = [
        'total_count',
    ]

    def __init__(self,
                 total_count=APIHelper.SKIP):
        """Constructor for the LeadsCountCategoryCommonResponse class"""

        # Initialize members of the class
        if total_count is not APIHelper.SKIP:
            self.total_count = total_count 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        total_count = dictionary.get("totalCount") if dictionary.get("totalCount") else APIHelper.SKIP
        # Return an object of this model
        return cls(total_count)

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'total_count={(self.total_count if hasattr(self, "total_count") else None)!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'total_count={(self.total_count if hasattr(self, "total_count") else None)!s})')

class LeadsCountCategoryResponse(LeadsCountCategoryCommonResponse):

    """Implementation of the 'LeadsCountCategoryResponse' model.

    Leads count for the selected month range.
    NOTE: This class inherits from 'LeadsCountCategoryCommonResponse'.

    Attributes:
        counts (List[LeadsCountCategorySummaryDetails]): Leads count grouped
            by category.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "counts": 'counts',
        "total_count": 'totalCount'
    }

    _optionals = [
        'counts',
    ]
    _optionals.extend(LeadsCountCategoryCommonResponse._optionals)

    def __init__(self,
                 counts=APIHelper.SKIP,
                 total_count=APIHelper.SKIP):
        """Constructor for the LeadsCountCategoryResponse class"""

        # Initialize members of the class
        if counts is not APIHelper.SKIP:
            self.counts = counts 

        # Call the constructor for the base class
        super(LeadsCountCategoryResponse, self).__init__(total_count)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        counts = None
        if dictionary.get('counts') is not None:
            counts = [LeadsCountCategorySummaryDetails.from_dictionary(x) for x in dictionary.get('counts')]
        else:
            counts = APIHelper.SKIP
        total_count = dictionary.get("totalCount") if dictionary.get("totalCount") else APIHelper.SKIP
        # Return an object of this model
        return cls(counts,
                   total_count)

    def __repr__(self):
        base_repr = super().__repr__()
        return (f'{self.__class__.__name__}('
                f'{base_repr[base_repr.find("(") + 1:-1]}, '
                f'counts={(self.counts if hasattr(self, "counts") else None)!r})')

    def __str__(self):
        base_str = super().__str__()
        return (f'{self.__class__.__name__}('
                f'{base_str[base_str.find("(") + 1:-1]}, '
                f'counts={(self.counts if hasattr(self, "counts") else None)!s})')
