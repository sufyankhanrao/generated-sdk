"""reportingapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from reportingapi.api_helper import APIHelper
from reportingapi.models.date_range_search import (
    DateRangeSearch,
)
from reportingapi.models.entity_1 import Entity1


class AuthTransactionsRequest(object):
    """Implementation of the 'AuthTransactionsRequest' model.

    Attributes:
        hierarchy (Entity1): Refers to merchant hierarchy levels
        card_type (str): Default - Credit<br>Debit not supported in this
            release.
        card_networks (List[CardNetworkTypeEnum]): Indicates the network of
            the cards which is associated with facilitating the
            payment.<br>Default - All Card Networks.s.
        date_range (DateRangeSearch): A range of dates that includes a
            particular start and end date and all dates in between the range.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "hierarchy": "hierarchy",
        "date_range": "dateRange",
        "card_type": "cardType",
        "card_networks": "cardNetworks",
    }

    _optionals = [
        "card_type",
        "card_networks",
    ]

    def __init__(self,
                 hierarchy=None,
                 date_range=None,
                 card_type=APIHelper.SKIP,
                 card_networks=APIHelper.SKIP):
        """Initialize a AuthTransactionsRequest instance."""
        # Initialize members of the class
        self.hierarchy = hierarchy
        if card_type is not APIHelper.SKIP:
            self.card_type = card_type
        if card_networks is not APIHelper.SKIP:
            self.card_networks = card_networks
        self.date_range = date_range

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        hierarchy = Entity1.from_dictionary(
            dictionary.get("hierarchy"))\
            if dictionary.get("hierarchy") else None
        date_range = DateRangeSearch.from_dictionary(
            dictionary.get("dateRange"))\
            if dictionary.get("dateRange") else None
        card_type =\
            dictionary.get("cardType")\
            if dictionary.get("cardType") else APIHelper.SKIP
        card_networks =\
            dictionary.get("cardNetworks")\
            if dictionary.get("cardNetworks") else APIHelper.SKIP
        # Return an object of this model
        return cls(hierarchy,
                   date_range,
                   card_type,
                   card_networks)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"hierarchy={self.hierarchy!r}, "
                f"card_type={(self.card_type
                     if hasattr(self, 'card_type') else None)!r}, "
                f"card_networks={(self.card_networks
                     if hasattr(self, 'card_networks') else None)!r}, "
                f"date_range={self.date_range!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"hierarchy={self.hierarchy!s}, "
                f"card_type={(self.card_type
                     if hasattr(self, 'card_type') else None)!s}, "
                f"card_networks={(self.card_networks
                     if hasattr(self, 'card_networks') else None)!s}, "
                f"date_range={self.date_range!s})")
