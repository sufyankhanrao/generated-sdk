"""reportingapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from reportingapi.api_helper import APIHelper
from reportingapi.models.summary_21 import Summary21


class NetSettledAmountCardTypeResponse(object):
    """Implementation of the 'NetSettledAmountCardTypeResponse' model.

    Results for net settlement amount by card type.

    Attributes:
        summaries (List[Summary21]): Summary details
        total_settled_gross_amount (float): Sum of Settled Gross Amount.
        total_settled_gross_count (int): Total number of transactions with
            gross settled amount.
        total_settled_net_amount (float): Sum of Settled Net Amount.
        total_settled_net_count (int): Total number of transactions with net
            settled amount.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "summaries": "summaries",
        "total_settled_gross_amount": "totalSettledGrossAmount",
        "total_settled_gross_count": "totalSettledGrossCount",
        "total_settled_net_amount": "totalSettledNetAmount",
        "total_settled_net_count": "totalSettledNetCount",
    }

    _optionals = [
        "summaries",
        "total_settled_gross_amount",
        "total_settled_gross_count",
        "total_settled_net_amount",
        "total_settled_net_count",
    ]

    def __init__(self,
                 summaries=APIHelper.SKIP,
                 total_settled_gross_amount=APIHelper.SKIP,
                 total_settled_gross_count=APIHelper.SKIP,
                 total_settled_net_amount=APIHelper.SKIP,
                 total_settled_net_count=APIHelper.SKIP):
        """Initialize a NetSettledAmountCardTypeResponse instance."""
        # Initialize members of the class
        if summaries is not APIHelper.SKIP:
            self.summaries = summaries
        if total_settled_gross_amount is not APIHelper.SKIP:
            self.total_settled_gross_amount = total_settled_gross_amount
        if total_settled_gross_count is not APIHelper.SKIP:
            self.total_settled_gross_count = total_settled_gross_count
        if total_settled_net_amount is not APIHelper.SKIP:
            self.total_settled_net_amount = total_settled_net_amount
        if total_settled_net_count is not APIHelper.SKIP:
            self.total_settled_net_count = total_settled_net_count

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        summaries = None
        if dictionary.get("summaries") is not None:
            summaries = [
                Summary21.from_dictionary(x)
                    for x in dictionary.get("summaries")
            ]
        else:
            summaries = APIHelper.SKIP
        total_settled_gross_amount =\
            dictionary.get("totalSettledGrossAmount")\
            if dictionary.get("totalSettledGrossAmount") else APIHelper.SKIP
        total_settled_gross_count =\
            dictionary.get("totalSettledGrossCount")\
            if dictionary.get("totalSettledGrossCount") else APIHelper.SKIP
        total_settled_net_amount =\
            dictionary.get("totalSettledNetAmount")\
            if dictionary.get("totalSettledNetAmount") else APIHelper.SKIP
        total_settled_net_count =\
            dictionary.get("totalSettledNetCount")\
            if dictionary.get("totalSettledNetCount") else APIHelper.SKIP
        # Return an object of this model
        return cls(summaries,
                   total_settled_gross_amount,
                   total_settled_gross_count,
                   total_settled_net_amount,
                   total_settled_net_count)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"summaries={(self.summaries
                     if hasattr(self, 'summaries') else None)!r}, "
                f"total_settled_gross_amount={(self.total_settled_gross_amount
                     if hasattr(self, 'total_settled_gross_amount') else None)!r}, "
                f"total_settled_gross_count={(self.total_settled_gross_count
                     if hasattr(self, 'total_settled_gross_count') else None)!r}, "
                f"total_settled_net_amount={(self.total_settled_net_amount
                     if hasattr(self, 'total_settled_net_amount') else None)!r}, "
                f"total_settled_net_count={(self.total_settled_net_count
                     if hasattr(self, 'total_settled_net_count') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"summaries={(self.summaries
                     if hasattr(self, 'summaries') else None)!s}, "
                f"total_settled_gross_amount={(self.total_settled_gross_amount
                     if hasattr(self, 'total_settled_gross_amount') else None)!s}, "
                f"total_settled_gross_count={(self.total_settled_gross_count
                     if hasattr(self, 'total_settled_gross_count') else None)!s}, "
                f"total_settled_net_amount={(self.total_settled_net_amount
                     if hasattr(self, 'total_settled_net_amount') else None)!s}, "
                f"total_settled_net_count={(self.total_settled_net_count
                     if hasattr(self, 'total_settled_net_count') else None)!s})")
