# -*- coding: utf-8 -*-

"""
reportingapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from reportingapi.api_helper import APIHelper


class PartnerCommonSummaryResponse(object):

    """Implementation of the 'PartnerCommonSummaryResponse' model.

    Common summary details for partner summary.

    Attributes:
        total_gross_revenue (float): Refers to sum of gross revenue. Gross
            Revenue is the total amount of sales recognized for a reporting
            period, prior to any deductions.
        total_process_revenue (float): Refers to Sum of Process Revenue.
            Process Revenue is the revenue earned from processing the
            transactions.
        total_settled_amount (float): Sum of settled amount per the  selection
            criteria.
        total_settled_count (int): Total number of settled transactions as per
            the  selection criteria

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "total_gross_revenue": 'totalGrossRevenue',
        "total_process_revenue": 'totalProcessRevenue',
        "total_settled_amount": 'totalSettledAmount',
        "total_settled_count": 'totalSettledCount'
    }

    _optionals = [
        'total_gross_revenue',
        'total_process_revenue',
        'total_settled_amount',
        'total_settled_count',
    ]

    def __init__(self,
                 total_gross_revenue=APIHelper.SKIP,
                 total_process_revenue=APIHelper.SKIP,
                 total_settled_amount=APIHelper.SKIP,
                 total_settled_count=APIHelper.SKIP):
        """Constructor for the PartnerCommonSummaryResponse class"""

        # Initialize members of the class
        if total_gross_revenue is not APIHelper.SKIP:
            self.total_gross_revenue = total_gross_revenue 
        if total_process_revenue is not APIHelper.SKIP:
            self.total_process_revenue = total_process_revenue 
        if total_settled_amount is not APIHelper.SKIP:
            self.total_settled_amount = total_settled_amount 
        if total_settled_count is not APIHelper.SKIP:
            self.total_settled_count = total_settled_count 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        total_gross_revenue = dictionary.get("totalGrossRevenue") if dictionary.get("totalGrossRevenue") else APIHelper.SKIP
        total_process_revenue = dictionary.get("totalProcessRevenue") if dictionary.get("totalProcessRevenue") else APIHelper.SKIP
        total_settled_amount = dictionary.get("totalSettledAmount") if dictionary.get("totalSettledAmount") else APIHelper.SKIP
        total_settled_count = dictionary.get("totalSettledCount") if dictionary.get("totalSettledCount") else APIHelper.SKIP
        # Return an object of this model
        return cls(total_gross_revenue,
                   total_process_revenue,
                   total_settled_amount,
                   total_settled_count)

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'total_gross_revenue={(self.total_gross_revenue if hasattr(self, "total_gross_revenue") else None)!r}, '
                f'total_process_revenue={(self.total_process_revenue if hasattr(self, "total_process_revenue") else None)!r}, '
                f'total_settled_amount={(self.total_settled_amount if hasattr(self, "total_settled_amount") else None)!r}, '
                f'total_settled_count={(self.total_settled_count if hasattr(self, "total_settled_count") else None)!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'total_gross_revenue={(self.total_gross_revenue if hasattr(self, "total_gross_revenue") else None)!s}, '
                f'total_process_revenue={(self.total_process_revenue if hasattr(self, "total_process_revenue") else None)!s}, '
                f'total_settled_amount={(self.total_settled_amount if hasattr(self, "total_settled_amount") else None)!s}, '
                f'total_settled_count={(self.total_settled_count if hasattr(self, "total_settled_count") else None)!s})')
