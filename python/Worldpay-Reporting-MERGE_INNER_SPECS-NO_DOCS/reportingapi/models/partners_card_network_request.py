# -*- coding: utf-8 -*-

"""
reportingapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from reportingapi.api_helper import APIHelper
from reportingapi.models.month_range import MonthRange
from reportingapi.models.sort_partner_card_network import SortPartnerCardNetwork


class PartnersCardNetworkRequest(object):

    """Implementation of the 'PartnersCardNetworkRequest' model.

    Attributes:
        independent_sales_channel_codes (List[str]): Independent sales channel
            value provides a secondary entity under the independent sales
            organization hierarchy level.
        month_range (MonthRange): Month Range for which the transaction is
            processed.
        card_networks (List[CardNetwork1Enum]): Brand or Network to which the
            card is associated.
        sort_results_by (SortPartnerCardNetwork): Used to sort the results.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "independent_sales_channel_codes": 'independentSalesChannelCodes',
        "month_range": 'monthRange',
        "card_networks": 'cardNetworks',
        "sort_results_by": 'sortResultsBy'
    }

    _optionals = [
        'card_networks',
        'sort_results_by',
    ]

    def __init__(self,
                 independent_sales_channel_codes=None,
                 month_range=None,
                 card_networks=APIHelper.SKIP,
                 sort_results_by=APIHelper.SKIP):
        """Constructor for the PartnersCardNetworkRequest class"""

        # Initialize members of the class
        self.independent_sales_channel_codes = independent_sales_channel_codes 
        self.month_range = month_range 
        if card_networks is not APIHelper.SKIP:
            self.card_networks = card_networks 
        if sort_results_by is not APIHelper.SKIP:
            self.sort_results_by = sort_results_by 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        independent_sales_channel_codes = dictionary.get("independentSalesChannelCodes") if dictionary.get("independentSalesChannelCodes") else None
        month_range = MonthRange.from_dictionary(dictionary.get('monthRange')) if dictionary.get('monthRange') else None
        card_networks = dictionary.get("cardNetworks") if dictionary.get("cardNetworks") else APIHelper.SKIP
        sort_results_by = SortPartnerCardNetwork.from_dictionary(dictionary.get('sortResultsBy')) if 'sortResultsBy' in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(independent_sales_channel_codes,
                   month_range,
                   card_networks,
                   sort_results_by)

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'independent_sales_channel_codes={self.independent_sales_channel_codes!r}, '
                f'month_range={self.month_range!r}, '
                f'card_networks={(self.card_networks if hasattr(self, "card_networks") else None)!r}, '
                f'sort_results_by={(self.sort_results_by if hasattr(self, "sort_results_by") else None)!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'independent_sales_channel_codes={self.independent_sales_channel_codes!s}, '
                f'month_range={self.month_range!s}, '
                f'card_networks={(self.card_networks if hasattr(self, "card_networks") else None)!s}, '
                f'sort_results_by={(self.sort_results_by if hasattr(self, "sort_results_by") else None)!s})')
