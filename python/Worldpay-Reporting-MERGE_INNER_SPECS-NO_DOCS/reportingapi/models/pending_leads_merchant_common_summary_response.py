"""reportingapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from reportingapi.api_helper import APIHelper
from reportingapi.models.pending_leads_summary_merchant_details import (
    PendingLeadsSummaryMerchantDetails,
)


class PendingLeadsMerchantCommonSummaryResponse(object):
    """Implementation of the 'PendingLeadsMerchantCommonSummaryResponse' model.

    Pending leads summary for selected month range.

    Attributes:
        total_estimated_revenue_amount (float): Total estiamted revenue amount
            of the pending leads.
        total_merchant_count (int): Total number of merchants in pipeline for
            the selected month range.
        total_lead_count (int): Total number of leads generated for the
            independent sales channel(s) for the selected range of month.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "total_estimated_revenue_amount": "totalEstimatedRevenueAmount",
        "total_merchant_count": "totalMerchantCount",
        "total_lead_count": "totalLeadCount",
    }

    _optionals = [
        "total_estimated_revenue_amount",
        "total_merchant_count",
        "total_lead_count",
    ]

    def __init__(self,
                 total_estimated_revenue_amount=APIHelper.SKIP,
                 total_merchant_count=APIHelper.SKIP,
                 total_lead_count=APIHelper.SKIP):
        """Initialize a PendingLeadsMerchantCommonSummaryResponse instance."""
        # Initialize members of the class
        if total_estimated_revenue_amount is not APIHelper.SKIP:
            self.total_estimated_revenue_amount = total_estimated_revenue_amount
        if total_merchant_count is not APIHelper.SKIP:
            self.total_merchant_count = total_merchant_count
        if total_lead_count is not APIHelper.SKIP:
            self.total_lead_count = total_lead_count

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        total_estimated_revenue_amount =\
            dictionary.get("totalEstimatedRevenueAmount")\
            if dictionary.get("totalEstimatedRevenueAmount") else APIHelper.SKIP
        total_merchant_count =\
            dictionary.get("totalMerchantCount")\
            if dictionary.get("totalMerchantCount") else APIHelper.SKIP
        total_lead_count =\
            dictionary.get("totalLeadCount")\
            if dictionary.get("totalLeadCount") else APIHelper.SKIP
        # Return an object of this model
        return cls(total_estimated_revenue_amount,
                   total_merchant_count,
                   total_lead_count)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"total_estimated_revenue_amount={(self.total_estimated_revenue_amount
                     if hasattr(self, 'total_estimated_revenue_amount') else None)!r}, "
                f"total_merchant_count={(self.total_merchant_count
                     if hasattr(self, 'total_merchant_count') else None)!r}, "
                f"total_lead_count={(self.total_lead_count
                     if hasattr(self, 'total_lead_count') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"total_estimated_revenue_amount={(self.total_estimated_revenue_amount
                     if hasattr(self, 'total_estimated_revenue_amount') else None)!s}, "
                f"total_merchant_count={(self.total_merchant_count
                     if hasattr(self, 'total_merchant_count') else None)!s}, "
                f"total_lead_count={(self.total_lead_count
                     if hasattr(self, 'total_lead_count') else None)!s})")

class PendingLeadsSummaryMerchantResponse(PendingLeadsMerchantCommonSummaryResponse):
    """Implementation of the 'PendingLeadsSummaryMerchantResponse' model.

    Pending leads summary for selected month range.
    NOTE: This class inherits from 'PendingLeadsMerchantCommonSummaryResponse'.

    Attributes:
        summaries (List[PendingLeadsSummaryMerchantDetails]): Pending leads
            summary grouped by merchant.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "summaries": "summaries",
        "total_estimated_revenue_amount": "totalEstimatedRevenueAmount",
        "total_merchant_count": "totalMerchantCount",
        "total_lead_count": "totalLeadCount",
    }

    _optionals = [
        "summaries",
    ]
    _optionals.extend(PendingLeadsMerchantCommonSummaryResponse._optionals)

    def __init__(self,
                 summaries=APIHelper.SKIP,
                 total_estimated_revenue_amount=APIHelper.SKIP,
                 total_merchant_count=APIHelper.SKIP,
                 total_lead_count=APIHelper.SKIP):
        """Initialize a PendingLeadsSummaryMerchantResponse instance."""
        # Initialize members of the class
        if summaries is not APIHelper.SKIP:
            self.summaries = summaries

        # Call the constructor for the base class
        super(PendingLeadsSummaryMerchantResponse, self).__init__(total_estimated_revenue_amount,
                                                                  total_merchant_count,
                                                                  total_lead_count)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        summaries = None
        if dictionary.get("summaries") is not None:
            summaries = [
                PendingLeadsSummaryMerchantDetails.from_dictionary(x)
                    for x in dictionary.get("summaries")
            ]
        else:
            summaries = APIHelper.SKIP
        total_estimated_revenue_amount =\
            dictionary.get("totalEstimatedRevenueAmount")\
            if dictionary.get("totalEstimatedRevenueAmount") else APIHelper.SKIP
        total_merchant_count =\
            dictionary.get("totalMerchantCount")\
            if dictionary.get("totalMerchantCount") else APIHelper.SKIP
        total_lead_count =\
            dictionary.get("totalLeadCount")\
            if dictionary.get("totalLeadCount") else APIHelper.SKIP
        # Return an object of this model
        return cls(summaries,
                   total_estimated_revenue_amount,
                   total_merchant_count,
                   total_lead_count)

    def __repr__(self):
        """Return a unambiguous string representation."""
        base_repr = super().__repr__()
        return (f"{self.__class__.__name__}("
                f"{base_repr[base_repr.find('(') + 1:-1]}, "
                f"summaries={(self.summaries
                     if hasattr(self, 'summaries') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        base_str = super().__str__()
        return (f"{self.__class__.__name__}("
                f"{base_str[base_str.find('(') + 1:-1]}, "
                f"summaries={(self.summaries
                     if hasattr(self, 'summaries') else None)!s})")
