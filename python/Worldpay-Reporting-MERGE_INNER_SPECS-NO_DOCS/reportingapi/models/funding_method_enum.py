# -*- coding: utf-8 -*-

"""
reportingapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class FundingMethodEnum(object):

    """Implementation of the 'FundingMethod' enum.

    Refers to the method that was used for the deposit. Bank Account indicates
    the methods used for a payment.

    Attributes:
        BANK_ACCOUNT: The enum member of type str.
        PREPAID_CARD: The enum member of type str.

    """
    BANK_ACCOUNT = 'BANK_ACCOUNT'

    PREPAID_CARD = 'PREPAID_CARD'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
