"""reportingapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from reportingapi.api_helper import APIHelper
from reportingapi.models.month_range_1 import (
    MonthRange1,
)
from reportingapi.models.sort_leads_count_category import (
    SortLeadsCountCategory,
)


class LeadsCountCategoryRequest(object):
    """Implementation of the 'LeadsCountCategoryRequest' model.

    Request message to get leads count by category.

    Attributes:
        independent_sales_channel_codes (List[str]): Independent sales channel
            value provides a secondary entity under the independent sales
            organization hierarchy level.
        category (List[CategoryEnum]): Refers to the status of the lead.
        month_range (MonthRange1): Month range for which data is to be
            retrieved. Maximum month range supported is 13 months.
        sort_results_by (SortLeadsCountCategory): Sorting order for the search
            results of leads category.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "independent_sales_channel_codes": "independentSalesChannelCodes",
        "month_range": "monthRange",
        "category": "category",
        "sort_results_by": "sortResultsBy",
    }

    _optionals = [
        "category",
        "sort_results_by",
    ]

    def __init__(self,
                 independent_sales_channel_codes=None,
                 month_range=None,
                 category=APIHelper.SKIP,
                 sort_results_by=APIHelper.SKIP):
        """Initialize a LeadsCountCategoryRequest instance."""
        # Initialize members of the class
        self.independent_sales_channel_codes = independent_sales_channel_codes
        if category is not APIHelper.SKIP:
            self.category = category
        self.month_range = month_range
        if sort_results_by is not APIHelper.SKIP:
            self.sort_results_by = sort_results_by

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        independent_sales_channel_codes =\
            dictionary.get("independentSalesChannelCodes")\
            if dictionary.get("independentSalesChannelCodes") else None
        month_range = MonthRange1.from_dictionary(
            dictionary.get("monthRange"))\
            if dictionary.get("monthRange") else None
        category =\
            dictionary.get("category")\
            if dictionary.get("category") else APIHelper.SKIP
        sort_results_by = SortLeadsCountCategory.from_dictionary(
            dictionary.get("sortResultsBy"))\
            if "sortResultsBy" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(independent_sales_channel_codes,
                   month_range,
                   category,
                   sort_results_by)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"independent_sales_channel_codes={
                    self.independent_sales_channel_codes!r}, "
                f"category={(self.category
                     if hasattr(self, 'category') else None)!r}, "
                f"month_range={self.month_range!r}, "
                f"sort_results_by={(self.sort_results_by
                     if hasattr(self, 'sort_results_by') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"independent_sales_channel_codes={
                    self.independent_sales_channel_codes!s}, "
                f"category={(self.category
                     if hasattr(self, 'category') else None)!s}, "
                f"month_range={self.month_range!s}, "
                f"sort_results_by={(self.sort_results_by
                     if hasattr(self, 'sort_results_by') else None)!s})")
