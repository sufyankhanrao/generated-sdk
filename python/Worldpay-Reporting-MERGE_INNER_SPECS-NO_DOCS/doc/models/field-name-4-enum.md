
# Field Name 4 Enum

List of fields for sorting the result set. The result set is sorted in the given order. <br> By default sorting will be done by CARD_NETWORK.

## Enumeration

`FieldName4Enum`

## Fields

| Name |
|  --- |
| `GROSS_REVENUE` |
| `PROCESS_REVENUE` |
| `SETTLED_AMOUNT` |
| `SETTLED_COUNT` |
| `CARD_NETWORK` |

## Example

```
CARD_NETWORK
```

