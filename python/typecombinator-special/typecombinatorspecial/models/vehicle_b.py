"""typecombinatorspecial.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from typecombinatorspecial.api_helper import (
    APIHelper,
)
from typecombinatorspecial.models.month_number_enum import (
    MonthNumberEnum,
)


class VehicleB(object):
    """Implementation of the 'VehicleB' model.

    Attributes:
        number_of_tyres (int): The model property of type int.
        model (MonthNumberEnum): The model property of type MonthNumberEnum.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "number_of_tyres": "NumberOfTyres",
        "model": "model",
    }

    def __init__(self,
                 number_of_tyres=None,
                 model=None):
        """Initialize a VehicleB instance."""
        # Initialize members of the class
        self.number_of_tyres = number_of_tyres
        self.model = model

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        number_of_tyres =\
            dictionary.get("NumberOfTyres")\
            if dictionary.get("NumberOfTyres") else None
        model = dictionary.get("model") if dictionary.get("model") else None
        # Return an object of this model
        return cls(number_of_tyres,
                   model)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                value=dictionary.number_of_tyres,
                type_callable=lambda value: isinstance(value, int)) \
                and APIHelper.is_valid_type(
                value=dictionary.model,
                type_callable=lambda value: MonthNumberEnum.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
            value=dictionary.get("NumberOfTyres"),
            type_callable=lambda value: isinstance(value, int)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("model"),
            type_callable=lambda value: MonthNumberEnum.validate(value))

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"number_of_tyres={self.number_of_tyres!r}, "
                f"model={self.model!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"number_of_tyres={self.number_of_tyres!s}, "
                f"model={self.model!s})")
