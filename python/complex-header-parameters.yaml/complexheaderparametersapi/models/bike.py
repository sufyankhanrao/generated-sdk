# -*- coding: utf-8 -*-

"""
complexheaderparametersapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from complexheaderparametersapi.api_helper import APIHelper


class Bike(object):

    """Implementation of the 'Bike' model.

    Attributes:
        make (str): The model property of type str.
        model (str): The model property of type str.
        year (int): The model property of type int.
        engine_capacity (int): The model property of type int.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "make": 'make',
        "model": 'model',
        "year": 'year',
        "engine_capacity": 'engineCapacity'
    }

    def __init__(self,
                 make=None,
                 model=None,
                 year=None,
                 engine_capacity=None):
        """Constructor for the Bike class"""

        # Initialize members of the class
        self.make = make 
        self.model = model 
        self.year = year 
        self.engine_capacity = engine_capacity 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        make = dictionary.get("make") if dictionary.get("make") else None
        model = dictionary.get("model") if dictionary.get("model") else None
        year = dictionary.get("year") if dictionary.get("year") else None
        engine_capacity = dictionary.get("engineCapacity") if dictionary.get("engineCapacity") else None
        # Return an object of this model
        return cls(make,
                   model,
                   year,
                   engine_capacity)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.make,
                                           type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.model,
                                            type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.year,
                                            type_callable=lambda value: isinstance(value, int)) \
                and APIHelper.is_valid_type(value=dictionary.engine_capacity,
                                            type_callable=lambda value: isinstance(value, int))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('make'),
                                       type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('model'),
                                        type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('year'),
                                        type_callable=lambda value: isinstance(value, int)) \
            and APIHelper.is_valid_type(value=dictionary.get('engineCapacity'),
                                        type_callable=lambda value: isinstance(value, int))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'make={self.make!r}, '
                f'model={self.model!r}, '
                f'year={self.year!r}, '
                f'engine_capacity={self.engine_capacity!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'make={self.make!s}, '
                f'model={self.model!s}, '
                f'year={self.year!s}, '
                f'engine_capacity={self.engine_capacity!s})')
