# -*- coding: utf-8 -*-

"""
multipleresponsesmissingcontent

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""

import json

from tests.controllers.controller_test_base import ControllerTestBase
from apimatic_core.utilities.comparison_helper import ComparisonHelper
from multipleresponsesmissingcontent.api_helper import APIHelper


class APIControllerTests(ControllerTestBase):

    controller = None

    @classmethod
    def setUpClass(cls):
        super(APIControllerTests, cls).setUpClass()
        cls.controller = cls.client.client
        cls.response_catcher = cls.controller.http_call_back

    # Todo: Add description for test test_response_with_content
    def test_response_with_content(self):
        # Parameters for the API call
        test_case = 'getWithContent'

        # Perform the API call through the SDK function
        result = self.controller.get_non_scalar_content(test_case)

        # Test response code
        assert self.response_catcher.response.status_code == 200
        
        # Test whether the captured response is as we expected
        assert result is not None
        expected_body = APIHelper.json_deserialize('{"id":14,"name":"some name"}')
        received_body = APIHelper.json_deserialize(self.response_catcher.response.text)
        assert ComparisonHelper.match_body(expected_body, received_body, check_values = True)

    # Todo: Add description for test test_response_without_content
    def test_response_without_content(self):
        # Parameters for the API call
        test_case = 'getWithoutContent'

        # Perform the API call through the SDK function
        result = self.controller.get_non_scalar_content(test_case)

        # Test response code
        assert self.response_catcher.response.status_code == 204

    # Todo: Add description for test test_response_with_string_content
    def test_response_with_string_content(self):
        # Parameters for the API call
        test_case = 'getWithContent'

        # Perform the API call through the SDK function
        result = self.controller.get_scalar_string_content(test_case)

        # Test response code
        assert self.response_catcher.response.status_code == 200
        
        # Test whether the captured response is as we expected
        assert result is not None
        assert 'some name' == self.response_catcher.response.text

    # Todo: Add description for test test_response_without_string_content
    def test_response_without_string_content(self):
        # Parameters for the API call
        test_case = 'getWithoutContent'

        # Perform the API call through the SDK function
        result = self.controller.get_scalar_string_content(test_case)

        # Test response code
        assert self.response_catcher.response.status_code == 204

    # Todo: Add description for test test_response_with_non_null_string_content
    def test_response_with_non_null_string_content(self):
        # Parameters for the API call
        test_case = 'getWithContent'

        # Perform the API call through the SDK function
        result = self.controller.get_scalar_non_null_string(test_case)

        # Test response code
        assert self.response_catcher.response.status_code == 200
        
        # Test whether the captured response is as we expected
        assert result is not None
        assert 'some name' == self.response_catcher.response.text

    # Todo: Add description for test test_response_without_non_null_string_content
    def test_response_without_non_null_string_content(self):
        # Parameters for the API call
        test_case = 'getWithoutContent'

        # Perform the API call through the SDK function
        result = self.controller.get_scalar_non_null_string(test_case)

        # Test response code
        assert self.response_catcher.response.status_code == 204

    # Todo: Add description for test test_response_with_number_content
    def test_response_with_number_content(self):
        # Parameters for the API call
        test_case = 'getWithContent'

        # Perform the API call through the SDK function
        result = self.controller.get_scalar_number_content(test_case)

        # Test response code
        assert self.response_catcher.response.status_code == 200
        
        # Test whether the captured response is as we expected
        assert result is not None
        assert '14' == self.response_catcher.response.text

    # Todo: Add description for test test_response_without_number_content
    def test_response_without_number_content(self):
        # Parameters for the API call
        test_case = 'getWithoutContent'

        # Perform the API call through the SDK function
        result = self.controller.get_scalar_number_content(test_case)

        # Test response code
        assert self.response_catcher.response.status_code == 204

