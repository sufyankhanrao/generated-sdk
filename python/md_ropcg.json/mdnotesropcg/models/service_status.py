"""mdnotesropcg.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from mdnotesropcg.api_helper import APIHelper


class ServiceStatus(object):
    """Implementation of the 'ServiceStatus' model.

    Attributes:
        app (str): The model property of type str.
        moto (str): The model property of type str.
        notes (int): The model property of type int.
        users (int): The model property of type int.
        time (str): The model property of type str.
        os (str): The model property of type str.
        php_version (str): The model property of type str.
        status (str): The model property of type str.
        additional_properties (Dict[str, object]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "app": "app",
        "moto": "moto",
        "notes": "notes",
        "users": "users",
        "time": "time",
        "os": "os",
        "php_version": "php_version",
        "status": "status",
    }

    _optionals = [
        "app",
        "moto",
        "notes",
        "users",
        "time",
        "os",
        "php_version",
        "status",
    ]

    def __init__(
        self,
        app=APIHelper.SKIP,
        moto=APIHelper.SKIP,
        notes=APIHelper.SKIP,
        users=APIHelper.SKIP,
        time=APIHelper.SKIP,
        os=APIHelper.SKIP,
        php_version=APIHelper.SKIP,
        status=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ServiceStatus instance."""
        # Initialize members of the class
        if app is not APIHelper.SKIP:
            self.app = app
        if moto is not APIHelper.SKIP:
            self.moto = moto
        if notes is not APIHelper.SKIP:
            self.notes = notes
        if users is not APIHelper.SKIP:
            self.users = users
        if time is not APIHelper.SKIP:
            self.time = time
        if os is not APIHelper.SKIP:
            self.os = os
        if php_version is not APIHelper.SKIP:
            self.php_version = php_version
        if status is not APIHelper.SKIP:
            self.status = status

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        app =\
            dictionary.get("app")\
            if dictionary.get("app")\
                else APIHelper.SKIP
        moto =\
            dictionary.get("moto")\
            if dictionary.get("moto")\
                else APIHelper.SKIP
        notes =\
            dictionary.get("notes")\
            if dictionary.get("notes")\
                else APIHelper.SKIP
        users =\
            dictionary.get("users")\
            if dictionary.get("users")\
                else APIHelper.SKIP
        time =\
            dictionary.get("time")\
            if dictionary.get("time")\
                else APIHelper.SKIP
        os =\
            dictionary.get("os")\
            if dictionary.get("os")\
                else APIHelper.SKIP
        php_version =\
            dictionary.get("php_version")\
            if dictionary.get("php_version")\
                else APIHelper.SKIP
        status =\
            dictionary.get("status")\
            if dictionary.get("status")\
                else APIHelper.SKIP

        # Clean out expected properties from dictionary
        additional_properties =\
            {k: v for k, v in dictionary.items() if k not in cls._names.values()}
        # Return an object of this model
        return cls(app,
                   moto,
                   notes,
                   users,
                   time,
                   os,
                   php_version,
                   status,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _app=(
            self.app
            if hasattr(self, "app")
            else None
        )
        _moto=(
            self.moto
            if hasattr(self, "moto")
            else None
        )
        _notes=(
            self.notes
            if hasattr(self, "notes")
            else None
        )
        _users=(
            self.users
            if hasattr(self, "users")
            else None
        )
        _time=(
            self.time
            if hasattr(self, "time")
            else None
        )
        _os=(
            self.os
            if hasattr(self, "os")
            else None
        )
        _php_version=(
            self.php_version
            if hasattr(self, "php_version")
            else None
        )
        _status=(
            self.status
            if hasattr(self, "status")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"app={_app!r}"
            f"moto={_moto!r}"
            f"notes={_notes!r}"
            f"users={_users!r}"
            f"time={_time!r}"
            f"os={_os!r}"
            f"php_version={_php_version!r}"
            f"status={_status!r}"
            f"additional_properties={_additional_properties!r}"
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _app=(
            self.app
            if hasattr(self, "app")
            else None
        )
        _moto=(
            self.moto
            if hasattr(self, "moto")
            else None
        )
        _notes=(
            self.notes
            if hasattr(self, "notes")
            else None
        )
        _users=(
            self.users
            if hasattr(self, "users")
            else None
        )
        _time=(
            self.time
            if hasattr(self, "time")
            else None
        )
        _os=(
            self.os
            if hasattr(self, "os")
            else None
        )
        _php_version=(
            self.php_version
            if hasattr(self, "php_version")
            else None
        )
        _status=(
            self.status
            if hasattr(self, "status")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"app={_app!s}"
            f"moto={_moto!s}"
            f"notes={_notes!s}"
            f"users={_users!s}"
            f"time={_time!s}"
            f"os={_os!s}"
            f"php_version={_php_version!s}"
            f"status={_status!s}"
            f"additional_properties={_additional_properties!s}"
            f")"
        )
