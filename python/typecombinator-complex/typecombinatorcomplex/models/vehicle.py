# -*- coding: utf-8 -*-

"""
typecombinatorcomplex

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from typecombinatorcomplex.api_helper import APIHelper


class Vehicle(object):

    """Implementation of the 'Vehicle' model.

    Attributes:
        number_of_tyres (str): The model property of type str.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "number_of_tyres": 'NumberOfTyres'
    }

    def __init__(self,
                 number_of_tyres=None):
        """Constructor for the Vehicle class"""

        # Initialize members of the class
        self.number_of_tyres = number_of_tyres 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        number_of_tyres = dictionary.get("NumberOfTyres") if dictionary.get("NumberOfTyres") else None
        # Return an object of this model
        return cls(number_of_tyres)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.number_of_tyres,
                                           type_callable=lambda value: isinstance(value, str))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('NumberOfTyres'),
                                       type_callable=lambda value: isinstance(value, str))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'number_of_tyres={self.number_of_tyres!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'number_of_tyres={self.number_of_tyres!s})')

class Car(Vehicle):

    """Implementation of the 'Car' model.
    NOTE: This class inherits from 'Vehicle'.

    Attributes:
        have_trunk (bool): The model property of type bool.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "have_trunk": 'HaveTrunk',
        "number_of_tyres": 'NumberOfTyres'
    }

    def __init__(self,
                 have_trunk=None,
                 number_of_tyres=None):
        """Constructor for the Car class"""

        # Initialize members of the class
        self.have_trunk = have_trunk 

        # Call the constructor for the base class
        super(Car, self).__init__(number_of_tyres)

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        have_trunk = dictionary.get("HaveTrunk") if "HaveTrunk" in dictionary.keys() else None
        number_of_tyres = dictionary.get("NumberOfTyres") if dictionary.get("NumberOfTyres") else None
        # Return an object of this model
        return cls(have_trunk,
                   number_of_tyres)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.have_trunk,
                                           type_callable=lambda value: isinstance(value, bool)) \
                and APIHelper.is_valid_type(value=dictionary.number_of_tyres,
                                            type_callable=lambda value: isinstance(value, str))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('HaveTrunk'),
                                       type_callable=lambda value: isinstance(value, bool)) \
            and APIHelper.is_valid_type(value=dictionary.get('NumberOfTyres'),
                                        type_callable=lambda value: isinstance(value, str))

    def __repr__(self):
        base_repr = super().__repr__()
        return (f'{self.__class__.__name__}('
                f'{base_repr[base_repr.find("(") + 1:-1]}, '
                f'have_trunk={self.have_trunk!r})')

    def __str__(self):
        base_str = super().__str__()
        return (f'{self.__class__.__name__}('
                f'{base_str[base_str.find("(") + 1:-1]}, '
                f'have_trunk={self.have_trunk!s})')
