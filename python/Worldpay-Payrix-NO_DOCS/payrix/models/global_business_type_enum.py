"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class GlobalBusinessTypeEnum(object):
    """Implementation of the 'GlobalBusinessType' enum.

    Attributes:
        SSN: Social Security Number.
        TIN: Employer Identification Number.
        OTHER: Other/Unknown TIN.
        CD: Federal
        AB: Alberta
        BC: British Columbia
        MB: Manitoba
        NB: New Brunswick
        NL: Newfoundland and Labrador
        NT: Northwest Territories
        NS: Nova Scotia
        NU: Nunavut
        ON: Ontario
        PE: Prince Edward Island
        QC: Quebec
        SK: Saskatchewan
        YT: Yukon

    """

    _all_values = ["ssn", "tin", "other", "CD", "AB", "BC", "MB", "NB", "NL", "NT", "NS", "NU", "ON", "PE", "QC", "SK", "YT"]
    SSN = "ssn"

    TIN = "tin"

    OTHER = "other"

    CD = "CD"

    AB = "AB"

    BC = "BC"

    MB = "MB"

    NB = "NB"

    NL = "NL"

    NT = "NT"

    NS = "NS"

    NU = "NU"

    ON = "ON"

    PE = "PE"

    QC = "QC"

    SK = "SK"

    YT = "YT"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
