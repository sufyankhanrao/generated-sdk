# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class Platform1Enum(object):

    """Implementation of the 'platform1' enum.

    Attributes:
        APPLE: The Apple Payment Processor.
        ELAVON: The Elavon processor.
        FIRSTDATA: The FirstData processor.
        GOOGLE: The Google Payment Processor.
        VANTIV: The WorldPay (aka Vantiv or Litle) eComm (aka VAP) processor.
        VCORE: The WorldPay (aka Vantiv) Core processor.
        TDBANKCA: External funding with TD Bank Canada via the Operating
            Account.
        WELLSACH: The Wells Fargo ACH processor.
        WELLSFARGO: The Wells Fargo Merchant Services processor.
        WFSINGLE: The WFSINGLE processor.
        WORLDPAY: The WORLDPAY processor.

    """
    _all_values = ['APPLE', 'ELAVON', 'FIRSTDATA', 'GOOGLE', 'VANTIV', 'VCORE', 'TDBANKCA', 'WELLSACH', 'WELLSFARGO', 'WFSINGLE', 'WORLDPAY']
    APPLE = 'APPLE'

    ELAVON = 'ELAVON'

    FIRSTDATA = 'FIRSTDATA'

    GOOGLE = 'GOOGLE'

    VANTIV = 'VANTIV'

    VCORE = 'VCORE'

    TDBANKCA = 'TDBANKCA'

    WELLSACH = 'WELLSACH'

    WELLSFARGO = 'WELLSFARGO'

    WFSINGLE = 'WFSINGLE'

    WORLDPAY = 'WORLDPAY'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
