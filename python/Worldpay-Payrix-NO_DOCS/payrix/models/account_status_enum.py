# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class AccountStatusEnum(object):

    """Implementation of the 'accountStatus' enum.

    Attributes:
        NOTREADY: Not Ready. The account holder is not yet ready to verify the
            Account.
        READY: Ready. The account is ready to be verified.
        CHALLENGED: Challenged. The account has processed the challenge.
        VERIFIED: Verified. The Account has been verified.
        MANUAL: Manual. There has been an issue during verification and
            further attempts to verify the Account will require manual
            intervention.

    """
    _all_values = [0, 1, 2, 3, 4]
    NOTREADY = 0

    READY = 1

    CHALLENGED = 2

    VERIFIED = 3

    MANUAL = 4

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
