# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class HoldActionEnum(object):

    """Implementation of the 'holdAction' enum.

    Attributes:
        NONE: No action.
        BLOCK: Block the Transaction from proceeding, This returns an error.
        HOLD: Hold the Transaction, It will not be captured until it is
            manually released.
        RESERVE: Reserve the Transaction, The funds for the transaction will
            not be released until the Transaction is manually reviewed.
        LIMIT: Block current activity.
        PASS: Passed decision(s).
        POSTREVIEWONLY: We onboard the merchant and wait for manual check
            later.

    """
    _all_values = [0, 1, 3, 4, 5, 6, 8]
    ENUM_NONE = 0

    BLOCK = 1

    HOLD = 3

    RESERVE = 4

    LIMIT = 5

    PASS = 6

    POSTREVIEWONLY = 8

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
