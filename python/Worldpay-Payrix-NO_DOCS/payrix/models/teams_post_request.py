# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper
from payrix.models.auto_cascade_disabled_enum import AutoCascadeDisabledEnum
from payrix.models.auto_cascade_owner_enum import AutoCascadeOwnerEnum
from payrix.models.frozen_enum import FrozenEnum
from payrix.models.inactive_enum import InactiveEnum


class TeamsPostRequest(object):

    """Implementation of the 'teamsPostRequest' model.

    Attributes:
        login (str): The Login that owns this Team.
        name (str): The name of this Team.  This field is stored as a text
            string and must be between 0 and 100 characters long.
        description (str): A description of this Team.  This field is stored
            as a text string and must be between 0 and 500 characters long.
        auto_cascade_disabled (AutoCascadeDisabledEnum): Whether new Logins
            created by member Logins of this Team will be added to this team
            or not. Default is 0 (enabled).   <details> <summary>Valid
            Values</summary>    - `0` - **Disabled**   - `1` - **Enabled**  
            </details>
        auto_cascade_owner (AutoCascadeOwnerEnum): An integer-boolean
            indicating whether or not the cascading functionality should
            extend to the owner of this team.   <details> <summary>Valid
            Values</summary>    - `0` - **OFF**   - `1` - **ON**   </details>
        inactive (InactiveEnum): Whether this resource is marked as inactive. 
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen. 
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "login": 'login',
        "auto_cascade_disabled": 'autoCascadeDisabled',
        "auto_cascade_owner": 'autoCascadeOwner',
        "inactive": 'inactive',
        "frozen": 'frozen',
        "name": 'name',
        "description": 'description'
    }

    _optionals = [
        'name',
        'description',
    ]

    def __init__(self,
                 login=None,
                 auto_cascade_disabled=0,
                 auto_cascade_owner=0,
                 inactive=0,
                 frozen=0,
                 name=APIHelper.SKIP,
                 description=APIHelper.SKIP):
        """Constructor for the TeamsPostRequest class"""

        # Initialize members of the class
        self.login = login 
        if name is not APIHelper.SKIP:
            self.name = name 
        if description is not APIHelper.SKIP:
            self.description = description 
        self.auto_cascade_disabled = auto_cascade_disabled 
        self.auto_cascade_owner = auto_cascade_owner 
        self.inactive = inactive 
        self.frozen = frozen 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        login = dictionary.get("login") if dictionary.get("login") else None
        auto_cascade_disabled = dictionary.get("autoCascadeDisabled") if dictionary.get("autoCascadeDisabled") else 0
        auto_cascade_owner = dictionary.get("autoCascadeOwner") if dictionary.get("autoCascadeOwner") else 0
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        name = dictionary.get("name") if dictionary.get("name") else APIHelper.SKIP
        description = dictionary.get("description") if dictionary.get("description") else APIHelper.SKIP
        # Return an object of this model
        return cls(login,
                   auto_cascade_disabled,
                   auto_cascade_owner,
                   inactive,
                   frozen,
                   name,
                   description)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.login,
                                           type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.auto_cascade_disabled,
                                            type_callable=lambda value: AutoCascadeDisabledEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.auto_cascade_owner,
                                            type_callable=lambda value: AutoCascadeOwnerEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.inactive,
                                            type_callable=lambda value: InactiveEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.frozen,
                                            type_callable=lambda value: FrozenEnum.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('login'),
                                       type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('autoCascadeDisabled'),
                                        type_callable=lambda value: AutoCascadeDisabledEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('autoCascadeOwner'),
                                        type_callable=lambda value: AutoCascadeOwnerEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('inactive'),
                                        type_callable=lambda value: InactiveEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('frozen'),
                                        type_callable=lambda value: FrozenEnum.validate(value))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'login={self.login!r}, '
                f'name={(self.name if hasattr(self, "name") else None)!r}, '
                f'description={(self.description if hasattr(self, "description") else None)!r}, '
                f'auto_cascade_disabled={self.auto_cascade_disabled!r}, '
                f'auto_cascade_owner={self.auto_cascade_owner!r}, '
                f'inactive={self.inactive!r}, '
                f'frozen={self.frozen!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'login={self.login!s}, '
                f'name={(self.name if hasattr(self, "name") else None)!s}, '
                f'description={(self.description if hasattr(self, "description") else None)!s}, '
                f'auto_cascade_disabled={self.auto_cascade_disabled!s}, '
                f'auto_cascade_owner={self.auto_cascade_owner!s}, '
                f'inactive={self.inactive!s}, '
                f'frozen={self.frozen!s})')
