"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper
from payrix.models.txn_sessions_configurations import (
    TxnSessionsConfigurations,
)


class TxnSessionsPostRequest(object):
    """Implementation of the 'txnSessionsPostRequest' model.

    Attributes:
        login (str): The login under which to perform this action. If you do
            not supply a login in this field, then the API defaults to the
            login that is currently authenticated.
        merchant (str): The Merchant being used to create this txn session.
        configurations (TxnSessionsConfigurations): Configurable data to be
            used to enhance security.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "login": "login",
        "merchant": "merchant",
        "configurations": "configurations",
    }

    _optionals = [
        "configurations",
    ]

    def __init__(self,
                 login=None,
                 merchant=None,
                 configurations=APIHelper.SKIP):
        """Initialize a TxnSessionsPostRequest instance."""
        # Initialize members of the class
        self.login = login
        self.merchant = merchant
        if configurations is not APIHelper.SKIP:
            self.configurations = configurations

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        login = dictionary.get("login") if dictionary.get("login") else None
        merchant =\
            dictionary.get("merchant")\
            if dictionary.get("merchant") else None
        configurations = TxnSessionsConfigurations.from_dictionary(
            dictionary.get("configurations"))\
            if "configurations" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(login,
                   merchant,
                   configurations)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"login={self.login!r}, "
                f"merchant={self.merchant!r}, "
                f"configurations={(self.configurations
                     if hasattr(self, 'configurations') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"login={self.login!s}, "
                f"merchant={self.merchant!s}, "
                f"configurations={(self.configurations
                     if hasattr(self, 'configurations') else None)!s})")
