# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class PayoutFlowScheduleEnum(object):

    """Implementation of the 'payoutFlowSchedule' enum.

    Attributes:
        DAYS: Daily. The Payout is paid every day.
        WEEKS: Weekly. The Payout is paid every week.
        MONTHS: Monthly. The Payout is paid every month.
        YEARS: Annually. The Payout is paid every year.
        SINGLE: Single. The Payout is a one-off payment.

    """
    _all_values = ['days', 'weeks', 'months', 'years', 'single']
    DAYS = 'days'

    WEEKS = 'weeks'

    MONTHS = 'months'

    YEARS = 'years'

    SINGLE = 'single'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
