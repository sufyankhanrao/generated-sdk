"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class CategoryEnum(object):
    """Implementation of the 'Category' enum.

    Attributes:
        CLASSIC: Classic credit or debit card.
        BUSINESS: Business credit or debit card.
        CORPORATE: Corporate credit or debit card.
        FLEET: Fleet card for transportation-related expenses.
        HSAFSA: HSA (Health Savings Account) or FSA (Flexible Spending
            Account) card.
        PLATINUM: Platinum credit card.
        PREPAID: Prepaid card.
        CORECARD: Core card.
        PERSONAL: Personal account.
        STANDARD: Standard account.
        OTHER: Other type of account.
        INTERNATIONAL: International account.

    """

    _all_values = ["classic", "business", "corporate", "fleet", "hsaFsa", "platinum", "prepaid", "coreCard", "personal", "standard", "other", "international"]
    CLASSIC = "classic"

    BUSINESS = "business"

    CORPORATE = "corporate"

    FLEET = "fleet"

    HSAFSA = "hsaFsa"

    PLATINUM = "platinum"

    PREPAID = "prepaid"

    CORECARD = "coreCard"

    PERSONAL = "personal"

    STANDARD = "standard"

    OTHER = "other"

    INTERNATIONAL = "international"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
