# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class SubscriptionOriginEnum(object):

    """Implementation of the 'subscriptionOrigin' enum.

    Attributes:
        ECOMMERCE: Customer will be subscribing through eCommerce.
        MAILORDERTELEPHONE: Customer will be subscribing by Mail
            Order/Telephone.
        APPLEPAY: Originated with Apple Pay.
        SUCCESSFUL3DSECURE: Originated as a Successful 3D Secure transaction.
        ATTEMPTED3DSECURE: Originated as an Attempted 3D Secure transaction.
        PAYFRAME: Originated in a Payframe.

    """
    _all_values = [2, 3, 4, 5, 6, 8]
    ECOMMERCE = 2

    MAILORDERTELEPHONE = 3

    APPLEPAY = 4

    SUCCESSFUL3DSECURE = 5

    ATTEMPTED3DSECURE = 6

    PAYFRAME = 8

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
