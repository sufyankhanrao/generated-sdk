# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper


class TxnSessionsConfigurations(object):

    """Implementation of the 'txnSessionsConfigurations' model.

    Configurable data to be used to enhance security.

    Attributes:
        duration (int): duration refers to How many minutes this key should
            live (automated voided when tried to use after expired).
        max_times_approved (int): maxTimesApproved refers to how many approved
            transactions related it can be used.
        max_times_use (int): maxTimesUse refers to how many times we can make
            request with this key.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "duration": 'duration',
        "max_times_approved": 'maxTimesApproved',
        "max_times_use": 'maxTimesUse'
    }

    _optionals = [
        'duration',
        'max_times_approved',
        'max_times_use',
    ]

    def __init__(self,
                 duration=APIHelper.SKIP,
                 max_times_approved=APIHelper.SKIP,
                 max_times_use=APIHelper.SKIP):
        """Constructor for the TxnSessionsConfigurations class"""

        # Initialize members of the class
        if duration is not APIHelper.SKIP:
            self.duration = duration 
        if max_times_approved is not APIHelper.SKIP:
            self.max_times_approved = max_times_approved 
        if max_times_use is not APIHelper.SKIP:
            self.max_times_use = max_times_use 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        duration = dictionary.get("duration") if dictionary.get("duration") else APIHelper.SKIP
        max_times_approved = dictionary.get("maxTimesApproved") if dictionary.get("maxTimesApproved") else APIHelper.SKIP
        max_times_use = dictionary.get("maxTimesUse") if dictionary.get("maxTimesUse") else APIHelper.SKIP
        # Return an object of this model
        return cls(duration,
                   max_times_approved,
                   max_times_use)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'duration={(self.duration if hasattr(self, "duration") else None)!r}, '
                f'max_times_approved={(self.max_times_approved if hasattr(self, "max_times_approved") else None)!r}, '
                f'max_times_use={(self.max_times_use if hasattr(self, "max_times_use") else None)!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'duration={(self.duration if hasattr(self, "duration") else None)!s}, '
                f'max_times_approved={(self.max_times_approved if hasattr(self, "max_times_approved") else None)!s}, '
                f'max_times_use={(self.max_times_use if hasattr(self, "max_times_use") else None)!s})')
