# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class TerminalTxnTypeEnum(object):

    """Implementation of the 'terminalTxnType' enum.

    Attributes:
        SALE: Sale Transaction, This is the most common type of Transaction,
            it processes a sale and charges the customer.
        AUTH: Auth Transaction, Authorizes and holds the requested total on
            the credit card.
        UNAUTH: Reverse Authorization, Reverses a prior Auth or Sale
            Transaction and releases the credit hold.
        REFUND: Refund Transaction, Refunds a prior Capture or Sale
            Transaction (total may be specified for a partial refund).
        BATCH: Batch out terminal.

    """
    _all_values = [1, 2, 4, 5, 13]
    SALE = 1

    AUTH = 2

    UNAUTH = 4

    REFUND = 5

    BATCH = 13

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
