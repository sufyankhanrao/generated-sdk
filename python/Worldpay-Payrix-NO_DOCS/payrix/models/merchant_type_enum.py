"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class MerchantTypeEnum(object):
    """Implementation of the 'merchantType' enum.

    Attributes:
        GENERAL: General use for unspecified type.
        INTERNAL: An internal failure result message.
        DATA: Missing merchant boarding related data failure message.
        LEGAL: Result message related to legal matters.
        DENIAL: Merchant boarding denial result message.
        PLATFORM: Processor boarding failure result message.
        PLATFORMNOTE: Processor note from merchant boarding.

    """

    _all_values = ["general", "internal", "data", "legal", "denial", "platform", "platformNote"]
    GENERAL = "general"

    INTERNAL = "internal"

    DATA = "data"

    LEGAL = "legal"

    DENIAL = "denial"

    PLATFORM = "platform"

    PLATFORMNOTE = "platformNote"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
