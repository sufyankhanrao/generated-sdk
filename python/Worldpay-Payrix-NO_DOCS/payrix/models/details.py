# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper
from payrix.models.details_properties import DetailsProperties


class Details(object):

    """Implementation of the 'details' model.

    An object describing the response, including the request ID and pagination
    indicators.

    Attributes:
        request_id (str): A unique identifier set by the API client, that is
            echoed by the response.  The request ID can be useful in
            troubleshooting and monitoring.
        totals (Any | None): The model property of type Any | None.
        page (DetailsProperties): Where the response lists multiple resources,
            the API splits the response into several 'pages'.  This object
            indicates the current and last available pages in the list.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "request_id": 'requestId',
        "totals": 'totals',
        "page": 'page'
    }

    _optionals = [
        'request_id',
        'totals',
        'page',
    ]

    def __init__(self,
                 request_id=APIHelper.SKIP,
                 totals=APIHelper.SKIP,
                 page=APIHelper.SKIP):
        """Constructor for the Details class"""

        # Initialize members of the class
        if request_id is not APIHelper.SKIP:
            self.request_id = request_id 
        if totals is not APIHelper.SKIP:
            self.totals = totals 
        if page is not APIHelper.SKIP:
            self.page = page 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from payrix.utilities.union_type_lookup import UnionTypeLookUp

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        request_id = dictionary.get("requestId") if dictionary.get("requestId") else APIHelper.SKIP
        totals = APIHelper.deserialize_union_type(UnionTypeLookUp.get('DetailsTotals'), dictionary.get('totals'), False) if dictionary.get('totals') is not None else APIHelper.SKIP
        page = DetailsProperties.from_dictionary(dictionary.get('page')) if 'page' in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(request_id,
                   totals,
                   page)

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'request_id={(self.request_id if hasattr(self, "request_id") else None)!r}, '
                f'totals={(self.totals if hasattr(self, "totals") else None)!r}, '
                f'page={(self.page if hasattr(self, "page") else None)!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'request_id={(self.request_id if hasattr(self, "request_id") else None)!s}, '
                f'totals={(self.totals if hasattr(self, "totals") else None)!s}, '
                f'page={(self.page if hasattr(self, "page") else None)!s})')
