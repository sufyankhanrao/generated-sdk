# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class CredentialTypeEnum(object):

    """Implementation of the 'credentialType' enum.

    Attributes:
        TRANSACTION: Processes a sale and charges the customer.
        BATCH: Batch processing.
        BOARDING: Boarding process.
        PAYMENTUPDATES: Updates to payment information.
        PAYOUT: Payout processing.
        PAYOUTCREDIT: Credit payouts.
        PAYOUTDEBIT: Debit payouts.
        CHARGEBACK: Chargeback processing.
        TXNREPORT: Transaction reporting.
        ACCOUNT: Account-related operations.
        ENTITYDECISION: Decision-making for entities.
        TXNCHECK: Transaction checks.
        PAYOUTREPORT: Reporting for payouts.
        FEEREPORT: Reporting for fees.
        CHARGEBACKREPORT: Reporting for chargebacks.
        BINREPORT: Reporting for BINs.
        WATCHLIST: Watchlist management.
        ENCRYPTION: Encryption-related operations.
        CHANGEREQUEST: Requests for changes.

    """
    _all_values = ['transaction', 'batch', 'boarding', 'paymentUpdates', 'payout', 'payoutCredit', 'payoutDebit', 'chargeback', 'txnReport', 'account', 'entityDecision', 'txnCheck', 'payoutReport', 'feeReport', 'chargebackReport', 'binReport', 'watchlist', 'encryption', 'changeRequest']
    TRANSACTION = 'transaction'

    BATCH = 'batch'

    BOARDING = 'boarding'

    PAYMENTUPDATES = 'paymentUpdates'

    PAYOUT = 'payout'

    PAYOUTCREDIT = 'payoutCredit'

    PAYOUTDEBIT = 'payoutDebit'

    CHARGEBACK = 'chargeback'

    TXNREPORT = 'txnReport'

    ACCOUNT = 'account'

    ENTITYDECISION = 'entityDecision'

    TXNCHECK = 'txnCheck'

    PAYOUTREPORT = 'payoutReport'

    FEEREPORT = 'feeReport'

    CHARGEBACKREPORT = 'chargebackReport'

    BINREPORT = 'binReport'

    WATCHLIST = 'watchlist'

    ENCRYPTION = 'encryption'

    CHANGEREQUEST = 'changeRequest'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
