"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper
from payrix.models.decisions_response import (
    DecisionsResponse,
)
from payrix.models.details import Details
from payrix.models.error_item import ErrorItem


class DecisionsResponseItem(object):
    """Implementation of the 'decisionsResponseItem' model.

    Attributes:
        data (List[DecisionsResponse]): The data object that the API returns
            for this request.
        details (Details): An object describing the response, including the
            request ID and pagination indicators.
        errors (List[ErrorItem]): An array of zero or more errors that
            occurred when the API processed the request.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "data": "data",
        "details": "details",
        "errors": "errors",
    }

    _optionals = [
        "data",
        "details",
        "errors",
    ]

    def __init__(self,
                 data=APIHelper.SKIP,
                 details=APIHelper.SKIP,
                 errors=APIHelper.SKIP):
        """Initialize a DecisionsResponseItem instance."""
        # Initialize members of the class
        if data is not APIHelper.SKIP:
            self.data = data
        if details is not APIHelper.SKIP:
            self.details = details
        if errors is not APIHelper.SKIP:
            self.errors = errors

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        data = None
        if dictionary.get("data") is not None:
            data = [
                DecisionsResponse.from_dictionary(x)
                    for x in dictionary.get("data")
            ]
        else:
            data = APIHelper.SKIP
        details = Details.from_dictionary(
            dictionary.get("details"))\
            if "details" in dictionary.keys() else APIHelper.SKIP
        errors = None
        if dictionary.get("errors") is not None:
            errors = [
                ErrorItem.from_dictionary(x)
                    for x in dictionary.get("errors")
            ]
        else:
            errors = APIHelper.SKIP
        # Return an object of this model
        return cls(data,
                   details,
                   errors)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"data={(self.data if hasattr(self, 'data') else None)!r}, "
                f"details={(self.details
                     if hasattr(self, 'details') else None)!r}, "
                f"errors={(self.errors if hasattr(self, 'errors') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"data={(self.data if hasattr(self, 'data') else None)!s}, "
                f"details={(self.details
                     if hasattr(self, 'details') else None)!s}, "
                f"errors={(self.errors if hasattr(self, 'errors') else None)!s})")
