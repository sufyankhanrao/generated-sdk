"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class StatusEnum(object):
    """Implementation of the 'Status' enum.

    Attributes:
        REGISTRATIONREQUESTED: The domains that have been requested to be
            registered.
        REGISTERING: In the process of being registered with Apple.
        REGISTERED: Successfully Registered with Apple.
        FAILEDREGISTRATION: A Failure occurred Registering.
        UNREGISTRATIONREQUESTED: Unregistration requested.
        UNREGISTERING: In the process of being un-registered from Apple.
        UNREGISTERED: Successfully Unregistered with Apple.

    """

    _all_values = ["registration_requested", "registering", "registered", "failed_registration", "unregistration_requested", "unregistering", "unregistered"]
    REGISTRATIONREQUESTED = "registration_requested"

    REGISTERING = "registering"

    REGISTERED = "registered"

    FAILEDREGISTRATION = "failed_registration"

    UNREGISTRATIONREQUESTED = "unregistration_requested"

    UNREGISTERING = "unregistering"

    UNREGISTERED = "unregistered"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
