# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class InvoiceStatusEnum(object):

    """Implementation of the 'invoiceStatus' enum.

    Attributes:
        PENDING: Initial status of invoice before any action is taken.
        CANCELLED: Updated status to indicate the invoice has expired.
        EXPIRED: Updated status once the invoice has expired.
        VIEWED: Updated status once the invoice is viewed by the customer.
        PAID: Updated status once the invoice has been paid by the customer.
        CONFIRMED: Updated status once the invoice has been confirmed by the
            customer.
        REFUNDED: Updated status once the invoice has been refunded.
        REJECTED: Updated status once the invoice has been rejected by the
            customer.

    """
    _all_values = ['pending', 'cancelled', 'expired', 'viewed', 'paid', 'confirmed', 'refunded', 'rejected']
    PENDING = 'pending'

    CANCELLED = 'cancelled'

    EXPIRED = 'expired'

    VIEWED = 'viewed'

    PAID = 'paid'

    CONFIRMED = 'confirmed'

    REFUNDED = 'refunded'

    REJECTED = 'rejected'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
