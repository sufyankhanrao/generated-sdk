# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class NoteDocumentStatusEnum(object):

    """Implementation of the 'noteDocumentStatus' enum.

    Attributes:
        CREATED: The database record has been created.
        PROCESSED: The document upload for this record was processed
            successfully.
        FAILED: The document upload for this record could not be processed.

    """
    CREATED = 'created'

    PROCESSED = 'processed'

    FAILED = 'failed'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
