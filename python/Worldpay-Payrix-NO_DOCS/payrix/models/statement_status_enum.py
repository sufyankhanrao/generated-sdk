# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class StatementStatusEnum(object):

    """Implementation of the 'statementStatus' enum.

    Attributes:
        PENDING: Statement amount is owed and is pending payment.
        PROCESSING: A payment is processing for this statement, pending
            completion.
        PARTIALLYPAID: The statement was partially paid, some amount is still
            outstanding.
        PAID: The statement was paid in full.
        PARTIALLYCANCELLED: The statement was partially cancelled, some amount
            is still outstanding.
        CANCELLED: The statement was completely cancelled and is no longer due
            for payment.

    """
    _all_values = ['pending', 'processing', 'partiallyPaid', 'paid', 'partiallyCancelled', 'cancelled']
    PENDING = 'pending'

    PROCESSING = 'processing'

    PARTIALLYPAID = 'partiallyPaid'

    PAID = 'paid'

    PARTIALLYCANCELLED = 'partiallyCancelled'

    CANCELLED = 'cancelled'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
