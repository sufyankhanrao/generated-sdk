# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class ReservedEnum(object):

    """Implementation of the 'Reserved' enum.

    Attributes:
        NONE: No action.
        BLOCK: Block transaction, will never be processed. The Entity is sent
            to the manual review queue.
        HOLD: Hold transaction, will not be captured.
        RESERVE: Reserve transaction, funds should be reserved.
        LIMIT: Block current activity, no change for merchant.
        PASS: Passed decision(s). Will not be set anywhere, will only be used
            for integration purposes.
        SKIP: We did not have policies to process.
        POSTREVIEWONLY: We onboard the merchant and wait for manual check
            later.
        SCHEDULERESERVERELEASE: Schedule the automatic release of the reserve.
        AUTOMATICREFUNDHOLD: Hold transaction, will not be captured. Automatic
            release when the associated sale is done.

    """
    _all_values = [0, 1, 3, 4, 5, 6, 7, 8, 9, 10]
    ENUM_NONE = 0

    BLOCK = 1

    HOLD = 3

    RESERVE = 4

    LIMIT = 5

    PASS = 6

    SKIP = 7

    POSTREVIEWONLY = 8

    SCHEDULERESERVERELEASE = 9

    AUTOMATICREFUNDHOLD = 10

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
