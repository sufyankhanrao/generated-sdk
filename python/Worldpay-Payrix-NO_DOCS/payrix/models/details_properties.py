"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper


class DetailsProperties(object):
    """Implementation of the 'detailsProperties' model.

    Where the response lists multiple resources, the API splits the response
    into several 'pages'.
    This object indicates the current and last available pages in the list.

    Attributes:
        current (int): The current page in the paginated resource list.
        last (int): The last available page in the paginated resource list.
        has_more (bool): Indicates if another page of results is available.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "current": "current",
        "last": "last",
        "has_more": "hasMore",
    }

    _optionals = [
        "current",
        "last",
        "has_more",
    ]

    def __init__(self,
                 current=APIHelper.SKIP,
                 last=APIHelper.SKIP,
                 has_more=APIHelper.SKIP):
        """Initialize a DetailsProperties instance."""
        # Initialize members of the class
        if current is not APIHelper.SKIP:
            self.current = current
        if last is not APIHelper.SKIP:
            self.last = last
        if has_more is not APIHelper.SKIP:
            self.has_more = has_more

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        current =\
            dictionary.get("current")\
            if dictionary.get("current") else APIHelper.SKIP
        last =\
            dictionary.get("last")\
            if dictionary.get("last") else APIHelper.SKIP
        has_more =\
            dictionary.get("hasMore")\
            if "hasMore" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(current,
                   last,
                   has_more)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"current={(self.current
                     if hasattr(self, 'current') else None)!r}, "
                f"last={(self.last if hasattr(self, 'last') else None)!r}, "
                f"has_more={(self.has_more
                     if hasattr(self, 'has_more') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"current={(self.current
                     if hasattr(self, 'current') else None)!s}, "
                f"last={(self.last if hasattr(self, 'last') else None)!s}, "
                f"has_more={(self.has_more
                     if hasattr(self, 'has_more') else None)!s})")
