"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class DisbursementResultCodeEnum(object):
    """Implementation of the 'disbursementResultCode' enum.

    Attributes:
        PENDING: Disbursement processing is pending account verification.
        INTERNAL: Failed. Internal note and/or failure.
        NSF: Failed. Disbursement return due to insufficient funds.
        BADACCOUNT: Failed. Disbursement return due to incorrect payment
            account information.
        UNAUTHORIZED: Failed. Disbursement return due to customer dispute of
            authorization.
        GENERAL: General note and/or failure.
        NOC: Notice of change.
        PARAMETER: Disbursement processing withheld due to parameter
            configuration.
        SAMEDAY: Failed. Same-day processing note and/or failure.
        TRANSFERDETAILS: Trace number note and/or data.
        PLATFORM: Failed. Processor note and/or failure.

    """

    _all_values = ["pending", "internal", "nsf", "badAccount", "unauthorized", "general", "noc", "parameter", "sameDay", "transferDetails", "platform"]
    PENDING = "pending"

    INTERNAL = "internal"

    NSF = "nsf"

    BADACCOUNT = "badAccount"

    UNAUTHORIZED = "unauthorized"

    GENERAL = "general"

    NOC = "noc"

    PARAMETER = "parameter"

    SAMEDAY = "sameDay"

    TRANSFERDETAILS = "transferDetails"

    PLATFORM = "platform"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
