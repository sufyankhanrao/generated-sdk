"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper


class BillingEventsPostRequest(object):
    """Implementation of the 'billingEventsPostRequest' model.

    Attributes:
        billing (str): The billing to which this billingEvent applies.
        event (BillingEventEnum): The type of event that triggers billing.
            <details> <summary>Valid Values</summary>  - `fees` - **Fee.** Fee
            events trigger the billing configuration. - `chargebacks` -
            **Chargebacks.** Chargeback events trigger the billing
            configuration. - `returns` - **Returns.** Return events trigger
            the billing configuration. - `profitShares` - **Profit Shares.**
            Profit Sharing events trigger the billing configuration. -
            `revShares` - **RevShares.** Rev Sharing events trigger the
            billing configuration.  </details>
        event_schedule (str): The record ID of a specific schedule for the
            event chosen for which the billing configuration applies, all
            other records of the same event are excluded.
        deduct_from_balance (DeductFromBalanceEnum): Whether to deduct the
            billing amount from the entity's account balance.  <details>
            <summary>Valid Values</summary>  - `0` - **Do not deduct from
            balance.** - `1` - **Deduct from balance.**  </details>
        inactive (InactiveEnum): Whether this resource is marked as inactive.
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen.
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "billing": "billing",
        "event": "event",
        "deduct_from_balance": "deductFromBalance",
        "inactive": "inactive",
        "frozen": "frozen",
        "event_schedule": "eventSchedule",
    }

    _optionals = [
        "event_schedule",
    ]

    def __init__(self,
                 billing=None,
                 event=None,
                 deduct_from_balance=0,
                 inactive=0,
                 frozen=0,
                 event_schedule=APIHelper.SKIP):
        """Initialize a BillingEventsPostRequest instance."""
        # Initialize members of the class
        self.billing = billing
        self.event = event
        if event_schedule is not APIHelper.SKIP:
            self.event_schedule = event_schedule
        self.deduct_from_balance = deduct_from_balance
        self.inactive = inactive
        self.frozen = frozen

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        billing = dictionary.get("billing") if dictionary.get("billing") else None
        event = dictionary.get("event") if dictionary.get("event") else None
        deduct_from_balance =\
            dictionary.get("deductFromBalance")\
            if dictionary.get("deductFromBalance") else 0
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        event_schedule =\
            dictionary.get("eventSchedule")\
            if dictionary.get("eventSchedule") else APIHelper.SKIP
        # Return an object of this model
        return cls(billing,
                   event,
                   deduct_from_balance,
                   inactive,
                   frozen,
                   event_schedule)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"billing={self.billing!r}, "
                f"event={self.event!r}, "
                f"event_schedule={(self.event_schedule
                     if hasattr(self, 'event_schedule') else None)!r}, "
                f"deduct_from_balance={self.deduct_from_balance!r}, "
                f"inactive={self.inactive!r}, "
                f"frozen={self.frozen!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"billing={self.billing!s}, "
                f"event={self.event!s}, "
                f"event_schedule={(self.event_schedule
                     if hasattr(self, 'event_schedule') else None)!s}, "
                f"deduct_from_balance={self.deduct_from_balance!s}, "
                f"inactive={self.inactive!s}, "
                f"frozen={self.frozen!s})")
