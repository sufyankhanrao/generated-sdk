"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper


class DecisionRulesPostRequest(object):
    """Implementation of the 'decisionRulesPostRequest' model.

    Attributes:
        decision (str): The identifier of the Decision that this Decision Rule
            applies.
        name (str): The name of this Decision Rule. This field is stored as a
            text string and must be between 0 and 100 characters long.
        description (str): Description of the Decision Rules.
        mtype (DecisionRuleTypeEnum): The type of logic to apply with this
            Decision Rule.  <details> <summary>Valid Values</summary>  -
            `less` - **Less.** - `equal` - **Equal.** - `notEqual` - **Not
            Equal.** - `greater` - **Greater.** - `swiped` - **Swiped.** -
            `signed` - **Signed.** - `type` - **Type.** - `origin` -
            **Origin.** - `method` - **Method.** - `cvvResult` - **CVV
            Result.** - `avsResult` - **AVS Result.** - `3dsResult` - **3DS
            Result. The Decision applies based on the results of a 3DS
            check.** - `related` - **Related.** - `relatedDelay` - **Related
            Delay.** - `relatedFloor` - **Related Floor.** - `relatedCeil` -
            **Related Ceil.** - `mcc` - **MCC.** - `merchantCountry` -
            **Merchant Country.** - `issuerCountry` - **Issuer Country.** -
            `international` - **International.** - `platform` - **Platform.**
            - `methodType` - **Method Type.** - `emv` - **EMV.** - `misuse` -
            **Misuse.** - `bin` - **BIN.** - `primary` - **Primary.** -
            `fundingCurrencyEqual` - **Funding Currency Equal.** -
            `fundingCurrencyNotEqual` - **Funding Currency Not Equal.** -
            `fundingCurrencyMismatch` - **Funding Currency Mismatch.** -
            `settledCurrencyMismatch` - **Settled Currency Mismatch.** -
            `imported` - **Imported.** - `subscription` - **Subscription.** -
            `publicAuth` - **Public Auth.** - `clientIp` - **Client IP.** -
            `debtRepayment` - **Debt Repayment.** - `cofType` - **COF Type.**
            - `entryMode` - **Entry Mode.**  </details>
        value (int): The value to compare against when evaluating this
            Decision Rule.
        grouping (str): A name for a group of rules to be applied in
            conjunction when evaluating this Decision Rule; When grouping is
            used, the Decision will be allowed to be processed if at least one
            of the rules are matched.
        inactive (InactiveEnum): Whether this resource is marked as inactive.
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen.
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "decision": "decision",
        "mtype": "type",
        "value": "value",
        "inactive": "inactive",
        "frozen": "frozen",
        "name": "name",
        "description": "description",
        "grouping": "grouping",
    }

    _optionals = [
        "name",
        "description",
        "grouping",
    ]

    def __init__(self,
                 decision=None,
                 mtype=None,
                 value=None,
                 inactive=0,
                 frozen=0,
                 name=APIHelper.SKIP,
                 description=APIHelper.SKIP,
                 grouping=APIHelper.SKIP):
        """Initialize a DecisionRulesPostRequest instance."""
        # Initialize members of the class
        self.decision = decision
        if name is not APIHelper.SKIP:
            self.name = name
        if description is not APIHelper.SKIP:
            self.description = description
        self.mtype = mtype
        self.value = value
        if grouping is not APIHelper.SKIP:
            self.grouping = grouping
        self.inactive = inactive
        self.frozen = frozen

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        decision =\
            dictionary.get("decision")\
            if dictionary.get("decision") else None
        mtype = dictionary.get("type") if dictionary.get("type") else None
        value = dictionary.get("value") if dictionary.get("value") else None
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        name =\
            dictionary.get("name")\
            if dictionary.get("name") else APIHelper.SKIP
        description =\
            dictionary.get("description")\
            if dictionary.get("description") else APIHelper.SKIP
        grouping =\
            dictionary.get("grouping")\
            if dictionary.get("grouping") else APIHelper.SKIP
        # Return an object of this model
        return cls(decision,
                   mtype,
                   value,
                   inactive,
                   frozen,
                   name,
                   description,
                   grouping)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"decision={self.decision!r}, "
                f"name={(self.name if hasattr(self, 'name') else None)!r}, "
                f"description={(self.description
                     if hasattr(self, 'description') else None)!r}, "
                f"mtype={self.mtype!r}, "
                f"value={self.value!r}, "
                f"grouping={(self.grouping
                     if hasattr(self, 'grouping') else None)!r}, "
                f"inactive={self.inactive!r}, "
                f"frozen={self.frozen!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"decision={self.decision!s}, "
                f"name={(self.name if hasattr(self, 'name') else None)!s}, "
                f"description={(self.description
                     if hasattr(self, 'description') else None)!s}, "
                f"mtype={self.mtype!s}, "
                f"value={self.value!s}, "
                f"grouping={(self.grouping
                     if hasattr(self, 'grouping') else None)!s}, "
                f"inactive={self.inactive!s}, "
                f"frozen={self.frozen!s})")
