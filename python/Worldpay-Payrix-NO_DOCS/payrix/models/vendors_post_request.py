# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper
from payrix.models.frozen_enum import FrozenEnum
from payrix.models.inactive_enum import InactiveEnum


class VendorsPostRequest(object):

    """Implementation of the 'vendorsPostRequest' model.

    Attributes:
        entity (str): The identifier of the Entity associated with this Vendor
            resource.
        division (str): ID of the division associated with this vendor.
        onboarding_contact_emails (str): This holds the different emails the
            partners will want to be reached for onboarding related
            queries/concerns, this is a way of allowing the risk team to
            manage contacts more efficiently.
        risk_contact_emails (str): This holds the different emails the
            partners will want to be reached for risk related
            queries/concerns, this is a way of allowing the risk team to
            manage contacts more efficiently.
        inactive (InactiveEnum): Whether this resource is marked as inactive. 
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen. 
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "entity": 'entity',
        "inactive": 'inactive',
        "frozen": 'frozen',
        "division": 'division',
        "onboarding_contact_emails": 'onboardingContactEmails',
        "risk_contact_emails": 'riskContactEmails'
    }

    _optionals = [
        'division',
        'onboarding_contact_emails',
        'risk_contact_emails',
    ]

    def __init__(self,
                 entity=None,
                 inactive=0,
                 frozen=0,
                 division=APIHelper.SKIP,
                 onboarding_contact_emails=APIHelper.SKIP,
                 risk_contact_emails=APIHelper.SKIP):
        """Constructor for the VendorsPostRequest class"""

        # Initialize members of the class
        self.entity = entity 
        if division is not APIHelper.SKIP:
            self.division = division 
        if onboarding_contact_emails is not APIHelper.SKIP:
            self.onboarding_contact_emails = onboarding_contact_emails 
        if risk_contact_emails is not APIHelper.SKIP:
            self.risk_contact_emails = risk_contact_emails 
        self.inactive = inactive 
        self.frozen = frozen 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        entity = dictionary.get("entity") if dictionary.get("entity") else None
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        division = dictionary.get("division") if dictionary.get("division") else APIHelper.SKIP
        onboarding_contact_emails = dictionary.get("onboardingContactEmails") if dictionary.get("onboardingContactEmails") else APIHelper.SKIP
        risk_contact_emails = dictionary.get("riskContactEmails") if dictionary.get("riskContactEmails") else APIHelper.SKIP
        # Return an object of this model
        return cls(entity,
                   inactive,
                   frozen,
                   division,
                   onboarding_contact_emails,
                   risk_contact_emails)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.entity,
                                           type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.inactive,
                                            type_callable=lambda value: InactiveEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.frozen,
                                            type_callable=lambda value: FrozenEnum.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('entity'),
                                       type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('inactive'),
                                        type_callable=lambda value: InactiveEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('frozen'),
                                        type_callable=lambda value: FrozenEnum.validate(value))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'entity={self.entity!r}, '
                f'division={(self.division if hasattr(self, "division") else None)!r}, '
                f'onboarding_contact_emails={(self.onboarding_contact_emails if hasattr(self, "onboarding_contact_emails") else None)!r}, '
                f'risk_contact_emails={(self.risk_contact_emails if hasattr(self, "risk_contact_emails") else None)!r}, '
                f'inactive={self.inactive!r}, '
                f'frozen={self.frozen!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'entity={self.entity!s}, '
                f'division={(self.division if hasattr(self, "division") else None)!s}, '
                f'onboarding_contact_emails={(self.onboarding_contact_emails if hasattr(self, "onboarding_contact_emails") else None)!s}, '
                f'risk_contact_emails={(self.risk_contact_emails if hasattr(self, "risk_contact_emails") else None)!s}, '
                f'inactive={self.inactive!s}, '
                f'frozen={self.frozen!s})')
