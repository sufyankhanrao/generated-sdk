"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class BinsTypeEnum(object):
    """Implementation of the 'binsType' enum.

    Attributes:
        CREDIT: Credit Card.
        DEBIT: Debit Card.
        DEBITCREDIT: Debit or Credit Card.
        CHARGE: Charge Card.
        PREPAID: Prepaid Debit Card.
        BANKACCOUNT: Bank Account Card.

    """

    _all_values = ["credit", "debit", "debitCredit", "charge", "prepaid", "bankAccount"]
    CREDIT = "credit"

    DEBIT = "debit"

    DEBITCREDIT = "debitCredit"

    CHARGE = "charge"

    PREPAID = "prepaid"

    BANKACCOUNT = "bankAccount"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
