# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class FeeRuleTypeEnum(object):

    """Implementation of the 'feeRuleType' enum.

    Attributes:
        SALE: Credit Card Only Sale Transaction Processes a sale and charges
            the customer.
        AUTH: Credit Card Only Auth Transaction Authorizes and holds the
            requested total on the credit card.
        CAPTURE: Credit Card Only Capture Transaction Finalizes a prior Auth
            Transaction and charges the customer.
        REVERSEAUTHORIZATION: Credit Card Only Reverse Authorization Reverses
            a prior Auth or Sale Transaction and releases the credit hold.
        REFUND: Credit Card Only Refund Transaction Refunds a prior Capture or
            Sale Transaction (total may be specified for a partial refund).
        ECHECKSALE: Echeck Only Echeck Sale Transaction Sale Transaction for
            ECheck payment.
        ECHECKREFUND: Echeck Only ECheck Refund Transaction Refund Transaction
            for prior ECheck Sale Transaction.
        ECHECKREDEPOSIT: Echeck Only Echeck Redeposit Transaction Attempt to
            redeposit a prior failed eCheck Sale Transaction.
        ECHECKVERIFICATION: Echeck Only Echeck Account Verification
            Transaction Attempt to verify eCheck payment details.

    """
    _all_values = [1, 2, 3, 4, 5, 7, 8, 11, 12]
    SALE = 1

    AUTH = 2

    CAPTURE = 3

    REVERSEAUTHORIZATION = 4

    REFUND = 5

    ECHECKSALE = 7

    ECHECKREFUND = 8

    ECHECKREDEPOSIT = 11

    ECHECKVERIFICATION = 12

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
