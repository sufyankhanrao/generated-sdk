"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper


class TxnsPayment(object):
    """Implementation of the 'txnsPayment' model.

    The payment method associated with this Transaction, including the card
    details.

    Attributes:
        method (TxnPaymentMethodEnum): The payment method for this Transaction.
        number (str): For credit payment method, the card number of the credit
            card associated with this Transaction. For eCheck payment method,
            the bank account number associated with this Transaction.
        routing (str): The routing code for the eCheck or bank account payment
            associated with this Transaction.
        expiration (str): The expiration date of the credit card associated
            with this Transaction. This field is stored as a text string in
            'MMYY' format, where 'MM' is the number of a month and 'YY' is the
            last two digits of a year. For example, '0623' for June 2023.
        cvv (int): The Card Verification Value (CVV) number of the credit card
            associated with this Transaction.  This field is expressed as an
            integer.
        track (Any): The 'Track' data (either Track 1, Track 2, or both) of
            the card associated with this Transaction.  This field is stored
            as a text string.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "method": "method",
        "number": "number",
        "routing": "routing",
        "expiration": "expiration",
        "cvv": "cvv",
        "track": "track",
    }

    _optionals = [
        "routing",
        "expiration",
        "cvv",
        "track",
    ]

    def __init__(self,
                 method=None,
                 number=None,
                 routing=APIHelper.SKIP,
                 expiration=APIHelper.SKIP,
                 cvv=0,
                 track=APIHelper.SKIP):
        """Initialize a TxnsPayment instance."""
        # Initialize members of the class
        self.method = method
        self.number = number
        if routing is not APIHelper.SKIP:
            self.routing = routing
        if expiration is not APIHelper.SKIP:
            self.expiration = expiration
        self.cvv = cvv
        if track is not APIHelper.SKIP:
            self.track = track

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        method = dictionary.get("method") if dictionary.get("method") else None
        number = dictionary.get("number") if dictionary.get("number") else None
        routing =\
            dictionary.get("routing")\
            if dictionary.get("routing") else APIHelper.SKIP
        expiration =\
            dictionary.get("expiration")\
            if dictionary.get("expiration") else APIHelper.SKIP
        cvv = dictionary.get("cvv") if dictionary.get("cvv") else 0
        track =\
            dictionary.get("track")\
            if dictionary.get("track") else APIHelper.SKIP
        # Return an object of this model
        return cls(method,
                   number,
                   routing,
                   expiration,
                   cvv,
                   track)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"method={self.method!r}, "
                f"number={self.number!r}, "
                f"routing={(self.routing
                     if hasattr(self, 'routing') else None)!r}, "
                f"expiration={(self.expiration
                     if hasattr(self, 'expiration') else None)!r}, "
                f"cvv={(self.cvv if hasattr(self, 'cvv') else None)!r}, "
                f"track={(self.track if hasattr(self, 'track') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"method={self.method!s}, "
                f"number={self.number!s}, "
                f"routing={(self.routing
                     if hasattr(self, 'routing') else None)!s}, "
                f"expiration={(self.expiration
                     if hasattr(self, 'expiration') else None)!s}, "
                f"cvv={(self.cvv if hasattr(self, 'cvv') else None)!s}, "
                f"track={(self.track if hasattr(self, 'track') else None)!s})")
