# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper
from payrix.models.credential_integration_enum import CredentialIntegrationEnum
from payrix.models.credential_type_enum import CredentialTypeEnum
from payrix.models.frozen_enum import FrozenEnum
from payrix.models.inactive_enum import InactiveEnum


class CredentialsPostRequest(object):

    """Implementation of the 'credentialsPostRequest' model.

    Attributes:
        entity (str): The identifier of the Entity that this Credential
            resource belongs to.
        name (str): The name of this Credential resource.  This field is
            stored as a text string and must be between 1 and 100 characters
            long.
        description (str): A description of this Credential resource.  This
            field is stored as a text string and must be between 1 and 100
            characters long.
        username (str): The username to use when authenticating to the
            integration associated with this Credential resource.  This field
            is stored as a text string and must be between 1 and 50 characters
            long.
        password (str): The password to use when authenticating to the
            integration associated with this Credential resource.  This field
            is stored as a text string and must be between 1 and 50 characters
            long.
        connect_username (str): The username to use when connecting to the
            integration associated with this Credential resource.  This field
            is stored as a text string and must be between 1 and 50 characters
            long.  This field is only necessary when it is required by the
            integration.
        connect_password (str): The password to use when connecting to the
            integration associated with this Credential resource.  This field
            is stored as a text string and must be between 1 and 50 characters
            long.  This field is only necessary when it is required by the
            integration.
        integration (CredentialIntegrationEnum): The payment platform
            integration using credential logins.   <details> <summary>Valid
            Values</summary>  - `APPLE` - **Apple API**  - `ELAVON` - **Elavon
            API**  - `FEDACH` - **FedACH Account Integration**  - `FIRSTDATA`
            - **FirstData / Clover Integration**  - `NEUTRINO` - **Neutrino
            eCommerce Integration**  - `OFAC` - **Office of Foreign Assets
            Control Sanctions**  - `PAYRIX` - **Payrix API**  - `PLAID` -
            **Plaid Account Integration**  - `SIFT` - **Sift Fraud
            Management**  - `SOCURE` - **Socure ID Verification**  -
            `SOUNDPAYMENTS` - **Sound Payments POS**  - `TDBANK` - **TD Bank
            Platform**  - `VANTIV` - **WorldPay / Vantiv eComm Platform**  -
            `VCORE` - **WorldPay / Vantiv Core Platform**  - `WEBSHIELD` -
            **Web Shield Merchant Monitoring**  - `WELLSACH` - **Wells Fargo
            Merchant Services ACH Processor.**  - `WELLSFARGO` - **Wells Fargo
            Merchant Services Payment Processor**  - `WFSINGLE` - **Wells
            Fargo Single Payment Processors**  </details>
        mtype (CredentialTypeEnum): The type of Transaction.  <details>
            <summary>Valid Values</summary>  - `1` - **Credit Card Only:**
            Sale Transaction. Processes a sale and charges the customer. - `2`
            - **Credit Card Only:** Auth Transaction. Authorizes and holds the
            requested total on the credit card. - `3` - **Credit Card Only:**
            Capture Transaction. Finalizes a prior Auth Transaction and
            charges the customer. - `4` - **Credit Card Only:** Reverse
            Authorization. Reverses a prior Auth or Sale Transaction and
            releases the credit hold. - `5` - **Credit Card Only:** Refund
            Transaction. Refunds a prior Capture or Sale Transaction (total
            may be specified for a partial refund). - `7` - **Echeck Only:**
            Echeck Sale Transaction. Sale Transaction for ECheck payment. -
            `8` - **ECheck Only:** ECheck Refund Transaction. Refund
            Transaction for prior ECheck Sale Transaction. - `11` - **Echeck
            Only:** Echeck Redeposit Transaction. Attempt to redeposit a prior
            failed eCheck Sale Transaction. - `12` - **Echeck Only:** Echeck
            Account Verification Transaction. Attempt to verify eCheck payment
            details.  </details>
        secret (str): The secret resource identifier to use when connecting
            using this Credential.  This field is only necessary when a
            private key is required by the integration.
        inactive (InactiveEnum): Whether this resource is marked as inactive. 
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen. 
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "entity": 'entity',
        "username": 'username',
        "integration": 'integration',
        "mtype": 'type',
        "inactive": 'inactive',
        "frozen": 'frozen',
        "name": 'name',
        "description": 'description',
        "password": 'password',
        "connect_username": 'connectUsername',
        "connect_password": 'connectPassword',
        "secret": 'secret'
    }

    _optionals = [
        'name',
        'description',
        'password',
        'connect_username',
        'connect_password',
        'secret',
    ]

    def __init__(self,
                 entity=None,
                 username=None,
                 integration=None,
                 mtype=None,
                 inactive=0,
                 frozen=0,
                 name=APIHelper.SKIP,
                 description=APIHelper.SKIP,
                 password=APIHelper.SKIP,
                 connect_username=APIHelper.SKIP,
                 connect_password=APIHelper.SKIP,
                 secret=APIHelper.SKIP):
        """Constructor for the CredentialsPostRequest class"""

        # Initialize members of the class
        self.entity = entity 
        if name is not APIHelper.SKIP:
            self.name = name 
        if description is not APIHelper.SKIP:
            self.description = description 
        self.username = username 
        if password is not APIHelper.SKIP:
            self.password = password 
        if connect_username is not APIHelper.SKIP:
            self.connect_username = connect_username 
        if connect_password is not APIHelper.SKIP:
            self.connect_password = connect_password 
        self.integration = integration 
        self.mtype = mtype 
        if secret is not APIHelper.SKIP:
            self.secret = secret 
        self.inactive = inactive 
        self.frozen = frozen 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        entity = dictionary.get("entity") if dictionary.get("entity") else None
        username = dictionary.get("username") if dictionary.get("username") else None
        integration = dictionary.get("integration") if dictionary.get("integration") else None
        mtype = dictionary.get("type") if dictionary.get("type") else None
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        name = dictionary.get("name") if dictionary.get("name") else APIHelper.SKIP
        description = dictionary.get("description") if dictionary.get("description") else APIHelper.SKIP
        password = dictionary.get("password") if dictionary.get("password") else APIHelper.SKIP
        connect_username = dictionary.get("connectUsername") if dictionary.get("connectUsername") else APIHelper.SKIP
        connect_password = dictionary.get("connectPassword") if dictionary.get("connectPassword") else APIHelper.SKIP
        secret = dictionary.get("secret") if dictionary.get("secret") else APIHelper.SKIP
        # Return an object of this model
        return cls(entity,
                   username,
                   integration,
                   mtype,
                   inactive,
                   frozen,
                   name,
                   description,
                   password,
                   connect_username,
                   connect_password,
                   secret)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.entity,
                                           type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.username,
                                            type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.integration,
                                            type_callable=lambda value: CredentialIntegrationEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.mtype,
                                            type_callable=lambda value: CredentialTypeEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.inactive,
                                            type_callable=lambda value: InactiveEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.frozen,
                                            type_callable=lambda value: FrozenEnum.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('entity'),
                                       type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('username'),
                                        type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('integration'),
                                        type_callable=lambda value: CredentialIntegrationEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('type'),
                                        type_callable=lambda value: CredentialTypeEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('inactive'),
                                        type_callable=lambda value: InactiveEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('frozen'),
                                        type_callable=lambda value: FrozenEnum.validate(value))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'entity={self.entity!r}, '
                f'name={(self.name if hasattr(self, "name") else None)!r}, '
                f'description={(self.description if hasattr(self, "description") else None)!r}, '
                f'username={self.username!r}, '
                f'password={(self.password if hasattr(self, "password") else None)!r}, '
                f'connect_username={(self.connect_username if hasattr(self, "connect_username") else None)!r}, '
                f'connect_password={(self.connect_password if hasattr(self, "connect_password") else None)!r}, '
                f'integration={self.integration!r}, '
                f'mtype={self.mtype!r}, '
                f'secret={(self.secret if hasattr(self, "secret") else None)!r}, '
                f'inactive={self.inactive!r}, '
                f'frozen={self.frozen!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'entity={self.entity!s}, '
                f'name={(self.name if hasattr(self, "name") else None)!s}, '
                f'description={(self.description if hasattr(self, "description") else None)!s}, '
                f'username={self.username!s}, '
                f'password={(self.password if hasattr(self, "password") else None)!s}, '
                f'connect_username={(self.connect_username if hasattr(self, "connect_username") else None)!s}, '
                f'connect_password={(self.connect_password if hasattr(self, "connect_password") else None)!s}, '
                f'integration={self.integration!s}, '
                f'mtype={self.mtype!s}, '
                f'secret={(self.secret if hasattr(self, "secret") else None)!s}, '
                f'inactive={self.inactive!s}, '
                f'frozen={self.frozen!s})')
