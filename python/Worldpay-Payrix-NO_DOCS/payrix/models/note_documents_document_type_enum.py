"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class NoteDocumentsDocumentTypeEnum(object):
    """Implementation of the 'noteDocumentsDocumentType' enum.

    Attributes:
        GENERAL: General use for unspecified type.
        PERSONALID: An identification document for a person.
        COMPANYID: An identification document for a company.
        VOIDCHECK: A voided check for bank account verification.
        BANKSTATEMENT: A bank statement.
        BANKLETTER: A bank letter for verification.
        CONTRACT: A contract for goods or services.
        TAXDOCUMENT: A tax document for verification.

    """

    _all_values = ["general", "personalId", "companyId", "voidCheck", "bankStatement", "bankLetter", "contract", "taxDocument"]
    GENERAL = "general"

    PERSONALID = "personalId"

    COMPANYID = "companyId"

    VOIDCHECK = "voidCheck"

    BANKSTATEMENT = "bankStatement"

    BANKLETTER = "bankLetter"

    CONTRACT = "contract"

    TAXDOCUMENT = "taxDocument"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
