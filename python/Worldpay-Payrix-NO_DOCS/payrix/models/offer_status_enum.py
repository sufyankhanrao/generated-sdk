# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class OfferStatusEnum(object):

    """Implementation of the 'OfferStatus' enum.

    Attributes:
        ACTIVE: Indicates that the cash advance application is active and the
            sub-merchant is currently repaying the cash advance.
        APPLIED: Indicates that the sub-merchant has applied for a cash
            advance and is awaiting approval from the vendor.
        APPROVED: Indicates that the sub-merchant's cash advance application
            has been approved by the vendor and the sub-merchant is expected
            to start repaying the cash advance.
        EXPIRED: Indicates that the cash advance application has expired and
            the sub-merchant is no longer eligible to receive the cash advance.

    """
    _all_values = ['ACTIVE', 'APPLIED', 'APPROVED', 'EXPIRED']
    ACTIVE = 'ACTIVE'

    APPLIED = 'APPLIED'

    APPROVED = 'APPROVED'

    EXPIRED = 'EXPIRED'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
