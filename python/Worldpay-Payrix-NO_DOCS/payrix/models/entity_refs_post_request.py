"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper
from payrix.models.default_4_enum import Default4Enum
from payrix.models.entity_platform_enum import (
    EntityPlatformEnum,
)
from payrix.models.entity_ref_stage_enum import (
    EntityRefStageEnum,
)
from payrix.models.frozen_enum import FrozenEnum
from payrix.models.inactive_enum import InactiveEnum
from payrix.models.staging_enum import StagingEnum


class EntityRefsPostRequest(object):
    """Implementation of the 'entityRefsPostRequest' model.

    Attributes:
        entity (str): The identifier of the Entity that owns this entityRefs
            resource.
        member (str): The identifier of the Member associated with this
            entityRefs resource. Default value is 'null'
        entity_route (str): The original 'entity_route' that routed the
            merchant onto the platform which then returned this reference
            information, if applicable.
        ref (str): The reference code itself.  This field is stored as a text
            string and must be between 0 and 50 characters long.
        stage (EntityRefStageEnum): An indicator showing what this terminalRef
            refers to.  <details> <summary>Valid Values</summary>  -
            `amexCharge` - **Amex Charge.** - `batch` - **Batch.** -
            `batchSuffix` - **Batch Suffix.** - `boarding` - **Boarding.** -
            `chain` - **The store chain ID for this Merchant.** - `chargeback`
            - **Chargeback.** - `companyDebit` - **Company Debit.** -
            `companyCredit` - **Company Credit.** - `create` - **Terminal
            Creation.** - `disbursement` - **Disbursement.** - `entity` -
            **The Entity ID.** - `batchFile` - **Batch File.** - `payoutFile`
            - **Payout File.** - `frontend` - **Frontend.** - `funding` -
            **The Payout funding ID for this Merchant.** - `member` - **The
            member ID for this Member.** - `merchant` - **The Merchant ID.** -
            `mid` - **The transaction Merchant ID (MID).** - `origId` -
            **Original ID.** - `store` - **The store ID for this Merchant.** -
            `software` - **Software.** - `terminal` - **Terminal.** -
            `displayName` - **Display Name.** - `domain` - **Domain.** -
            `expressCreds` - **Express Credentials.**  </details>
        staging (StagingEnum): Allows a merchant to transact under the Staging
            environment while boarding is in progress, and allows for
            configuration with valid values such as **Disabled** or
            **Enabled**.  <details> <summary>Valid Values</summary>  - `0` -
            **Disabled** - `1` - **Enabled**  </details>
        platform (EntityPlatformEnum): The processor that issued this
            terminalTxnRef.  <details> <summary>Valid Values</summary>  -
            `APPLE` - **APPLE** - `ELAVON` - **ELAVON** - `FIRSTDATA` -
            **FIRSTDATA** - `GOOGLE` - **GOOGLE** - `VANTIV` - **VANTIV** -
            `VCORE` - **VCORE** - `WELLSACH` - **WELLSACH** - `WELLSFARGO` -
            **WELLSFARGO** - `WFSINGLE` - **WFSINGLE** - `WORLDPAY` -
            **WORLDPAY** - `TDBANKCA` - **TDBANKCA**  </details>
        currency (CurrencyEnum): The currency of the entityRef. See <a
            href="https://www.iban.com/currency-codes"
            target="_blank">Currency codes</a>  for all valid values.
        funding_currency (FundingCurrencyEnum): The currency for which this
            entityRefs resource was funded on. See <a
            href="https://www.iban.com/currency-codes"
            target="_blank">Currency codes</a>  for all valid values.
        default (Default4Enum): Whether this entityRef is the default one.
            Default entityRefs will have priority when processing transactions
            with no MID or Platform set. <details> <summary>Valid
            Values</summary> - `0` - **Not default.** - `1` - **Default.**
            </details>
        fbo (str): The FBO from which this entity refers to.
        options (str): A string-JSON of options for this reference value.
        inactive (InactiveEnum): Whether this resource is marked as inactive.
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen.
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "entity": "entity",
        "member": "member",
        "ref": "ref",
        "stage": "stage",
        "staging": "staging",
        "platform": "platform",
        "default": "default",
        "inactive": "inactive",
        "frozen": "frozen",
        "entity_route": "entityRoute",
        "currency": "currency",
        "funding_currency": "fundingCurrency",
        "fbo": "fbo",
        "options": "options",
    }

    _optionals = [
        "entity_route",
        "currency",
        "funding_currency",
        "fbo",
        "options",
    ]

    _nullables = [
        "entity",
        "member",
        "entity_route",
        "currency",
        "funding_currency",
        "fbo",
        "options",
    ]

    def __init__(self,
                 entity=None,
                 member=None,
                 ref=None,
                 stage=None,
                 staging=0,
                 platform=None,
                 default=0,
                 inactive=0,
                 frozen=0,
                 entity_route=APIHelper.SKIP,
                 currency=APIHelper.SKIP,
                 funding_currency=APIHelper.SKIP,
                 fbo=APIHelper.SKIP,
                 options=APIHelper.SKIP):
        """Initialize a EntityRefsPostRequest instance."""
        # Initialize members of the class
        self.entity = entity
        self.member = member
        if entity_route is not APIHelper.SKIP:
            self.entity_route = entity_route
        self.ref = ref
        self.stage = stage
        self.staging = staging
        self.platform = platform
        if currency is not APIHelper.SKIP:
            self.currency = currency
        if funding_currency is not APIHelper.SKIP:
            self.funding_currency = funding_currency
        self.default = default
        if fbo is not APIHelper.SKIP:
            self.fbo = fbo
        if options is not APIHelper.SKIP:
            self.options = options
        self.inactive = inactive
        self.frozen = frozen

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        entity = dictionary.get("entity") if dictionary.get("entity") else None
        member = dictionary.get("member") if dictionary.get("member") else None
        ref = dictionary.get("ref") if dictionary.get("ref") else None
        stage = dictionary.get("stage") if dictionary.get("stage") else None
        staging = dictionary.get("staging") if dictionary.get("staging") else 0
        platform =\
            dictionary.get("platform")\
            if dictionary.get("platform") else None
        default = dictionary.get("default") if dictionary.get("default") else 0
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        entity_route =\
            dictionary.get("entityRoute")\
            if "entityRoute" in dictionary.keys() else APIHelper.SKIP
        currency =\
            dictionary.get("currency")\
            if "currency" in dictionary.keys() else APIHelper.SKIP
        funding_currency =\
            dictionary.get("fundingCurrency")\
            if "fundingCurrency" in dictionary.keys() else APIHelper.SKIP
        fbo =\
            dictionary.get("fbo")\
            if "fbo" in dictionary.keys() else APIHelper.SKIP
        options =\
            dictionary.get("options")\
            if "options" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(entity,
                   member,
                   ref,
                   stage,
                   staging,
                   platform,
                   default,
                   inactive,
                   frozen,
                   entity_route,
                   currency,
                   funding_currency,
                   fbo,
                   options)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                value=dictionary.entity,
                type_callable=lambda value: isinstance(value, str),
                is_value_nullable=True) \
                and APIHelper.is_valid_type(
                value=dictionary.member,
                type_callable=lambda value: isinstance(value, str),
                is_value_nullable=True) \
                and APIHelper.is_valid_type(
                value=dictionary.ref,
                type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(
                value=dictionary.stage,
                type_callable=lambda value: EntityRefStageEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.staging,
                type_callable=lambda value: StagingEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.platform,
                type_callable=lambda value: EntityPlatformEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.default,
                type_callable=lambda value: Default4Enum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.inactive,
                type_callable=lambda value: InactiveEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.frozen,
                type_callable=lambda value: FrozenEnum.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
            value=dictionary.get("entity"),
            type_callable=lambda value: isinstance(value, str),
            is_value_nullable=True) \
            and APIHelper.is_valid_type(
            value=dictionary.get("member"),
            type_callable=lambda value: isinstance(value, str),
            is_value_nullable=True) \
            and APIHelper.is_valid_type(
            value=dictionary.get("ref"),
            type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("stage"),
            type_callable=lambda value: EntityRefStageEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("staging"),
            type_callable=lambda value: StagingEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("platform"),
            type_callable=lambda value: EntityPlatformEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("default"),
            type_callable=lambda value: Default4Enum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("inactive"),
            type_callable=lambda value: InactiveEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("frozen"),
            type_callable=lambda value: FrozenEnum.validate(value))

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"entity={self.entity!r}, "
                f"member={self.member!r}, "
                f"entity_route={(self.entity_route
                     if hasattr(self, 'entity_route') else None)!r}, "
                f"ref={self.ref!r}, "
                f"stage={self.stage!r}, "
                f"staging={self.staging!r}, "
                f"platform={self.platform!r}, "
                f"currency={(self.currency
                     if hasattr(self, 'currency') else None)!r}, "
                f"funding_currency={(self.funding_currency
                     if hasattr(self, 'funding_currency') else None)!r}, "
                f"default={self.default!r}, "
                f"fbo={(self.fbo if hasattr(self, 'fbo') else None)!r}, "
                f"options={(self.options
                     if hasattr(self, 'options') else None)!r}, "
                f"inactive={self.inactive!r}, "
                f"frozen={self.frozen!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"entity={self.entity!s}, "
                f"member={self.member!s}, "
                f"entity_route={(self.entity_route
                     if hasattr(self, 'entity_route') else None)!s}, "
                f"ref={self.ref!s}, "
                f"stage={self.stage!s}, "
                f"staging={self.staging!s}, "
                f"platform={self.platform!s}, "
                f"currency={(self.currency
                     if hasattr(self, 'currency') else None)!s}, "
                f"funding_currency={(self.funding_currency
                     if hasattr(self, 'funding_currency') else None)!s}, "
                f"default={self.default!s}, "
                f"fbo={(self.fbo if hasattr(self, 'fbo') else None)!s}, "
                f"options={(self.options
                     if hasattr(self, 'options') else None)!s}, "
                f"inactive={self.inactive!s}, "
                f"frozen={self.frozen!s})")
