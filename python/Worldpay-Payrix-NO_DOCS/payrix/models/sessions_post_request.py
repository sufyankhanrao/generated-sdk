"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper


class SessionsPostRequest(object):
    """Implementation of the 'sessionsPostRequest' model.

    Attributes:
        login (str): The Login that owns this resource.
        public (SessionPublicEnum): Whether this Session should have access to
            only public resources.  All other resources are private.
            <details> <summary>Valid Values</summary>    - `0` - **Session can
            also access private resources.**    - `1` - **Session can only
            access public resources.**  </details>
        token (TokenEnum): Whether or not an AuthToken is required as part of
            the authentication when this Apikey is used.  <details>
            <summary>Valid Values</summary>  - `0` - **The authToken mechanism
            is disabled.** - `1` - **The authToken mechanism is required for
            'POST' and 'DELETE' requests.**  </details>
        mfa_authenticated (MfaAuthenticatedEnum): Whether or not the MFA is
            enabled.. <details> <summary>Valid Values</summary>  - `0` - **MFA
            is disabled.**  - `1` - **MFA is enabled.**  </details>
        inactive (InactiveEnum): Whether this resource is marked as inactive.
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen.
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "public": "public",
        "token": "token",
        "mfa_authenticated": "mfaAuthenticated",
        "inactive": "inactive",
        "frozen": "frozen",
        "login": "login",
    }

    _optionals = [
        "login",
    ]

    def __init__(self,
                 public=0,
                 token=0,
                 mfa_authenticated=0,
                 inactive=0,
                 frozen=0,
                 login=APIHelper.SKIP):
        """Initialize a SessionsPostRequest instance."""
        # Initialize members of the class
        if login is not APIHelper.SKIP:
            self.login = login
        self.public = public
        self.token = token
        self.mfa_authenticated = mfa_authenticated
        self.inactive = inactive
        self.frozen = frozen

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        public = dictionary.get("public") if dictionary.get("public") else 0
        token = dictionary.get("token") if dictionary.get("token") else 0
        mfa_authenticated =\
            dictionary.get("mfaAuthenticated")\
            if dictionary.get("mfaAuthenticated") else 0
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        login =\
            dictionary.get("login")\
            if dictionary.get("login") else APIHelper.SKIP
        # Return an object of this model
        return cls(public,
                   token,
                   mfa_authenticated,
                   inactive,
                   frozen,
                   login)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"login={(self.login if hasattr(self, 'login') else None)!r}, "
                f"public={self.public!r}, "
                f"token={self.token!r}, "
                f"mfa_authenticated={self.mfa_authenticated!r}, "
                f"inactive={self.inactive!r}, "
                f"frozen={self.frozen!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"login={(self.login if hasattr(self, 'login') else None)!s}, "
                f"public={self.public!s}, "
                f"token={self.token!s}, "
                f"mfa_authenticated={self.mfa_authenticated!s}, "
                f"inactive={self.inactive!s}, "
                f"frozen={self.frozen!s})")
