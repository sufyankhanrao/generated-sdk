# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper
from payrix.models.create_enum import CreateEnum
from payrix.models.delete_enum import DeleteEnum
from payrix.models.reference_enum import ReferenceEnum
from payrix.models.team_admin_enum import TeamAdminEnum
from payrix.models.team_login_read_enum import TeamLoginReadEnum
from payrix.models.update_enum import UpdateEnum


class TeamLoginPostRequest(object):

    """Implementation of the 'teamLoginPostRequest' model.

    Attributes:
        login (str): The Login that owns this resource.
        team (str): The identifier of the Team resource that the Login
            identified in the 'login' field should be marked as part of.
        create (CreateEnum): Create rights for this Login on this Team. 
            <details> <summary>Valid Values</summary>  - `0` - **None** - `1`
            - **Allow**  </details>
        read (TeamLoginReadEnum): Read rights for this Login on this Team. 
            <details> <summary>Valid Values</summary>  - `0` - **None** - `1`
            - **Allow**  </details>
        update (UpdateEnum): Update rights for this Login on this Team. 
            <details> <summary>Valid Values</summary>  - `0` - **None** - `1`
            - **Allow**  </details>
        delete (DeleteEnum): Delete rights for this Login on this Team. 
            <details> <summary>Valid Values</summary>  - `0` - **None** - `1`
            - **Allow**  </details>
        reference (ReferenceEnum): Reference use rights for this Login on this
            Team.  <details> <summary>Valid Values</summary>  - `0` - **None**
            - `1` - **Allow**  </details>
        team_admin (TeamAdminEnum): Team administration rights for this Login
            on this Team.  <details> <summary>Valid Values</summary>  - `0` -
            **None** - `1` - **Allow**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "login": 'login',
        "team": 'team',
        "create": 'create',
        "read": 'read',
        "update": 'update',
        "delete": 'delete',
        "reference": 'reference',
        "team_admin": 'teamAdmin'
    }

    def __init__(self,
                 login=None,
                 team=None,
                 create=0,
                 read=0,
                 update=0,
                 delete=0,
                 reference=0,
                 team_admin=0):
        """Constructor for the TeamLoginPostRequest class"""

        # Initialize members of the class
        self.login = login 
        self.team = team 
        self.create = create 
        self.read = read 
        self.update = update 
        self.delete = delete 
        self.reference = reference 
        self.team_admin = team_admin 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        login = dictionary.get("login") if dictionary.get("login") else None
        team = dictionary.get("team") if dictionary.get("team") else None
        create = dictionary.get("create") if dictionary.get("create") else 0
        read = dictionary.get("read") if dictionary.get("read") else 0
        update = dictionary.get("update") if dictionary.get("update") else 0
        delete = dictionary.get("delete") if dictionary.get("delete") else 0
        reference = dictionary.get("reference") if dictionary.get("reference") else 0
        team_admin = dictionary.get("teamAdmin") if dictionary.get("teamAdmin") else 0
        # Return an object of this model
        return cls(login,
                   team,
                   create,
                   read,
                   update,
                   delete,
                   reference,
                   team_admin)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.login,
                                           type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.team,
                                            type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(value=dictionary.create,
                                            type_callable=lambda value: CreateEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.read,
                                            type_callable=lambda value: TeamLoginReadEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.update,
                                            type_callable=lambda value: UpdateEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.delete,
                                            type_callable=lambda value: DeleteEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.reference,
                                            type_callable=lambda value: ReferenceEnum.validate(value)) \
                and APIHelper.is_valid_type(value=dictionary.team_admin,
                                            type_callable=lambda value: TeamAdminEnum.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('login'),
                                       type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('team'),
                                        type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(value=dictionary.get('create'),
                                        type_callable=lambda value: CreateEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('read'),
                                        type_callable=lambda value: TeamLoginReadEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('update'),
                                        type_callable=lambda value: UpdateEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('delete'),
                                        type_callable=lambda value: DeleteEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('reference'),
                                        type_callable=lambda value: ReferenceEnum.validate(value)) \
            and APIHelper.is_valid_type(value=dictionary.get('teamAdmin'),
                                        type_callable=lambda value: TeamAdminEnum.validate(value))

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'login={self.login!r}, '
                f'team={self.team!r}, '
                f'create={self.create!r}, '
                f'read={self.read!r}, '
                f'update={self.update!r}, '
                f'delete={self.delete!r}, '
                f'reference={self.reference!r}, '
                f'team_admin={self.team_admin!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'login={self.login!s}, '
                f'team={self.team!s}, '
                f'create={self.create!s}, '
                f'read={self.read!s}, '
                f'update={self.update!s}, '
                f'delete={self.delete!s}, '
                f'reference={self.reference!s}, '
                f'team_admin={self.team_admin!s})')
