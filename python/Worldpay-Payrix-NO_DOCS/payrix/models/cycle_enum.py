# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class CycleEnum(object):

    """Implementation of the 'Cycle' enum.

    Attributes:
        RETRIEVAL: Initial request from the issuer for more information on
            this Transaction.
        FIRST: First Chargeback from issuer for this Transaction.
        ARBITRATION: Arbitration is being sought for this Chargeback.
        REVERSAL: Chargeback was reversed.
        REPRESENTMENT: Merchant is being represented to the issuer with the
            Chargeback response posted.
        PREARBITRATION: Chargeback is no longer representable, Merchant must
            choose to accept or arbitrate with the card brand.
        ARBITRATIONLOST: Arbitration lost.
        ARBITRATIONSPLIT: Arbitration split.
        ARBITRATIONWON: Arbitration won.
        ISSUERACCEPTPREARBITRATION: Issuer accepted the pre-arbitration
            response.
        ISSUERDECLINEDPREARBITRATION: Issuer declined pre-arbitration.
        RESPONSETOISSUERPREARBITRATION: Response to issuer pre-arbitration.
        MERCHANTACCEPTEDPREARBITRATION: Merchant accepted the pre-arbitration
            response.
        MERCHANTDECLINEDPREARBITRATION: Merchant declined the pre-arbitration
            response.
        PRECOMPLIANCE: Pre-compliance.
        COMPLIANCE: Compliance.

    """
    _all_values = ['retrieval', 'first', 'arbitration', 'reversal', 'representment', 'preArbitration', 'arbitrationLost', 'arbitrationSplit', 'arbitrationWon', 'issuerAcceptPreArbitration', 'issuerDeclinedPreArbitration', 'responseToIssuerPreArbitration', 'merchantAcceptedPreArbitration', 'merchantDeclinedPreArbitration', 'preCompliance', 'compliance']
    RETRIEVAL = 'retrieval'

    FIRST = 'first'

    ARBITRATION = 'arbitration'

    REVERSAL = 'reversal'

    REPRESENTMENT = 'representment'

    PREARBITRATION = 'preArbitration'

    ARBITRATIONLOST = 'arbitrationLost'

    ARBITRATIONSPLIT = 'arbitrationSplit'

    ARBITRATIONWON = 'arbitrationWon'

    ISSUERACCEPTPREARBITRATION = 'issuerAcceptPreArbitration'

    ISSUERDECLINEDPREARBITRATION = 'issuerDeclinedPreArbitration'

    RESPONSETOISSUERPREARBITRATION = 'responseToIssuerPreArbitration'

    MERCHANTACCEPTEDPREARBITRATION = 'merchantAcceptedPreArbitration'

    MERCHANTDECLINEDPREARBITRATION = 'merchantDeclinedPreArbitration'

    PRECOMPLIANCE = 'preCompliance'

    COMPLIANCE = 'compliance'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
