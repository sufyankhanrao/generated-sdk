"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper


class ProfitShareRulesPostRequest(object):
    """Implementation of the 'profitShareRulesPostRequest' model.

    Attributes:
        profit_share (str): The identifier of the ProfitShare that this
            ProfitShare Rule applies to.
        name (str): The name of this ProfitShare Rule.  This field is stored
            as a text string and must be between 0 and 100 characters long.
        description (str): Description of the Profit Share Rules.
        mtype (ProfitShareRuleTypeEnum): The type of logic to apply with this
            ProfitShare Rule.  <details> <summary>Valid Values</summary>  -
            `less` - **The ProfitShare applies only if the entry amount is
            lower than the amount set in the 'value' field of the ProfitShare
            Rule.** - `equal` - **The ProfitShare applies only if the entry
            amount is exactly the same as the amount set in the 'value' field
            of the ProfitShare Rule.** - `notEqual` - **The ProfitShare
            applies only if the entry amount is not exactly equal to the
            amount set in the 'value' field of the ProfitShare Rule.** -
            `greater` - **The ProfitShare applies only if the entry amount is
            higher than the amount set in the 'value' field of the ProfitShare
            Rule.** - `event` - **The ProfitShare applies only if the entry
            event is exactly the same as the event set in the 'value' field of
            the ProfitShare Rule.** - `notEvent` - **The ProfitShare applies
            only if the entry event is not exactly equal to the event set in
            the 'value' field of the ProfitShare Rule.** - `fee` - **The
            ProfitShare applies only if the entry fee is exactly the same as
            the fee set in the 'value' field of the ProfitShare Rule.** -
            `notFee` - **The ProfitShare applies only if the entry fee is not
            exactly equal to the fee set in the 'value' field of the
            ProfitShare Rule.** - `fromentity` - **The ProfitShare applies
            only if the entry fromentity (or opposingEntryfromentity) is
            exactly the same as the entity set in the 'value' field of the
            ProfitShare Rule.** - `notFromentity` - **The ProfitShare applies
            only if the entry fromentity (or opposingEntryfromentity) is not
            exactly equal to the entity set in the 'value' field of the
            ProfitShare Rule.**  </details>
        value (str): The value to compare against when evaluating this
            ProfitShare Rule.
        grouping (str): A name for a group of rules to be applied in
            conjunction when evaluating this ProfitShare Rule.  When grouping
            is used the ProfitShare will be allowed to be processed if at
            least one of the rules are matched.
        inactive (InactiveEnum): Whether this resource is marked as inactive.
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen.
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "profit_share": "profitShare",
        "mtype": "type",
        "value": "value",
        "inactive": "inactive",
        "frozen": "frozen",
        "name": "name",
        "description": "description",
        "grouping": "grouping",
    }

    _optionals = [
        "name",
        "description",
        "grouping",
    ]

    def __init__(self,
                 profit_share=None,
                 mtype=None,
                 value=None,
                 inactive=0,
                 frozen=0,
                 name=APIHelper.SKIP,
                 description=APIHelper.SKIP,
                 grouping=APIHelper.SKIP):
        """Initialize a ProfitShareRulesPostRequest instance."""
        # Initialize members of the class
        self.profit_share = profit_share
        if name is not APIHelper.SKIP:
            self.name = name
        if description is not APIHelper.SKIP:
            self.description = description
        self.mtype = mtype
        self.value = value
        if grouping is not APIHelper.SKIP:
            self.grouping = grouping
        self.inactive = inactive
        self.frozen = frozen

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        profit_share =\
            dictionary.get("profitShare")\
            if dictionary.get("profitShare") else None
        mtype = dictionary.get("type") if dictionary.get("type") else None
        value = dictionary.get("value") if dictionary.get("value") else None
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        name =\
            dictionary.get("name")\
            if dictionary.get("name") else APIHelper.SKIP
        description =\
            dictionary.get("description")\
            if dictionary.get("description") else APIHelper.SKIP
        grouping =\
            dictionary.get("grouping")\
            if dictionary.get("grouping") else APIHelper.SKIP
        # Return an object of this model
        return cls(profit_share,
                   mtype,
                   value,
                   inactive,
                   frozen,
                   name,
                   description,
                   grouping)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"profit_share={self.profit_share!r}, "
                f"name={(self.name if hasattr(self, 'name') else None)!r}, "
                f"description={(self.description
                     if hasattr(self, 'description') else None)!r}, "
                f"mtype={self.mtype!r}, "
                f"value={self.value!r}, "
                f"grouping={(self.grouping
                     if hasattr(self, 'grouping') else None)!r}, "
                f"inactive={self.inactive!r}, "
                f"frozen={self.frozen!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"profit_share={self.profit_share!s}, "
                f"name={(self.name if hasattr(self, 'name') else None)!s}, "
                f"description={(self.description
                     if hasattr(self, 'description') else None)!s}, "
                f"mtype={self.mtype!s}, "
                f"value={self.value!s}, "
                f"grouping={(self.grouping
                     if hasattr(self, 'grouping') else None)!s}, "
                f"inactive={self.inactive!s}, "
                f"frozen={self.frozen!s})")
