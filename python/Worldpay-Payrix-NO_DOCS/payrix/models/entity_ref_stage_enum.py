"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class EntityRefStageEnum(object):
    """Implementation of the 'entityRefStage' enum.

    Attributes:
        AMEXCHARGE: Amex Charge.
        BATCH: Batch.
        BATCHSUFFIX: Batch Suffix.
        BOARDING: Boarding.
        CHAIN: The store chain ID for this Merchant.
        CHARGEBACK: Chargeback.
        COMPANYDEBIT: Company Debit.
        COMPANYCREDIT: Company Credit.
        CREATE: Terminal Creation.
        DISBURSEMENT: Disbursement.
        ENTITY: The Entity ID.
        BATCHFILE: Batch File.
        PAYOUTFILE: Payout File.
        FRONTEND: Frontend.
        FUNDING: The Payout funding ID for this Merchant.
        MEMBER: The member ID for this Member.
        MERCHANT: The Merchant ID.
        MID: The transaction Merchant ID (MID).
        ORIGID: Original ID.
        STORE: The store ID for this Merchant.
        SOFTWARE: Software.
        TERMINAL: Terminal.
        DISPLAYNAME: Display Name.
        DOMAIN: Domain.
        EXPRESSCREDS: Express Credentials.

    """

    _all_values = ["amexCharge", "batch", "batchSuffix", "boarding", "chain", "chargeback", "companyDebit", "companyCredit", "create", "disbursement", "entity", "batchFile", "payoutFile", "frontend", "funding", "member", "merchant", "mid", "origId", "store", "software", "terminal", "displayName", "domain", "expressCreds"]
    AMEXCHARGE = "amexCharge"

    BATCH = "batch"

    BATCHSUFFIX = "batchSuffix"

    BOARDING = "boarding"

    CHAIN = "chain"

    CHARGEBACK = "chargeback"

    COMPANYDEBIT = "companyDebit"

    COMPANYCREDIT = "companyCredit"

    CREATE = "create"

    DISBURSEMENT = "disbursement"

    ENTITY = "entity"

    BATCHFILE = "batchFile"

    PAYOUTFILE = "payoutFile"

    FRONTEND = "frontend"

    FUNDING = "funding"

    MEMBER = "member"

    MERCHANT = "merchant"

    MID = "mid"

    ORIGID = "origId"

    STORE = "store"

    SOFTWARE = "software"

    TERMINAL = "terminal"

    DISPLAYNAME = "displayName"

    DOMAIN = "domain"

    EXPRESSCREDS = "expressCreds"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
