# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class TargetEnum(object):

    """Implementation of the 'Target' enum.

    Attributes:
        ACTIVATION: Terminal activation.
        AUTH: Apply this decision during transaction authorization.
        POSTAUTH: Immediately after authorization.
        CAPTURE: Apply this decision during transaction capture.
        REFUND: Apply this decision when processing a refund.
        CREATEENTITY: Process decisions during entity creation.
        UNDERWRITING: Underwriting. Check the members before they are boarded.
        PREBOARD: Preboard. Check the Merchant before they are boarded.
        POSTBOARD: Check the Merchant after they are boarded.
        TXN: Check the Merchant when they process a Transaction.
        TXNVOLUME: Check the Merchant when their transaction volume hits a
            certain amount.
        PAYOUT: Check the Merchant when a Payout occurs.
        PAYOUTVOLUME: Check the Merchant when the volume of Payouts to the
            Merchant hits a certain amount.

    """
    _all_values = ['activation', 'auth', 'postauth', 'capture', 'refund', 'createEntity', 'underwriting', 'preboard', 'postboard', 'txn', 'txnVolume', 'payout', 'payoutVolume']
    ACTIVATION = 'activation'

    AUTH = 'auth'

    POSTAUTH = 'postauth'

    CAPTURE = 'capture'

    REFUND = 'refund'

    CREATEENTITY = 'createEntity'

    UNDERWRITING = 'underwriting'

    PREBOARD = 'preboard'

    POSTBOARD = 'postboard'

    TXN = 'txn'

    TXNVOLUME = 'txnVolume'

    PAYOUT = 'payout'

    PAYOUTVOLUME = 'payoutVolume'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
