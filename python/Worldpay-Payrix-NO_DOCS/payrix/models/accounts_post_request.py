"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper
from payrix.models.account_primary_enum import (
    AccountPrimaryEnum,
)
from payrix.models.account_status_enum import (
    AccountStatusEnum,
)
from payrix.models.account_type_enum import (
    AccountTypeEnum,
)
from payrix.models.accounts_account import AccountsAccount
from payrix.models.currency_enum import CurrencyEnum
from payrix.models.payout_post_request import (
    PayoutPostRequest,
)


class AccountsPostRequest(object):
    """Implementation of the 'accountsPostRequest' model.

    A request body for creating new bank account information for one or more
    entities.

    Attributes:
        entity (str): The identifier of the Entity associated with this
            Account.
        account (AccountsAccount): An object representing details of the
            Account, including the type of Account (method), Account number,
            and routing code.
        name (str): A client-supplied name for this bank account.  This field
            is stored as a string and must be between 0 and 100 characters
            long.
        description (str): A client-supplied description for this bank
            account.  This field is stored as a string and must be between 0
            and 100 characters long.
        primary (AccountPrimaryEnum): Indicates whether the Account is the
            primary Account for the associated Entity.  Only one Account
            associated with each Entity can be the primary Account.  <details>
            <summary>Valid Values</summary>  - `0` - **Not primary** - `1` -
            **Primary**  </details>
        mtype (AccountTypeEnum): The type of financial account: debit, credit,
            or both.  <details> <summary>Valid Values</summary>  - `all` -
            **Debit/checking and credit** - `credit` - **Credit only** -
            `debit` - **Debit/checking only**  </details>
        status (AccountStatusEnum): The status of the Account.  <details>
            <summary>Valid Values</summary>  - `0` - **Not Ready.** - `1` -
            **Ready.** - `2` - **Challenged.** - `3` - **Verified.** - `4` -
            **Manual.**  </details>
        expiration (str): The expiration date of the related debit account.
            The date is specified as a four-digit string in MMYY format, for
            example, `0118` for January 2018.
        currency (CurrencyEnum): The currency of this Account. The default is
            `USD`. See <a href="https://www.iban.com/currency-codes"
            target="_blank">Currency codes</a>  for all valid values.
        public_token (str): The token received from the Plaid account
            connection process.
        mask (str): The account number mask, showing the last four digits of
            the account number.
        inactive (InactiveEnum): Whether this resource is marked as inactive.
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen.
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>
        payouts (List[PayoutPostRequest]): The model property of type
            List[PayoutPostRequest].

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "entity": "entity",
        "account": "account",
        "primary": "primary",
        "mtype": "type",
        "status": "status",
        "currency": "currency",
        "name": "name",
        "description": "description",
        "expiration": "expiration",
        "public_token": "publicToken",
        "mask": "mask",
        "inactive": "inactive",
        "frozen": "frozen",
        "payouts": "payouts",
    }

    _optionals = [
        "name",
        "description",
        "expiration",
        "public_token",
        "mask",
        "inactive",
        "frozen",
        "payouts",
    ]

    _nullables = [
        "currency",
    ]

    def __init__(self,
                 entity=None,
                 account=None,
                 primary=None,
                 mtype="all",
                 status=0,
                 currency=None,
                 name=APIHelper.SKIP,
                 description=APIHelper.SKIP,
                 expiration=APIHelper.SKIP,
                 public_token=APIHelper.SKIP,
                 mask=APIHelper.SKIP,
                 inactive=0,
                 frozen=0,
                 payouts=APIHelper.SKIP):
        """Initialize a AccountsPostRequest instance."""
        # Initialize members of the class
        self.entity = entity
        self.account = account
        if name is not APIHelper.SKIP:
            self.name = name
        if description is not APIHelper.SKIP:
            self.description = description
        self.primary = primary
        self.mtype = mtype
        self.status = status
        if expiration is not APIHelper.SKIP:
            self.expiration = expiration
        self.currency = currency
        if public_token is not APIHelper.SKIP:
            self.public_token = public_token
        if mask is not APIHelper.SKIP:
            self.mask = mask
        self.inactive = inactive
        self.frozen = frozen
        if payouts is not APIHelper.SKIP:
            self.payouts = payouts

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        entity = dictionary.get("entity") if dictionary.get("entity") else None
        account = AccountsAccount.from_dictionary(
            dictionary.get("account"))\
            if dictionary.get("account") else None
        primary = dictionary.get("primary") if dictionary.get("primary") else None
        mtype = dictionary.get("type") if dictionary.get("type") else "all"
        status = dictionary.get("status") if dictionary.get("status") else 0
        currency =\
            dictionary.get("currency")\
            if dictionary.get("currency") else None
        name =\
            dictionary.get("name")\
            if dictionary.get("name") else APIHelper.SKIP
        description =\
            dictionary.get("description")\
            if dictionary.get("description") else APIHelper.SKIP
        expiration =\
            dictionary.get("expiration")\
            if dictionary.get("expiration") else APIHelper.SKIP
        public_token =\
            dictionary.get("publicToken")\
            if dictionary.get("publicToken") else APIHelper.SKIP
        mask =\
            dictionary.get("mask")\
            if dictionary.get("mask") else APIHelper.SKIP
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        payouts = None
        if dictionary.get("payouts") is not None:
            payouts = [
                PayoutPostRequest.from_dictionary(x)
                    for x in dictionary.get("payouts")
            ]
        else:
            payouts = APIHelper.SKIP
        # Return an object of this model
        return cls(entity,
                   account,
                   primary,
                   mtype,
                   status,
                   currency,
                   name,
                   description,
                   expiration,
                   public_token,
                   mask,
                   inactive,
                   frozen,
                   payouts)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                value=dictionary.entity,
                type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(
                value=dictionary.account,
                type_callable=lambda value: AccountsAccount.validate(value),
                is_model_dict=True) \
                and APIHelper.is_valid_type(
                value=dictionary.primary,
                type_callable=lambda value: AccountPrimaryEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.mtype,
                type_callable=lambda value: AccountTypeEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.status,
                type_callable=lambda value: AccountStatusEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.currency,
                type_callable=lambda value: CurrencyEnum.validate(value),
                is_value_nullable=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
            value=dictionary.get("entity"),
            type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("account"),
            type_callable=lambda value: AccountsAccount.validate(value),
            is_model_dict=True) \
            and APIHelper.is_valid_type(
            value=dictionary.get("primary"),
            type_callable=lambda value: AccountPrimaryEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("type"),
            type_callable=lambda value: AccountTypeEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("status"),
            type_callable=lambda value: AccountStatusEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("currency"),
            type_callable=lambda value: CurrencyEnum.validate(value),
            is_value_nullable=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"entity={self.entity!r}, "
                f"account={self.account!r}, "
                f"name={(self.name if hasattr(self, 'name') else None)!r}, "
                f"description={(self.description
                     if hasattr(self, 'description') else None)!r}, "
                f"primary={self.primary!r}, "
                f"mtype={self.mtype!r}, "
                f"status={self.status!r}, "
                f"expiration={(self.expiration
                     if hasattr(self, 'expiration') else None)!r}, "
                f"currency={self.currency!r}, "
                f"public_token={(self.public_token
                     if hasattr(self, 'public_token') else None)!r}, "
                f"mask={(self.mask if hasattr(self, 'mask') else None)!r}, "
                f"inactive={(self.inactive
                     if hasattr(self, 'inactive') else None)!r}, "
                f"frozen={(self.frozen if hasattr(self, 'frozen') else None)!r}, "
                f"payouts={(self.payouts
                     if hasattr(self, 'payouts') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"entity={self.entity!s}, "
                f"account={self.account!s}, "
                f"name={(self.name if hasattr(self, 'name') else None)!s}, "
                f"description={(self.description
                     if hasattr(self, 'description') else None)!s}, "
                f"primary={self.primary!s}, "
                f"mtype={self.mtype!s}, "
                f"status={self.status!s}, "
                f"expiration={(self.expiration
                     if hasattr(self, 'expiration') else None)!s}, "
                f"currency={self.currency!s}, "
                f"public_token={(self.public_token
                     if hasattr(self, 'public_token') else None)!s}, "
                f"mask={(self.mask if hasattr(self, 'mask') else None)!s}, "
                f"inactive={(self.inactive
                     if hasattr(self, 'inactive') else None)!s}, "
                f"frozen={(self.frozen if hasattr(self, 'frozen') else None)!s}, "
                f"payouts={(self.payouts
                     if hasattr(self, 'payouts') else None)!s})")
