"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper
from payrix.models.batch_platform_enum import (
    BatchPlatformEnum,
)
from payrix.models.batch_status_enum import (
    BatchStatusEnum,
)
from payrix.models.frozen_enum import FrozenEnum
from payrix.models.inactive_enum import InactiveEnum


class BatchesPostRequest(object):
    """Implementation of the 'batchesPostRequest' model.

    Attributes:
        merchant (str): The identifier of the Merchant that is associated with
            this Batch.
        platform (BatchPlatformEnum): The platform used to process this Batch.
            <details> <summary>Valid Values</summary>  - `APPLE` - **Apple
            Payment Processor** - `ELAVON` - **ELAVON Payment Processor** -
            `FIRSTDATA` - **FirstData Payment Processor** - `GOOGLE` -
            **Google Payment Processor** - `VANTIV` - **Worldpay / Vantiv
            eComm Payment Processor (VAP)** - `VCORE` - **Worldpay / Vantiv
            Core Payment Processor** - `WELLSACH` - **Wells Fargo Merchant
            Services Payment Processor (ACH)** - `WELLSFARGO` - **Wells Fargo
            Merchant Services Payment Processor** - `WFSINGLE` - **Wells Fargo
            Single Payment Processor** - `WORLDPAY` - **Worldpay Payment
            Processor** - `TDBANKCA` - **TD Bank Canada Payment Processor**
            </details>
        status (BatchStatusEnum): The current status of this Batch.  <details>
            <summary>Valid Values</summary>  - `open`  - **This Batch can
            accept more Transactions.** - `closed` - **This Batch is closed to
            new Transactions and is ready to be sent to the processor.**
            </details>
        ref (str): The reference code of the batch.  This field is
            automatically generated and stored as a text string and must be
            between 0 and 50 characters long.
        client_ref (str): The merchant's reference code of the batch.  This
            field is stored as a text string and must be between 0 and 50
            characters long.
        processing_id (str): Internal ID set for processing.
        inactive (InactiveEnum): Whether this resource is marked as inactive.
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen.
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "merchant": "merchant",
        "platform": "platform",
        "status": "status",
        "inactive": "inactive",
        "frozen": "frozen",
        "ref": "ref",
        "client_ref": "clientRef",
        "processing_id": "processingId",
    }

    _optionals = [
        "ref",
        "client_ref",
        "processing_id",
    ]

    def __init__(self,
                 merchant=None,
                 platform=None,
                 status="open",
                 inactive=0,
                 frozen=0,
                 ref=APIHelper.SKIP,
                 client_ref=APIHelper.SKIP,
                 processing_id=APIHelper.SKIP):
        """Initialize a BatchesPostRequest instance."""
        # Initialize members of the class
        self.merchant = merchant
        self.platform = platform
        self.status = status
        if ref is not APIHelper.SKIP:
            self.ref = ref
        if client_ref is not APIHelper.SKIP:
            self.client_ref = client_ref
        if processing_id is not APIHelper.SKIP:
            self.processing_id = processing_id
        self.inactive = inactive
        self.frozen = frozen

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        merchant =\
            dictionary.get("merchant")\
            if dictionary.get("merchant") else None
        platform =\
            dictionary.get("platform")\
            if dictionary.get("platform") else None
        status = dictionary.get("status") if dictionary.get("status") else "open"
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        ref = dictionary.get("ref") if dictionary.get("ref") else APIHelper.SKIP
        client_ref =\
            dictionary.get("clientRef")\
            if dictionary.get("clientRef") else APIHelper.SKIP
        processing_id =\
            dictionary.get("processingId")\
            if dictionary.get("processingId") else APIHelper.SKIP
        # Return an object of this model
        return cls(merchant,
                   platform,
                   status,
                   inactive,
                   frozen,
                   ref,
                   client_ref,
                   processing_id)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                value=dictionary.merchant,
                type_callable=lambda value: isinstance(value, str)) \
                and APIHelper.is_valid_type(
                value=dictionary.platform,
                type_callable=lambda value: BatchPlatformEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.status,
                type_callable=lambda value: BatchStatusEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.inactive,
                type_callable=lambda value: InactiveEnum.validate(value)) \
                and APIHelper.is_valid_type(
                value=dictionary.frozen,
                type_callable=lambda value: FrozenEnum.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
            value=dictionary.get("merchant"),
            type_callable=lambda value: isinstance(value, str)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("platform"),
            type_callable=lambda value: BatchPlatformEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("status"),
            type_callable=lambda value: BatchStatusEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("inactive"),
            type_callable=lambda value: InactiveEnum.validate(value)) \
            and APIHelper.is_valid_type(
            value=dictionary.get("frozen"),
            type_callable=lambda value: FrozenEnum.validate(value))

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"merchant={self.merchant!r}, "
                f"platform={self.platform!r}, "
                f"status={self.status!r}, "
                f"ref={(self.ref if hasattr(self, 'ref') else None)!r}, "
                f"client_ref={(self.client_ref
                     if hasattr(self, 'client_ref') else None)!r}, "
                f"processing_id={(self.processing_id
                     if hasattr(self, 'processing_id') else None)!r}, "
                f"inactive={self.inactive!r}, "
                f"frozen={self.frozen!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"merchant={self.merchant!s}, "
                f"platform={self.platform!s}, "
                f"status={self.status!s}, "
                f"ref={(self.ref if hasattr(self, 'ref') else None)!s}, "
                f"client_ref={(self.client_ref
                     if hasattr(self, 'client_ref') else None)!s}, "
                f"processing_id={(self.processing_id
                     if hasattr(self, 'processing_id') else None)!s}, "
                f"inactive={self.inactive!s}, "
                f"frozen={self.frozen!s})")
