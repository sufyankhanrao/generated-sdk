"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class SecretsPlatformEnum(object):
    """Implementation of the 'secretsPlatform' enum.

    Attributes:
        APPLE: Apple Payment Processor.
        ELAVON: ELAVON Payment Processor.
        FIRSTDATA: FirstData Payment Processor.
        GOOGLE: Google Payment Processor.
        VANTIV: WorldPay / Vantiv eComm Payment Processor (VAP).
        VCORE: WorldPay / Vantiv Core Payment Processor.
        WELLSACH: Wells Fargo Merchant Services Payment Processor (ACH).
        WELLSFARGO: Wells Fargo Merchant Services Payment Processor.
        WFSINGLE: Wells Fargo Single Payment Processor.
        WORLDPAY: Worldpay Payment Processor.
        TDBANKCA: TdBankCA Payment Processor.

    """

    _all_values = ["APPLE", "ELAVON", "FIRSTDATA", "GOOGLE", "VANTIV", "VCORE", "WELLSACH", "WELLSFARGO", "WFSINGLE", "WORLDPAY", "TDBANKCA"]
    APPLE = "APPLE"

    ELAVON = "ELAVON"

    FIRSTDATA = "FIRSTDATA"

    GOOGLE = "GOOGLE"

    VANTIV = "VANTIV"

    VCORE = "VCORE"

    WELLSACH = "WELLSACH"

    WELLSFARGO = "WELLSFARGO"

    WFSINGLE = "WFSINGLE"

    WORLDPAY = "WORLDPAY"

    TDBANKCA = "TDBANKCA"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
