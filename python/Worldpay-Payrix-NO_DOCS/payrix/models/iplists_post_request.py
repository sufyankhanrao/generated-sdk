# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from payrix.api_helper import APIHelper


class IplistsPostRequest(object):

    """Implementation of the 'iplistsPostRequest' model.

    Attributes:
        login (str): The Login that owns this resource.
        version (VersionEnum): Whether this IP List contains IPv4 addresses
            (for example, 198.51.100.113) or IPv6 addresses (for example,
            2001:0db8:85a3:0000:0000:8a2e:0370:7334).  You can only use one
            type in each IP List resource.  <details> <summary>Valid
            Values</summary>  - `4` - **IPv4 addresses.** (Example:
            198.51.100.113) - `6` - **IPv6 addresses.** (Example:
            2001:0db8:85a3:0000:0000:8a2e:0370:7334)  </details>
        mtype (IplistTypeEnum): The type of this IP List.  <details>
            <summary>Valid Values</summary>  - `0` - **Blacklisted IP address
            range.** - `1` - **Whitelisted IP address range.**  </details>
        start (str): The lowest IP address that should be included in this IP
            List.  The valid data type for this field depends on whether the
            IP list is set to be an IPv4 or IPv6 list in the 'type' field. 
            For an IPv4 list, only IPv4 addresses such as 198.51.100.113 are
            permitted. For an IPv6 list, only IPv6 addresses such as
            2001:0db8:85a3:0000:0000:8a2e:0370:7334 are permitted.
        finish (str): The highest IP address that should be included in this
            IP List.  The valid values for this field depend on whether the IP
            list is set to be an IPv4 or IPv6 list in the 'type' field.  For
            an IPv4 list, only IPv4 addresses, such as '198.51.100.113', are
            permitted. For an IPv6 list, only IPv6 addresses, such as
            '2001:0db8:85a3:0000:0000:8a2e:0370:7334' are permitted.
        inactive (InactiveEnum): Whether this resource is marked as inactive. 
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen. 
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "login": 'login',
        "version": 'version',
        "mtype": 'type',
        "start": 'start',
        "inactive": 'inactive',
        "frozen": 'frozen',
        "finish": 'finish'
    }

    _optionals = [
        'finish',
    ]

    def __init__(self,
                 login=None,
                 version=4,
                 mtype=0,
                 start=None,
                 inactive=0,
                 frozen=0,
                 finish=APIHelper.SKIP):
        """Constructor for the IplistsPostRequest class"""

        # Initialize members of the class
        self.login = login 
        self.version = version 
        self.mtype = mtype 
        self.start = start 
        if finish is not APIHelper.SKIP:
            self.finish = finish 
        self.inactive = inactive 
        self.frozen = frozen 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        login = dictionary.get("login") if dictionary.get("login") else None
        version = dictionary.get("version") if dictionary.get("version") else 4
        mtype = dictionary.get("type") if dictionary.get("type") else 0
        start = dictionary.get("start") if dictionary.get("start") else None
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        finish = dictionary.get("finish") if dictionary.get("finish") else APIHelper.SKIP
        # Return an object of this model
        return cls(login,
                   version,
                   mtype,
                   start,
                   inactive,
                   frozen,
                   finish)

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'login={self.login!r}, '
                f'version={self.version!r}, '
                f'mtype={self.mtype!r}, '
                f'start={self.start!r}, '
                f'finish={(self.finish if hasattr(self, "finish") else None)!r}, '
                f'inactive={self.inactive!r}, '
                f'frozen={self.frozen!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'login={self.login!s}, '
                f'version={self.version!s}, '
                f'mtype={self.mtype!s}, '
                f'start={self.start!s}, '
                f'finish={(self.finish if hasattr(self, "finish") else None)!s}, '
                f'inactive={self.inactive!s}, '
                f'frozen={self.frozen!s})')
