# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class EntityCheckStageEnum(object):

    """Implementation of the 'entityCheckStage' enum.

    Attributes:
        CREATEENTITY: Merchant created. No Signup Form submitted.
        UNDERWRITING: Risk/Underwriting Review. Merchant Signup Form submitted.
        PREBOARD: Check the Merchant before they are boarded.
        POSTBOARD: Check the Merchant after they are boarded.
        TRANSACTION: Check the Merchant when they process a Transaction.
        TRANSACTIONVOLUME: Check the Merchant when their transaction volume
            hits a certain amount.
        PAYOUT: Check the Merchant when a Payout occurs.
        PAYOUTVOLUME: Check the Merchant when the volume of Payouts to the
            Merchant hits a certain amount.

    """
    _all_values = ['createEntity', 'underwriting', 'preboard', 'postboard', 'txn', 'txnVolume', 'payout', 'payoutVolume']
    CREATEENTITY = 'createEntity'

    UNDERWRITING = 'underwriting'

    PREBOARD = 'preboard'

    POSTBOARD = 'postboard'

    TRANSACTION = 'txn'

    TRANSACTIONVOLUME = 'txnVolume'

    PAYOUT = 'payout'

    PAYOUTVOLUME = 'payoutVolume'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
