"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from payrix.api_helper import APIHelper


class ApiKeysPostRequest(object):
    """Implementation of the 'apiKeysPostRequest' model.

    Attributes:
        login (str): The login under which to perform this action. If you do
            not supply a login in this field, then the API defaults to the
            login that is currently authenticated.
        name (str): The name for the key. An apikey may represent a private
            (secret) key code that is used internally (server-side) by the
            application authenticating to the API.   A private key should
            never be exposed to external/untrusted entities and should be
            immediately deactivated if compromised. All API resources and
            actions can be accessed/performed with a private apikey.
        description (str): Description of ApiKeys.
        public (ApiKeyPublicEnum): Whether this API key should have access to
            only public resources.  All other resources are private. <details>
            <summary>Valid Values</summary>  - `0` - **The key can also access
            private resources.**  - `1` - **The key can only access public
            resources.**  </details>
        token (TokenEnum): Whether or not an AuthToken is required as part of
            the authentication when this Apikey is used.  <details>
            <summary>Valid Values</summary>  - `0` - **The authToken mechanism
            is disabled.** - `1` - **The authToken mechanism is required for
            'POST' and 'DELETE' requests.**  </details>
        inactive (InactiveEnum): Whether this resource is marked as inactive.
            <details> <summary>Valid Values</summary>  - `0` - **Active** -
            `1` - **Inactive**  </details>
        frozen (FrozenEnum): Whether this resource is marked as frozen.
            <details> <summary>Valid Values</summary>  - `0` - **Not Frozen**
            - `1` - **Frozen**  </details>

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "login": "login",
        "public": "public",
        "token": "token",
        "inactive": "inactive",
        "frozen": "frozen",
        "name": "name",
        "description": "description",
    }

    _optionals = [
        "name",
        "description",
    ]

    def __init__(self,
                 login=None,
                 public=0,
                 token=0,
                 inactive=0,
                 frozen=0,
                 name=APIHelper.SKIP,
                 description=APIHelper.SKIP):
        """Initialize a ApiKeysPostRequest instance."""
        # Initialize members of the class
        self.login = login
        if name is not APIHelper.SKIP:
            self.name = name
        if description is not APIHelper.SKIP:
            self.description = description
        self.public = public
        self.token = token
        self.inactive = inactive
        self.frozen = frozen

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        login = dictionary.get("login") if dictionary.get("login") else None
        public = dictionary.get("public") if dictionary.get("public") else 0
        token = dictionary.get("token") if dictionary.get("token") else 0
        inactive = dictionary.get("inactive") if dictionary.get("inactive") else 0
        frozen = dictionary.get("frozen") if dictionary.get("frozen") else 0
        name =\
            dictionary.get("name")\
            if dictionary.get("name") else APIHelper.SKIP
        description =\
            dictionary.get("description")\
            if dictionary.get("description") else APIHelper.SKIP
        # Return an object of this model
        return cls(login,
                   public,
                   token,
                   inactive,
                   frozen,
                   name,
                   description)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"login={self.login!r}, "
                f"name={(self.name if hasattr(self, 'name') else None)!r}, "
                f"description={(self.description
                     if hasattr(self, 'description') else None)!r}, "
                f"public={self.public!r}, "
                f"token={self.token!r}, "
                f"inactive={self.inactive!r}, "
                f"frozen={self.frozen!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"login={self.login!s}, "
                f"name={(self.name if hasattr(self, 'name') else None)!s}, "
                f"description={(self.description
                     if hasattr(self, 'description') else None)!s}, "
                f"public={self.public!s}, "
                f"token={self.token!s}, "
                f"inactive={self.inactive!s}, "
                f"frozen={self.frozen!s})")
