"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class InvoiceParameterTypeEnum(object):
    """Implementation of the 'invoiceParameterType' enum.

    Attributes:
        APIKEY: The apikey that will be used for the invoice.
        FROMEMAIL: The email the invoice will be sent from.
        REPLYTOEMAIL: The reply-to email address for the invoice.
        DBA: Name of the company that will be shown on the invoice.
        ADDRESS1: Address1 shown on the invoice.
        ADDRESS2: Address2 shown on the invoice.
        CITY: City shown on the invoice.
        STATE: State shown on the invoice.
        ZIP: Zip code shown on the invoice.
        COUNTRY: Country shown on the invoice.
        PHONE: Phone number shown on the invoice.
        WEBSITE: Website address shown on the invoice.
        RECEIPTTITLE: The title when the invoice is sent.
        RECEIPTMESSAGE: The message when the invoice is sent.
        BILLINGREQUIRED: Whether the billing address will be required from the
            customer.
        SHIPPINGREQUIRED: Whether the shipping address will be required from
            the customer.
        ECHECKAUTHORIZATION: Authorization for ECheck payments.

    """

    _all_values = ["apikey", "fromEmail", "replyToEmail", "dba", "address1", "address2", "city", "state", "zip", "country", "phone", "website", "receiptTitle", "receiptMessage", "billingRequired", "shippingRequired", "eCheckAuthorization"]
    APIKEY = "apikey"

    FROMEMAIL = "fromEmail"

    REPLYTOEMAIL = "replyToEmail"

    DBA = "dba"

    ADDRESS1 = "address1"

    ADDRESS2 = "address2"

    CITY = "city"

    STATE = "state"

    ZIP = "zip"

    COUNTRY = "country"

    PHONE = "phone"

    WEBSITE = "website"

    RECEIPTTITLE = "receiptTitle"

    RECEIPTMESSAGE = "receiptMessage"

    BILLINGREQUIRED = "billingRequired"

    SHIPPINGREQUIRED = "shippingRequired"

    ECHECKAUTHORIZATION = "eCheckAuthorization"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
