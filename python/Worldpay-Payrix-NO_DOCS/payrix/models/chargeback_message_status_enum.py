# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class ChargebackMessageStatusEnum(object):

    """Implementation of the 'chargebackMessageStatus' enum.

    Attributes:
        REQUESTED: The chargeback has been requested.
        PROCESSING: The chargeback is currently being processed.
        FAILED: The chargeback process has failed.
        DENIED: The chargeback has been denied.
        PROCESSED: The chargeback has been successfully processed.

    """
    _all_values = ['requested', 'processing', 'failed', 'denied', 'processed']
    REQUESTED = 'requested'

    PROCESSING = 'processing'

    FAILED = 'failed'

    DENIED = 'denied'

    PROCESSED = 'processed'

    @classmethod
    def validate(cls, value):
        """Validates value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values
   
    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
