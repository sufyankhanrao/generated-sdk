"""payrix.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class MerchantStatusEnum(object):
    """Implementation of the 'merchantStatus' enum.

    Attributes:
        NOTREADY: Occurs when a new Merchant is created. Unable to process
            payments.
        READY: New Merchant submitted for underwriting approval after
            submitting a signup form.
        BOARDED: Merchant has been approved and boarded to the platform.
            Payment processing now available.
        MANUAL: Set internally by platform underwriting. New Merchant is
            pending manual verification.
        CLOSED: Set internally by platform underwriting. New Merchant was
            declined and cannot access the platform.
        INCOMPLETE: Set by user boarding the merchant. Can be manually set to
            "save" an incomplete Merchant boarding request for a later time.
        PENDING: New Merchant was submitted for boarding, platform access
            pending review.

    """

    _all_values = [0, 1, 2, 3, 4, 5, 6]
    NOTREADY = 0

    READY = 1

    BOARDED = 2

    MANUAL = 3

    CLOSED = 4

    INCOMPLETE = 5

    PENDING = 6

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
