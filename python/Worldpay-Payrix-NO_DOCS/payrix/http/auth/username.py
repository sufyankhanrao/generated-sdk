# -*- coding: utf-8 -*-

"""
payrix

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""

import os
from apimatic_core.authentication.header_auth import HeaderAuth


class Username(HeaderAuth):

    @property
    def error_message(self):
        """Display error message on occurrence of authentication failure
        in Username

        """
        return "Username: USERNAME is undefined."

    def __init__(self, username_credentials):
        auth_params = {}
        if username_credentials is not None \
                and username_credentials.username is not None:
            auth_params["USERNAME"] = username_credentials.username
        super().__init__(auth_params=auth_params)


class UsernameCredentials:

    @property
    def username(self):
        return self._username

    def __init__(self, username):
        if username is None:
            raise ValueError('username cannot be None')
        self._username = username

    def clone_with(self, username=None):
        return UsernameCredentials(username or self.username)

    @classmethod
    def from_environment(cls):
        username = os.getenv('USERNAME_USERNAME', None)

        if username is None:
            return None

        return cls(
            username=username
        )