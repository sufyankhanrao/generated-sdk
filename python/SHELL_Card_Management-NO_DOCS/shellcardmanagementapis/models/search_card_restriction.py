"""shell_card_management_ap_is.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

from shellcardmanagementapis.api_helper import (
    APIHelper,
)


class SearchCardRestriction(object):
    """Implementation of the 'SearchCardRestriction' model.

    Attributes:
        card_id (int): Unique Card Id   Optional if PAN is given, else
            mandatory.
        pan (str): Card PAN.  Optional if CardId is given, else mandatory.
            Note: PAN is ignored if CardId is given.  Example:
            7002051006629890645

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "card_id": "CardId",
        "pan": "PAN",
    }

    _optionals = [
        "card_id",
        "pan",
    ]

    _nullables = [
        "card_id",
        "pan",
    ]

    def __init__(self,
                 card_id=APIHelper.SKIP,
                 pan=APIHelper.SKIP):
        """Initialize a SearchCardRestriction instance."""
        # Initialize members of the class
        if card_id is not APIHelper.SKIP:
            self.card_id = card_id
        if pan is not APIHelper.SKIP:
            self.pan = pan

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        card_id =\
            dictionary.get("CardId")\
            if "CardId" in dictionary.keys() else APIHelper.SKIP
        pan =\
            dictionary.get("PAN")\
            if "PAN" in dictionary.keys() else APIHelper.SKIP
        # Return an object of this model
        return cls(card_id,
                   pan)

    def __repr__(self):
        """Return a unambiguous string representation."""
        return (f"{self.__class__.__name__}("
                f"card_id={(self.card_id
                     if hasattr(self, 'card_id') else None)!r}, "
                f"pan={(self.pan if hasattr(self, 'pan') else None)!r})")

    def __str__(self):
        """Return a human-readable string representation."""
        return (f"{self.__class__.__name__}("
                f"card_id={(self.card_id
                     if hasattr(self, 'card_id') else None)!s}, "
                f"pan={(self.pan if hasattr(self, 'pan') else None)!s})")
