"""goeapi.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""


class InfusionType1(object):
    """Implementation of the 'InfusionType1' enum.

    Defines the duration between each infusion the investor makes. If set to
    'yearly', the investor makes an infusion every year on the same day of the
    year. Setting this to 'monthly' means the infusion is made every month, on
    the same day of the month. Idiosyncrasies in the calendar, such as
    28/29/30/31 day months and leap years are already considered.

    Attributes:
        MONTHLY: The enum member of type str.
        YEARLY: The enum member of type str.
        additional_properties (Dict[str, Any]): The additional properties for
            the model.

    """

    MONTHLY = "monthly"

    YEARLY = "yearly"

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
