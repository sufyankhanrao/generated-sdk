# -*- coding: utf-8 -*-

"""
goeapi

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""


class RiskProfile1(object):

    """Implementation of the 'RiskProfile1' enum.

    Defines the risk the investor is willing to take; the returned scenarios
    are chosen/tailored based on this risk. Risk profiles represent increasing
    risk in the following order: conservative > conservatively moderate >
    moderate > moderately aggressive > aggressive. The investor may only
    choose one risk profile.

    Attributes:
        CONSERVATIVE: The enum member of type str.
        ENUM_CONSERVATIVELY MODERATE: The enum member of type str.
        MODERATE: The enum member of type str.
        ENUM_MODERATELY AGGRESSIVE: The enum member of type str.
        AGGRESSIVE: The enum member of type str.
        additional_properties (Dict[str, Any]): The additional properties for
            the model.

    """
    CONSERVATIVE = 'conservative'

    ENUM_CONSERVATIVELY_MODERATE = 'conservatively moderate'

    MODERATE = 'moderate'

    ENUM_MODERATELY_AGGRESSIVE = 'moderately aggressive'

    AGGRESSIVE = 'aggressive'

    @classmethod
    def from_value(cls, value, default=None):
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
